[package]
name = "graph-widgets"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
eframe = { workspace = true }
egui = { workspace = true }
# The egui-agnostic layout engines (ForceAtlas2 worker + tree/radial layouts)
# live here; this crate is the egui adapter that re-exports them with
# `egui::Vec2` signatures, converting at the boundary.
hiker-graph = { workspace = true }
