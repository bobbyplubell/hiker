# Bug tracking

Known issues in the codebase. Each row is a kebab-case slug, a one-line description, optional file:line, and (where useful) a brief note on the intended fix. Same conventions as `status.md` — resolve by fixing in code, then strike the row (or remove it). If a bug ends up reshaping a feature, update the feature row in `status.md` instead of carrying both.

**No `// status: bug-…` markers in code.** Unlike feature slugs (which are stable references future readers may want to grep for), bug slugs are short-lived: once the row here is struck, the slug stops being meaningful. Tagging fixes inline just clutters the source. The git history and this file are enough.


## Open

| Slug | File | Notes |
| ---- | ---- | ----- |
| `bug-session-restore-shows-modal` | session-restore path (frontend) | spec (`autosave.md` `autosave-recovery-auto-restore` + `autosave-tab-state-silent-restore`) says recovered buffers and prior-session tabs auto-restore silently as dirty tabs with **no** modal; the current build shows a modal on session restore. Fix: drop the restore-time modal, restore silently per spec |
| `bug-compute-diff-ipc-payload-unbounded` | the host (`compute_diff`), `core/src/diff.rs` (`compute`) | full `DiffResult` round-trips over IPC as JSON. For a multi-MB note with heavy edits, the payload is multiple MB of inline strings — the diff itself plus every context line — even though the UI already has both `before` and `after` in memory. Naturally bounded once the deferred `diff-viewer-grouped-hunks` variant lands (smaller hunks → smaller payload), and the eventual `mcp-tool-diff` will need a budget anyway. Fix at that point: a `compute_diff_grouped(..., context: u32) -> DiffResult` entry point + per-call size cap; or, if it ever bites in v1, switch the IPC to ts-rs binary serde. **Deferred-paired**: this row stays open as a pointer; the actual fix lands with `diff-viewer-grouped-hunks` |
| `bug-preview-tab-promotion-policy-in-ui` | `app/src/tabs.rs` / `app/src/workbench_host.rs` (preview→sticky promotion sites: edit / double-click / drag / keep-open verb) | the "first edit promotes preview → sticky" state machine + the at-most-one-preview invariant are policy encoded across several UI callsites. Movable into a `core::tabs` state machine (`opened_preview` / `edited` / `pinned` / `closed` events; UI only renders). Lower-priority than the drift / hash slugs — there's no second consumer (MCP/CLI don't have tabs), so the win is purely "policy in one place." Defer until a second consumer appears, but file the slug so the invariant is named |
| `bug-settings-apply-orchestration-in-ui` | `app/src/panels/settings/mod.rs` (the settings-apply path) | the apply step is a pure mapping from `Settings` → list of UI mutations; the *registry of which setting affects which surface* could live in core as a `SettingsImpact` table, with UI dispatching per-surface effects from a typed list. Marginal value at current settings.md size — flagged for completeness, not for near-term action. Revisit if the settings surface grows materially or if a second consumer (e.g. CLI `hiker config set` with live-apply semantics) appears. **Still deferred as of 2026-05-09**: the `bug-main-ts-god-module` extraction moved this into its own module as a pure mapping over per-surface mutator deps, which captures most of the practical win; the remaining core-side `SettingsImpact` table only pays off with a second consumer, and none has appeared (CLI still doesn't live-apply, MCP doesn't observe UI settings) |
| `bug-chat-session-resume-drops-tool-calls` | `core/src/sessions.rs` (`parse_session`), the host chat module (resume path) | Spec update lands first under `chat-session-markdown-store` (`llm.md`): resuming a session must rehydrate the tool-call structure from the markdown's `hiker-tool-call` fenced blocks instead of dropping them. Current behavior matches the prior "lossy resume" rule; the agent now mis-infers it can write notes without tool calls because the resumed history shows no prior tool use. Fix: extend `parse_session` to round-trip tool calls + tool results back into `SessionState.history`; demote `chat-session-markdown-store` in `status.md` from `done` to `partial` until this lands |
| `bug-trails-no-draft-pipeline` | `core/src/trails.rs` (`create_trail`, draft path, `hiker.draft` flag); `core/src/clustering.rs` (proposal emission); the host (MCP `trail_create` wrapper); `app/src/panels/changes.rs` (activity detail page Pending filter); `app/src/sidebar/trails.rs` ("Show drafts" toggle, banner row, proposed waypoint rows) | spec evolved 2026-05-10 to allow agent/clustering-proposed draft trails (`trail-draft-from-agent`, `trail-draft-from-clustering`, `trail-draft-review-surface`). Replaces the prior "user-authored only" rule. Fix touches several layers: (1) `core::trails::create_trail` accepts a `draft: bool` arg; when true, places trail-doc at `<vault>/.hiker/trails/drafts/<trail-id>.md` and stamps `hiker.draft: true` in frontmatter; waypoints still live at `.hiker/trails/<trail-id>/waypoints/`; (2) `trails_list` and the Trails sidebar dropdown filter out drafts unless caller passes `include_drafts=true` / the user toggles "Show drafts"; (3) MCP `trail_create(draft=true)` exposes the new arg; default is `agent-write-review-mode`-aware (review on → drafts default true); (4) clustering pipeline gains an opt-in (`[clustering] propose_trails = false` default) that emits a draft trail when the cluster contains an implicit reading-order chain; (5) trails panel renders a banner row with [Accept trail] [Reject] for draft trails, plus minimal collapsed rows with [Accept] [Reject] for proposed waypoint additions; the activity detail page's Pending filter also lists these with the same Accept/Reject; (6) Trails sidebar dropdown grows a "Show drafts" per-vault toggle (off by default). New planned slugs: `trail-draft-from-agent`, `trail-draft-from-clustering`, `trail-draft-review-surface`. Pre-req: `staging-review-activity-detail-filter` + `staging-accept-reject-from-trails` (both `planned`) — fix can land independently if the draft preview surface uses a minimal trail-panel banner in the interim |
| `bug-editor-settings-not-persisted-and-not-global` | `app/src/panels/buffer/{toolbar_menus.rs,mod.rs::persist_view_setting}`, `app/src/state.rs::set_setting`, `core/src/config/sections.rs` (`EditorConfig`) | **DEFERRED — needs owner decision (2026-05-22 overnight run).** (A) *Persistence:* the View-menu toggles already route `persist_view_setting → AppState::set_setting(Vault) → Config::set` (swaps the in-memory `Arc<RwLock<Config>>`) and are re-read on `Config::load` at vault open; a new round-trip test (`core/src/config/tests.rs::editor_view_toggles_persist_and_reload`) confirms all 11 `[editor]` toggles survive reload at the config layer. So persistence is *wired in code* — but you reported it failing in practice, which a config-layer test can't disprove (a wiring gap between the toggle handler and disk, or a View-menu-vs-Settings-panel split, wouldn't surface there, and the egui GUI can't be exercised headless). **Need from you:** an in-app repro — which setting, toggled where (View menu vs Settings panel), observed vs expected after relaunch — to locate the gap. (B) *Apply-to-all-open-tabs:* this **contradicts the current spec** — `editor.md:419-422` scopes the View menu to "active buffer at most" and "explicitly rejects per-file scoped toggles"; `editor.md:412-413` makes wrap/render-txt a *session-scope override of the vault default*. So "apply to all open tabs" is a **spec change**, not a bug fix. **Need from you:** decide whether to amend `editor.md` to make these editor preferences global across open buffers (→ becomes a real impl task) or keep the speced active-buffer-with-vault-default model (→ part B is wontfix). |
| `bug-windows-onnx-model-not-loading` | ONNX/embedder load path (core embeddings) | On Windows the ONNX model fails to load; possibly already fixed. **Deferred: requires Windows to reproduce/verify; cannot validate on the Linux dev host** |
| `bug-concurrent-edit-compose-length-mismatch-panic` | `editor/editor-core/src/change.rs:198` (`Set::compose` assert), `editor/editor-core/src/history.rs` (coalesce path, ~line 121), the editor↔CRDT binding (`app/.../editor_binding.rs`) | **HIGH — hard crash during concurrent editing.** `thread 'main' panicked … compose length mismatch: 311 vs 318`. `Set::compose` asserts `self.len_after == other.len_before`; the 7-char gap is an agent op advancing the doc out-of-band (accepted `working` state pulled in via the reverse step) between two user keystrokes, so the second keystroke's changeset — computed against the pre-agent-op doc — is composed onto a doc that already moved. Surfaces in undo-history coalescing (inverse compose). Fix is NOT a one-liner: the coalescing assumption "consecutive keystrokes apply to the same base" is broken by the layered (accepted+working+pending) model; needs a rebase/transform step (or coalesce-abort) when the doc advanced out-of-band since the last recorded change. Ties into `[[project_oplog_crdt_followups]]` correctness pass. Repro: user types while an agent edit is accepted into the same buffer |
| `bug-double-click-word-select-needs-repro` | `editor/editor-view/src/command.rs:929` (`mouse_down`, `2 => word`), `editor/editor-egui/src/translate.rs:125` (click_count) | user asked for double-click smart (word) highlighting. Investigation found it's **already wired** — double-click selects a word via `unicode_word_indices` (Unicode-correct, punctuation-aware), triple-click selects the line, with a regression test in `editor/editor-view/tests/multiclick.rs` (see resolved `bug-no-progressive-multiclick-selection`). Either already satisfied or broken only in-app at the egui translate layer. **Needs an in-app repro** (which build, what was clicked, observed vs expected) before any code change |
| `bug-editor-no-scroll-cursor-into-view-on-edit` | `editor/editor-view/src/command.rs:402` (`insert_text`), `editor/editor-view/src/viewport.rs:193` (`scroll_y`), `command.rs:1204` (`scroll_by`, wheel-only) | typing/editing does not snap-scroll the caret into view; `scroll_by` exists only for wheel scroll and there's no "ensure caret visible" method. The omission is general (motion keys don't scroll-into-view either). Fix: add `ViewState::scroll_cursor_into_view(state)` clamping `scroll_y` so the caret line is within the viewport (respect `scroll_past_end`), call it after edits — and decide whether motion/selection should also trigger it. Moderate; the scope decision (edit-only vs all caret moves) wants an owner call |
| `bug-minimap-on-still-hurts-scroll-perf` | `editor/editor-egui/src/minimap.rs:975` (`paint_marks`/`paint_thumb` per-frame), `:995-1036` (`paint_marks`) | despite the resolved `bug-minimap-scroll-perf-large-files` (texture cache, rebuild-on-change keyed off `content_id`+decoration signature; the texture early-returns on scroll frames), the user still reports scroll lag with the minimap on. The texture path looks correct; the remaining per-frame cost is the off-texture overlays — `paint_marks` walks every selection range + search match and does a `height_map.text_height(line)` lookup per match, every frame (heavy with many search hits / large selections). Fix: **profile first**, then memoize the per-line height lookups across the loop (or cap/aggregate marks). Don't reopen the texture path |
| `bug-snapshot-hide-diff-shows-buffer-not-snapshot` | `app/src/panels/buffer/mod.rs:970-985` (snapshot diff toggle), `app/src/tab.rs:163` (snapshot opens with `diff: Some(Disk)`), `app/src/panels/buffer/diff_overlay.rs:209`, spec `docs/diff.md:66` | reported: in the snapshot preview (bottom-left version dropdown), 'hide diff' shows the *current buffer* rather than the plain snapshot. Snapshot tabs open with diff pre-set to `Disk` (snapshot-vs-now). Per-spec the buffer rope always holds the snapshot and toggling diff only adds/removes decorations, so 'hide diff' should leave the snapshot visible — investigation could not confirm a path that swaps in the live buffer, so **needs an in-app repro** to pin whether the doc is actually being replaced on this branch (note `op-log` branch has a newer `diff_overlay.rs` alongside the older decorations path). **User's preferred end state (feature, not just a fix):** add a diff toggle to the *standard* editor and fold the snapshot diffing controls into that one unified surface — already the direction in `docs/diff.md`; spec/feature work, file under `status.md` if pursued |
| `bug-no-device-name-transfer-on-enroll` | `hiker-sync/src/identity.rs` (`DeviceFingerprint`), `hiker-sync/src/protocol.rs:51` (`Hello`/`HelloAck`), `app/src/sync_service.rs:156` (local `aliases.json`), `app/src/panels/sync.rs`, spec `docs/sync.md` | **feature request, needs spec update — not a bug.** Today device identity is the immutable Ed25519 fingerprint; human names exist only as *local-only* aliases (`aliases.json`, user-scope, per-device, never synced) settable on the Sync page. The user wants to name *this* device and have that name transfer on enrollment. Needs: a `device_name` field on the `Hello`/`HelloAck` handshake, vault-scope (`[sync]`) storage of fingerprint→name so all peers see it, and a naming-ownership decision (self-named vs enroller-named, conflict resolution). Spec `sync.md` should define this first; ties into `[[project_sync_impl]]` (vault-key-inband / enrollment still being built) |
## Resolved

Fixed 2026-05-27 (triage batch — quick fixes):

| Slug | Fix |
| ---- | --- |
| `bug-crlf-backspace-leaves-stray-cr` | `delete_at_selections` in `editor/editor-core/src/state.rs` handled an empty-selection Backspace by stepping back one UTF-8 char (`back = start-1`, walk while `!is_char_boundary`), so at a `\r\n` boundary it deleted only `\n` and left a stray `\r`. Fix: after computing `back`, if the slice `back..start` is `"\n"` and the byte before `back` is `b'\r'`, decrement `back` once more so the whole `\r\n` is removed as one unit; lone `\r`, lone `\n`, normal chars, and multi-byte chars are unaffected. Tests `backspace_crlf_deletes_both_bytes` + `backspace_lf_only_deletes_one_byte` in `state.rs`. |
| `bug-oplog-bootstrap-nonutf8-warn-spam` | The op-log bootstrap (`core/src/ops/op_writes.rs`) re-read non-utf-8 files every run and re-logged the WARN (no skip marker, unlike the indexer). Added a persistent `bootstrap_skipped (path, skip_reason, skipped_at)` table to the op-log's `doc-index.db` (`core/src/oplog/meta.rs`, `CREATE TABLE IF NOT EXISTS` — no version bump) with `put_bootstrap_skip` / `is_bootstrap_skipped` helpers, surfaced on `OpLog` as `mark_bootstrap_skipped` / `is_bootstrap_skipped` (`core/src/oplog/mod.rs`). Bootstrap now checks the marker before `read_file` (silent skip on later runs), and on a read failure logs the WARN once then records the marker. Skipping non-utf-8 stays the behavior — this only kills the repeat re-read + log spam. Test `bootstrap_skips_non_utf8_and_persists_skip_marker` in `core/src/ops/tests.rs` (two bootstraps; second is a no-op). |
| `bug-chat-input-selection-highlight-invisible` | The global selection fill in `app/src/theme.rs` used `from_rgba_premultiplied(0xb8,0xd1,0xff,0x99)` — premultiplied RGB at ~60% made an already-pale blue nearly invisible over the light panel bg. Switched to `from_rgba_unmultiplied(0x2f,0x6f,0xed,0x70)` — the full accent blue (same token as links / the selection stroke) at ~44% alpha, so the highlight reads clearly against both the chat input and the near-white editor surface while staying theme-cohesive. Global (every text input + editor), color kept in the theme palette. |
| `bug-editor-caret-no-blink` | The focus-gated caret in `editor/editor-egui/src/widget.rs` painted solid every frame (no blink). `paint_cursors` now computes visibility from `view.last_interaction.elapsed()`: solid for the first 530ms after any interaction (never blinks mid-typing), then a 530ms-half-period on/off blink while idle + focused; unfocused still early-returns (hidden). `paint()` schedules `ctx.request_repaint_after(time-until-next-phase)` (clamped ≥1ms, focus-only) instead of an unconditional per-frame repaint, so the blink animates without spinning CPU. `last_interaction` is already `touch()`-reset on every keyboard + mouse interaction (`editor/editor-view/src/command.rs`), so the caret shows solid the instant the user acts. 37 editor-egui tests pass. |

Fixed 2026-05-22 (overnight bug run):

| Slug | Fix |
| ---- | --- |
| `bug-tree-drag-blocks-expand-click` | `paint_row` in `app/src/sidebar/clusters/tree.rs` wrapped the whole row (including the chevron `ImageButton`) in `dnd_drag_source`, which begins a drag on press with *no* motion threshold and claims the pointer for the source rect — so `chev_resp.clicked()` never fired. (The left-panel Files tree works because it uses `dnd_set_drag_payload` after a plain allocation, which only starts a drag past egui's motion threshold, leaving press-release as a `clicked()`.) Mirrored that pattern: allocate the row with `Sense::click_and_drag()`, paint the chevron as its own overlapping click target, attach the `DragNode` payload via `dnd_set_drag_payload`. Click-to-expand and drag-to-reparent now coexist; drop handling unchanged. Covers both review + completed trees (one `show_tree` path). No unit test (egui interaction; matches the sibling tree's no-test culture). |
| `bug-gutter-toggle-mouse-offset` | The painter placed text at `left + (hide_gutter ? 4.0 : gutter_width)` (`editor/editor-egui/src/widget.rs`), but the inverse click→byte mappers (`view_to_buffer`/`view_to_buffer_at_line` in `editor/editor-view/src/command.rs`) unconditionally subtracted the full `gutter_width`, so with line numbers OFF the caret landed ~52px left of the pointer. Added `ViewState::content_origin_x()` (const fn, single source of truth: gutter width shown / 4px pad hidden) and routed paint, hit-test, `text_area_w` wrap width, tooltip (`tooltip.rs`), and completion popup (`completion.rs`) x through it — the same divergence existed latent in tooltip/completion. Doesn't touch the `bug-highlight-broken-header-lines` column math (operates downstream on a content-relative x). Tests in `command.rs`. |
| `bug-new-tab-not-scrolled-into-view` | In `egui-workbench/src/editor_area.rs` (the tab strip lives entirely here; the bug-row `app/src/tabs.rs` pointer was a red herring). Added a persistent `last_active_per_group: HashMap<TileId, TabId>` (per-group because splits each have their own active tab); `tab_ui` calls `tab_response.scroll_to_me(None)` when the active tab differs from the group's last-seen active, then updates the tracker. Gated by the pure predicate `should_scroll_active_into_view(is_active, prev, tab)` so it fires only on the activation-change frame and never overrides the user's manual horizontal scroll; `replace_tree` clears the tracker so a restored active tab scrolls in. 3 unit tests. |
| `bug-all-tabs-scroll-together` | Input-routing bug, not state ownership (`ViewState.scroll_y` is already per-buffer; bug-row pointer was stale). The editor body's wheel-scroll gate in `editor/editor-egui/src/widget.rs` was `response.hovered() || response.has_focus()`. egui's `smooth_scroll_delta` is a single global per-frame value, so a focused-but-not-hovered split pane consumed the same delta and scrolled in lockstep with the hovered pane. Gate on `hovered()` alone (matches the minimap + tab-strip wheel handlers). Keyboard PageUp/PageDown uses the focus-gated key path, so nothing is lost. Regression test `focused_but_unhovered_editor_does_not_scroll` in `editor/editor-egui/tests/regressions.rs` (verified it fails with the old condition). |
| `bug-minimap-scroll-perf-large-files` + `bug-minimap-no-selection-highlight` + `bug-minimap-scroll-only-drag` | All in `editor/editor-egui/src/minimap.rs`. (1) Perf: added a host-owned `minimap::Cache` memoizing per-line metrics (`measure_lines`, keyed on `doc.content_id()`) and structural classification (`classify_lines`, keyed on `(content_id, decorations.signature)`); the per-frame O(lines) measure+classify is now an O(1) cache check on scroll/idle frames. Cache field lives on `Buffer` (`app/src/buffer.rs`), mirroring `PaintCache` so editor-view stays egui-free; threaded via `.with_cache(...)` in `app/src/panels/buffer/mod.rs`. (2) Selection/search marks: paints colored strips for lines touched by a non-empty selection range and (when find is active) search matches — both ranges already computed, no extra scan. (3) Wheel scroll: hovering the strip with no button down routes the wheel delta through `command::handle(InputEvent::Scroll)`, reusing the editor body's clamp (incl. `scroll_past_end`). 6 unit tests in editor-egui. Perf/pixels need an in-app eyeball. |
| `bug-trails-drag-no-between-target` | The Trails sidebar (`app/src/sidebar/trails.rs`) already supported drop-between internally — `resolve_drop` split the card at its center (top half = `MoveOp::Before`, bottom half = `MoveOp::Child` nest) and two discrete grey strips (`head_drop_zone` / `tail_drop_zone`) handled head/tail. Replaced the center split with a three-band scheme decided by a pure predicate `drop_band(pointer_y, top, bottom) -> Above \| Into \| Below` (top/bottom 30% edge bands = insert sibling above/below, wide middle = nest as child). Added `MoveOp::After(target)` (sibling insert at idx+1) to the reorder backend in `state.rs`; bottom band of the last card = trail tail, top band of the first card = head, so the grey `head_drop_zone`/`tail_drop_zone` strips were deleted entirely. A single accent insertion-line is painted at the target edge (or a card outline for nest) while a drag hovers, mirroring the clusters-tree feedback. Reparent-into-side-trail preserved per `trails-dnd-ingestion` / side-trail model. Unit tests: `drop_band_tests` (4) + `MoveOp::After` (3) in `state.rs`. Drag interaction itself needs an in-app eyeball. |
| `bug-live-preview-reflow-spazz-below-cursor` | Root cause: a one-frame decoration/doc mismatch on edit frames. The host rebuilds decoration layers against the previous frame's doc (`app/src/panels/buffer/mod.rs:311`+), but the keystroke is applied inside the widget (`editor/editor-egui/src/widget.rs` `command::handle`). On an edit frame `state.doc` is post-edit while the height-affecting `markdown` layer still describes the pre-edit doc, so `apply_line_height_decorations` maps stale `range.start`s through the new doc and assigns the wrong line the tall heading height for one frame (jump-then-settle, worst on headings below the cursor); newline edits additionally hit `sync_to_lines` clearing all overrides. Fix in `widget.rs::measure`: detect the stale frame via `pre_doc_id != doc_id` and, on it, skip applying the mismatched ranges, keep last frame's height overrides (new non-destructive `HeightMap::set_line_count` in `viewport.rs` instead of the clearing `sync_to`), and force an immediate repaint; the next frame (fresh host decorations) re-derives. Single-char edits with no line-count change become a no-op, so heights below the cursor stay stable. Tests in `viewport.rs`. Confirm in-app: type rapidly above headings — they should no longer jump. |
| `bug-decoration-heightmap-footgun` | Implemented the owner's recommended fix. Added a `HeightAffecting` trait + `affects_height: bool` field on `RangeSet<T>` in `editor/editor-core/src/rangeset.rs` (computed once at construction by folding entries); `impl HeightAffecting for Decoration` in `decoration.rs` returns true for `Line{hide:true}` / `Line{height_scale:Some(_)}` / `Block(_)` / `BlockWidget{..}`. `DecorationLayers::push` in `editor/editor-view/src/viewport.rs` now auto-routes: height-affecting sets go to `push_with_heights`, others take the signature-mix path — preserving the height-driver's height-layers-only scan (flag computed at build time, not per frame). `push_with_heights` kept as an explicit override; the app already calls it directly (`app/src/panels/buffer/mod.rs:328,438`), so app behavior is unchanged. `editor/examples/markdown` (which only ever called `push`) now routes its height decorations correctly with no edits — that was the live footgun. Tests in editor-core + `editor/editor-view/tests/decoration_height_routing.rs`. **Note:** the bug-row file pointer was stale — the `push`/`push_with_heights` machinery lives in editor-view, not editor-core. |
| `bug-highlight-broken-header-lines` | Root cause was the inverse mapper (pointer-x → source byte) in `editor/editor-view/src/command.rs`, not the painter. It counted columns from byte 0 of the full source line (including the `## ` prefix) and divided by the base glyph width. On a header line two errors compounded: the leading `#`/`## ` renders as a zero-width `Replace` (first visible glyph is the content), and header glyphs render at `Mark.font_scale`. So clicks/drags selected the wrong source bytes and the highlights (painted correctly by the already-correct forward path) landed on the wrong glyphs. Unified `view_to_buffer`/`view_to_buffer_at_line` onto `col_x_to_line_byte`, which divides x by `base_char_w * scale` and skips contiguous leading hidden-`Replace` markers (char-boundary-safe). Handles only leading markers; mid-line marks use the verbatim walk. Tests in `command.rs`. Drag-selection pixel alignment over headers worth an in-app eyeball (can't verify headless). |
| `bug-inline-triple-backtick-breaks-codeblock` + `bug-multiple-codeblocks-mis-detected` | Single root cause: `style_fenced_code_block` in `editor/editor-md/src/styling.rs` decided which lines were fences by position in pulldown-cmark's block range (first/last line), which misfired on unterminated fences (phantom closer) and on lines that merely *contain* a triple-backtick. Replaced with content-based `is_fence_line(line, allow_info)`: a fence is a line that, trimmed, is only a 3+ run of `` ` ``/`~` plus (on an opener) an optional info string — closers must be bare, and a backtick info string may not contain a backtick. Each qualifying line flips fence state, so inline ` ```x``` ` never opens a block and multiple blocks pair independently. Per-block cursor reveal (`live-preview-code-fence-block-reveal`) preserved. 5 regression tests in `editor/editor-md/tests/integration.rs`. |
| `bug-paste-list-item-duplicate-bullet` | Added an `on_paste(&self, state, pasted) -> Option<String>` hook to the `IndentProvider` trait (`editor/editor-view/src/viewport.rs`, default `None`), implemented by the markdown indenter (`markdown_strip_bullet_on_paste` in `editor/editor-md/src/indenter.rs`) reusing `parse_list_line`/`cursor_list_line`. The paste branch in `editor/editor-view/src/command.rs` consults it before inserting. Strip fires only when the caret is a single empty selection at the list line's content-start (right after `<indent><marker> `) and the pasted first line is a list item with the *same marker token* — then the pasted line's own marker prefix is stripped, so no doubled bullet. Marker-token matching means ordered (`1. `) / `*` / `+` are handled symmetrically without special-casing. No editor-view → editor-md dependency added. Tests in `editor/editor-md/tests/indent.rs`. |
| `bug-no-progressive-multiclick-selection` | Progressive selection was already wired end-to-end (`editor/editor-egui/src/translate.rs` computes `click_count` 1/2/3 from egui's `button_double_clicked`/`button_triple_clicked`; `editor/editor-view/src/command.rs` `mouse_down` maps `2 => word`, `3 => whole line`), but had no regression test guarding it — the bug-row premise ("view never maps cc=3 → select-line") was stale, likely fixed during the egui integration. Added `editor/editor-view/tests/multiclick.rs` locking in double-click=word and triple-click=line (incl. last-line-without-newline) so the upcoming CRDT switch can't silently regress it. No code change needed. |
| `bug-cut-not-copying-to-clipboard` | The `Action` enum carried a single outcome, so the old `InputEvent::Cut` handler had to pick between `Copy` and `Replace` and chose `Replace` — losing the clipboard write (an explicit "hack for v1" `let _ = text`). Added `Action::Cut { text, state }` to `editor/editor-view/src/command.rs` carrying both halves; `editor/editor-egui/src/widget.rs` `consume_action` now does `ctx.copy_text(text)` then replaces state. Clipboard text still comes solely from `copy_selection` (no duplication). Empty-selection cut takes the whole line (mirrors Copy) — a follow-up commit added `Editor::delete_ranges` + a shared `cut_range` helper so the empty-selection *delete* spans the same whole line as the copy (the first cut landed `delete_at_selections`, which only backspaced one char). New tests in `editor/editor-view/tests/cut.rs`. |
| `bug-autopair-backtick-overzealous` | Rule (a) type-over the auto-closer was already implemented (`autopair_skip`). Added rule (b) in `editor/editor-view/src/pairs.rs`: `autopair_transform` now returns `None` (plain insert, no closer) when any cursor has a non-whitespace char immediately to its right — so surrounding existing text with backticks (or any pair) no longer injects a stray closer. Whitespace / doc-end still auto-close. Applies uniformly to all pairs (the machinery is generic; no backtick special-case). Tests in `editor/editor-view/tests/autopair.rs`. |
| `bug-editor-no-context-menu` | New `app/src/panels/buffer/clipboard_menu.rs` attaches a right-click menu (Cut / Copy / Paste / Select All) to the editor text response. Verbs deliberately reuse the editor's existing input path: Cut/Copy/Paste are issued as egui `ViewportCommand::RequestCut/Copy/Paste` (eframe injects the same `Event::Cut/Copy/Paste` the OS shortcuts produce, routed through the one clipboard path); Select All injects a primary-modifier `A` key event the loop maps to `motion::select_all`. Each verb requests editor focus first. Placed in a sibling module rather than inflating `mod.rs` past the length cap. |
| `bug-buffer-ahead-of-index-stuck` | The badge condition recomputed every frame but compared against an indexer hash cache that only refreshed on a coarse 2s timer, so after the index caught up the "buffer ahead of index" badge lingered (read as stuck). Added `Buffer::is_ahead_of_index()` (pure predicate over `index_hash_cache` vs live `current_hash()`) + an `index_pending_last` field tracking the indexer's last-seen pending state, both in `app/src/buffer.rs`. The status-bar refresh in `app/src/panels/buffer/mod.rs` now forces an immediate stored-hash re-read on the indexer's pending→done edge (`Handle::is_pending`), not just on the 2s timer, so the badge clears at once. Unit tests (`index_badge_tests`) cover never-indexed / diverged / catches-up. Logic relocated to `buffer.rs` (where `index_hash_cache`/`current_hash` live) to keep `mod.rs` under the length cap — genuine relocation, not a suffix split. |
| `bug-author-metadata-flattened-at-accept` | Added a nullable first-class `surface` column to `core/src/changes.rs` (`ChangeRow`/`ChangeAppend`, schema v2→v3 additive `ALTER TABLE … ADD COLUMN surface TEXT`, with the `migrate` fn refactored into `migrate_v1_to_v3`/`migrate_v2_to_v3`/`reencode_content_zstd` to stay under the clippy cognitive-complexity budget — genuine extraction, no `#[allow]`). Both staging accept paths in `core/src/staging/ops.rs` (whole-file/edit write + `accept_move`) now pass `surface: Some(&proposal.surface)`; all other `ChangeAppend` sites (direct user/agent writes) pass `surface: None`. `author` stays `"user"` on accept (correct per `changes.md` — acceptance is a user decision), but the originating producer (`"triage"`, etc.) is preserved as a queryable field instead of buried in the metadata JSON. Tests in `core/src/staging/tests.rs` assert the accepted write/rename carry `surface == Some(...)` with `author == "user"`. Mirrors `op-log.md`'s `op-log-side-table` direction. **Spec drift to reconcile:** `changes.md:158` still documents the old "surface lives in metadata" behavior. |
| `bug-list-bullet-no-tab-indent` | Extended the `IndentProvider` trait (`editor/editor-view/src/viewport.rs`) with `on_tab` / `on_shift_tab` hooks (default `None`), implemented in `editor/editor-md/src/indenter.rs` (`markdown_indent_on_tab` / `markdown_outdent_on_shift_tab`) reusing the existing `parse_list_line` recognition via a new shared `cursor_list_line` helper. When the caret is on a list item, Tab inserts one indent-step (4 spaces) before the line's existing indentation and Shift+Tab removes up to one step (or a single leading tab); both leave the selection to map through the change so the caret tracks the line. The command dispatch in `editor/editor-view/src/command.rs` consults the provider before the default `indent_tab` / `shift_tab_outdent`, so non-list lines and outdent-at-column-0 fall through unchanged. Tests in `editor/editor-md/tests/indent.rs`. |
| `bug-backspace-not-removing-tab-width-spaces` | Added a module-level `backspace_outdent` helper + `TAB_WIDTH` const in `editor/editor-view/src/command.rs`; the Backspace branch now tries it before `delete_at_selections`. It deletes a whole tab-width group only when the caret is a single empty selection sitting in its line's leading indentation, that indentation is all spaces, and the run before the caret is a non-zero multiple of the tab width — so grouping is confined to leading indentation and partial runs / mid-line text / tab chars fall back to single-char delete. `indent_tab` now inserts `" ".repeat(TAB_WIDTH)` from the same const so insert and delete widths stay in lockstep. Tests in `editor/editor-view/tests/backspace_indent.rs`. |

Fixed 2026-05-13 (write-note review):

| Slug | Fix |
| ---- | --- |
| `bug-write-note-pending-banner-oversized` | The write-note pending banner (inserted between the toolbar and the editor surface in `app/src/panels/buffer/mod.rs`) ballooned to roughly two-thirds of the pane height instead of staying at content height. Fix constrains the toolbar / banner / status-bar to content height while the active content surface absorbs the remaining vertical space. Banner kept its existing amber strip styling. |
| `bug-write-note-review-redundant-exit-x` | The write-note-review mode controls (`app/src/panels/buffer/mod.rs`) rendered an Exit button next to Accept / Reject, duplicating the agent-diff toolbar toggle's exit affordance (forbidden by `patch-review.md:52`). Dropped the redundant "Exit review" button. Also removed the "Accepted" toast on accept — the buffer reload after opening the target path is the user-visible confirmation; the toast was un-specced chrome. Reject's toast stays (the user-visible signal there is just the mode exit, which is easy to miss). Updated `docs/patch-review.md:107` to drop "Exit" from the write-note-review verb list and `docs/status.md:739` to remove the "Drift as of 2026-05-13" note. |
| `bug-write-note-review-accept-stale-proposal-id` | Already addressed in the chat panel (`app/src/chat/render.rs`, landed alongside `bug-chat-tool-card-stale-after-cross-surface-accept`). The Accept handler now resolves live proposal ids at click time, which for single-id whole-file proposals (`write_note` / `set_frontmatter` / `apply_tag`) queries the staging list for the target path, filters by matching `action` + `target_path`, and picks the newest live proposal — falling back to the captured id when the lookup turns up nothing. Batch (`edit_note`) shapes use the captured ids directly since they're authoritative for the lifetime of the batch. The result is that a chat-card Accept on a `write_note` rewrite resolves against whatever proposal is currently pending, even when the originally-captured id was replayed/reissued through staging churn. |
| `bug-patch-review-gutter-not-restored-on-exit` | The patch-review gutter (`app/src/panels/buffer/decorations.rs`, wired in `app/src/panels/buffer/mod.rs`) is only meaningful during a review session, but its widened reserved track (hosting the per-hunk Accept/Reject buttons) stayed in place after the mode exited. Fix: the gutter is only widened while patch-review mode is active and collapses back to zero width on exit; per-hunk rendering keeps working because only the containing track's reserved width is collapsed. |

Fixed 2026-05-13 (chat tool-card):

| Slug | Fix |
| ---- | --- |
| `bug-chat-tool-card-no-link-for-staged-writes` | Added an open-staging-proposal seam to the chat panel (`app/src/chat/render.rs`); the host wires it through the same proposal-review dispatcher the vault-home activity widget / file tree use (which routes so `note-open-routes-to-pending-review` lands the user in patch-review for `edit_note` and write-note-review for whole-file proposals). The tool-card head click now, for note-touching tool calls (`get_note`, `write_note`, `edit_note`, `set_frontmatter`, `apply_tag`, `remove_tag` — `edit_note` was added per the bug row), resolves the touched note's rel-path (`rel_path` / `path` on the result, args fallback) and any staged ids (`staging_id` singular for whole-file proposals, `staging_ids` plural for `edit_note` batches), then routes through the open-staging-proposal seam when any captured id is still pending, falling back to the open-note-link seam when the proposal has resolved. Non-touching tools keep the prior toggle-card behavior. The chevron click still expands/collapses the JSON unaffected. The broader `chat-tool-call-opens-touched-note` feature stays `planned` in `status.md` — this lands the staged-write prong plus the routing seam, but not the multi-touch picker. |
| `bug-chat-tool-card-stale-after-cross-surface-accept` | The chat panel (`app/src/chat/render.rs`) now reacts to staging-changed events and maintains a local pending-staging-ids set refreshed from the staging list. Accept/Reject button rendering is driven both on `tool_result` and on every staging-changed event; buttons are shown only when at least one of the card's captured `staging_id` / `staging_ids` is still in the live pending set. A cross-surface accept/reject (activity-detail row, tree context-menu, staging-preview surface) therefore clears the chat card's buttons live, and an in-card accept/reject re-renders authoritatively after the staging-changed event echoes back. The existing `bug-write-note-review-accept-stale-proposal-id` fix (resolving live proposal ids inside the handler) was preserved. |

Fixed 2026-05-13 (staging surface routing):

| Slug | Fix |
| ---- | --- |
| `bug-staging-accept-navigates-from-activity-detail` | The vault-home accept path (the activity-detail / home-widget surface — both the detail-page proposal rows and the home overview pending-preview rows route here) no longer navigates to the accepted target. Accept now just runs the staging-accept action and, when the target happens to already be the active buffer in plain `"file"` mode, reloads its contents from disk in place (dirty → toast warning, clean → silent reload + dispatch the fresh body) so the user sees the result of the accept without leaving the list. Tree-context (`app/src/sidebar/files.rs`) accept and staging-preview accept paths are unchanged — they still navigate to the target, matching the chat-card behavior. `status.md`'s `staging-accept-navigates-to-preview` row flipped from `partial` to `done` and reworded to call out the activity-detail carve-out. |
| `bug-activity-pending-row-skips-patch-review-mode` | Two-part fix in the file-open path (`app/src/actions.rs` + `app/src/panels/buffer/mod.rs`). (1) Proposal-review opening now delegates to the shared file-open entry point instead of opening the file and then separately entering patch-review mode, so every pending-row click path (vault-home proposal / pending-preview rows, tree context-menu Select-version, autosave staging-preview restore) shares one entry point. (2) The file-open path itself now honors the `note-open-routes-to-pending-review` contract for `edit_note` proposals: if pending `edit_note` proposals exist for the path, open the file as normal and then enter patch-review mode once the buffer is active. Whole-file proposal routing into write-note-review is unchanged. The misleading comment claiming `edit_note` proposals "do NOT auto-route" was deleted — that drift was the proximate cause of the bug, and it now matches what `status.md:720` says. |

Fixed 2026-05-13 (nav back):

| Slug | Fix |
| ---- | --- |
| `bug-nav-back-from-recent-activity-skips-home` | Recent-activity drill-in never opened a `home-detail` app-page tab — both vault-home entry points (the widget header and the per-row activity-preview click) switched to the recent-activity detail view in place rather than through the tab-open path, so the active tab stayed the `home` kind and no nav-history checkpoint fired. Pressing Back from the visible "Recent activity" surface therefore ran Back from the `home` nav-stack entry, which correctly popped to the previously-open note — skipping past Home because the Home → Recent-activity transition was never recorded. Fix: route both clicks through the tab-open path (`app/src/workbench_host.rs`) that opens a `home-detail` recent-activity tab, evicting the home preview and recording the nav checkpoint, so the nav stack now records `[…, home]` with `current = home-detail` and Back pops to Home as expected. The detail-view flip itself is unchanged — the bug was purely the click handlers bypassing the tab-open seam. |

Fixed 2026-05-13 (menu trigger toggle):

| Slug | Fix |
| ---- | --- |
| `bug-menu-trigger-reopens-instead-of-toggling` | The dropdown-toggle guard added by `bug-dropdown-button-click-toggles-not-closes` ("same trigger + open menu → close") never fired for the Mutations / View Options buttons (`app/src/toolbar.rs`) because the outside-click dismissal ran before the trigger button's own click handler, so the click then saw a closed state and re-opened. Fix: the outside-click dismissal now short-circuits when the click target is the open menu's trigger, deferring dismissal to the trigger's own click handler, which hits the same-trigger toggle branch and closes cleanly. Callsites without a tracked trigger are unaffected. |

Fixed 2026-05-13 (snapshot diff toggle):

| Slug | Fix |
| ---- | --- |
| `bug-snapshot-preview-diff-toggle-broken` | The snapshot-preview diff toggle (`app/src/panels/buffer/decorations.rs`) applied the diff doc-replacement and decorations against a buffer that the preview had pinned read-only on open, so the doc-replacement (unified diff text on entry; snapshot blob on exit) and the decoration application both silently no-op'd — click did nothing, no error. Fix: mirror the dirty-buffer-diff shape — lift read-only immediately before the diff-direction's doc-replacing write and always restore it after (covers the in-flight-guard-stuck case too). Round-trip verified on both the off→on and on→off branches. The read-file path was not the culprit; left unchanged. |

Fixed 2026-05-13 (tree dirty dot only on active tab):

| Slug | Fix |
| ---- | --- |
| `bug-tree-dirty-dot-only-on-active-tab` | The file tree (`app/src/sidebar/files.rs`) only marked the active buffer's row as dirty and cleared the marker from every other row — so a background tab with unsaved edits lost its dot the instant the user switched to a sibling tab. Fix: build the set of paths to mark by walking every open buffer and applying the same per-entry dirty check the tab strip uses (saved-vs-loaded text for background entries, live dirty state for the active one). Every row with a dirty open buffer keeps its dot regardless of which tab is active. Pending agent-review files were already covered by the staging-dirty marker applied during the tree render, so no change was needed for that prong of the user's ask. |

Fixed 2026-05-13 (staging list order non-deterministic within same-ms batch):

| Slug | Fix |
| ---- | --- |
| `bug-staging-list-order-flaky-within-batch` | `build_list_query` in `core/src/staging.rs` ordered by `created_at_ms ASC, id ASC`. ULIDs share their 48-bit timestamp prefix within the same millisecond but have random 80-bit tails, so two rows inserted by one `propose_batch` call (same ms) sorted in an order that didn't match insertion order. The test `propose_batch_assigns_shared_batch_id_and_per_edit_payloads` flaked accordingly. Switched the tiebreaker to `rowid ASC` — SQLite's hidden rowid (the table has `id TEXT PRIMARY KEY`, not `WITHOUT ROWID`) increments with insertion order and is stable. |

Fixed 2026-05-13 (tab-strip click swallowed mid-edit):

| Slug | Fix |
| ---- | --- |
| `bug-tab-switch-requires-double-click-after-edit` | With ≥2 dirty buffers open, an edit followed immediately by a click on another tab required two clicks to switch. Root cause was in the tab strip (`app/src/workbench_host.rs`): a tab-strip rebuild triggered by the post-edit/blur status refresh landed between the tab's press and release, so the first click was swallowed. The "only after a new edit" pattern is just the case where the recent doc-change makes the surrounding render path active. Fix: memoize the rendered tab-strip signature (active path, plus per-tab path / basename / folder / dirty / preview / kind) and short-circuit when nothing changed since the last render, so the tab strip stays stable across no-op re-renders and the click lands on the first attempt. Also a free perf win — every keystroke previously rebuilt N tab buttons. |

Fixed 2026-05-13 (write-note review drops dirty buffer on exit):

| Slug | Fix |
| ---- | --- |
| `bug-write-note-review-reject-discards-dirty-buffer` | Entering write-note-review (`app/src/panels/buffer/mod.rs`) replaced the live editor with the proposed content without first persisting the outgoing file-mode buffer's state — the user's unsaved edits only existed in the live editor, so they were silently dropped. Exiting then cleared the editor on reject (or Exit) instead of restoring the prior tab, so the dirty buffer was unreachable even though the open-buffer entry still existed. Fix: (1) on entry, snapshot the outgoing file-mode buffer's state (same persist step a tab switch already does); (2) on exit, if a file-mode buffer for the target path is still open, re-activate it (state, dirty content, undo history) — otherwise fall back to the previous "clear editor" behavior. Accept path was unaffected because `write-note-review-blocks-on-dirty` refuses accept while any open buffer for the target is dirty. |

Fixed 2026-05-13 (staging storage moves to SQLite):

| Slug | Fix |
| ---- | --- |
| `bug-staging-storage-not-sqlite` | Rewrote `core/src/staging.rs` against a SQLite `<vault>/.hiker/staging.db` (single `proposals` table, schema per `settings.md` "Storage"). Content lives in a zstd-compressed BLOB column (level 3, same encoding as `changes-content-zstd`); per-id `.md` sidecars removed. `list(StagingFilter)` now runs an indexed SQL query (filter clauses on `target_path`/`surface`/`state`/`trail_id`/`json_extract(metadata,'$.session_id')`) instead of scanning an in-memory array. WAL + `synchronous=NORMAL`, `pragma user_version` with fail-loud schema-version mismatch. Pre-1.0 — no legacy migration code; users with the prior JSON+sidecar layout delete `.hiker/staging/` to start fresh. `staging_content` command unchanged (delegates to `Staging::content`, which now reads the BLOB). Public API of `core::staging` is unchanged; callers don't move. |

Fixed 2026-05-13 (nav history across app-page tabs + "app pages" naming cleanup):

| Slug | Fix |
| ---- | --- |
| `bug-nav-history-broken-for-app-page-tabs` | Three independent gaps fanned out into one failure mode (nav stack stuck / skipping). (1) Nav-state inference (`app/src/workbench_host.rs`) now derives state from the active tab's kind + payload — parsing the `home-detail` view and `properties` path out of the tab identity — instead of reading stale view flips. Added a `properties` nav-state variant with a matching apply branch (`app/src/panels/properties.rs`). (2) Dropped a stale observer that double-recorded nav transitions: every kind switch already goes through tab activation, which ends with a nav checkpoint, but the observer fired again afterward and occasionally read mid-transition state. (3) Navigation now captures the previous nav entry before applying the new one. Applying an app-page navigation evicts the active preview tab and prunes it from the nav stack; the prune nulled the current entry when it matched, so the subsequent push silently dropped the entry the user just navigated away from (the "skipped entries" symptom). The push now uses the captured previous entry, and the prune also recognizes `properties` nav-state entries. **Naming cleanup folded in:** umbrella term "app pages" (or "app-page tabs" where the tab-strip aspect matters) replaces the inconsistent "page-kind tabs" / "meta pages" wording across `editor.md`, `status.md`, and code comments. `editor.md` §`tab-kinds` now introduces "app pages" in one sentence and uses it consistently below. Slug `tab-kinds` itself keeps its name. |

Fixed 2026-05-13 (home recent-activity widget consumes unified feed):

| Slug | Fix |
| ---- | --- |
| `bug-home-recent-activity-missing-pending-agent-review` | The home recent-activity widget (`app/src/panels/home.rs`) now reads the merged activity feed (count + list) instead of the changes-only pair, so pending staging proposals appear as preview rows alongside committed change rows — same `core::activity` feed the editor status-bar version dropdown (`status-bar-version-dropdown-uses-unified-feed`) consumes. Pending rows render as compact staging rows with inline `[Accept]` / `[Reject]` buttons (per `staging-accept-reject-from-activity-detail`); clicking the row routes to the open-staging-proposal seam. The widget repaints live on staging-changed events so proposals appear/disappear/are accepted/rejected without a manual refresh. |
| `bug-agent-created-note-preview-missing-accept-reject` | Opening / closing the staging preview (`app/src/panels/buffer/mod.rs`) now re-runs the full active-tab render instead of only repainting the status bar + mode-controls slot. When the user opened the staging preview from the activity-detail page (a `home-detail` kind tab), the toolbar/mode-controls were hidden; the buffer-kind transition into the staging-preview wasn't re-evaluating toolbar visibility, so the staging mode-controls ran into a hidden host and Accept/Reject never appeared. The buffer state already stamps the rel-path + proposal id and the staging mode-controls were already in place — only the toolbar-visibility refresh was missing. Same fix applied symmetrically on close so the toolbar hides again when returning to the activity-detail page. Accept-navigate path (`bug-staging-accept-no-navigation-staging-preview`) unchanged. |

Fixed 2026-05-11 (config watcher + events bus cleanup):

| Slug | Fix |
| ---- | --- |
| `bug-config-toml-on-disk-changes-not-observed` | Added `notify` watcher on both config TOML files (user-scope `~/.config/hiker/config.toml` and vault-scope `vault/.hiker/config.toml`) in the host. Debounced at 500ms; on change calls `Config::load()`, swaps the in-memory `RwLock<Config>` (plus live `tasks.set_cfg` / `mcp_tools` mirrors), and emits config-reloaded events with the merged config payload. Write-loop suppressed: `set_setting_inner` records `Instant::now()` in a shared `AppState::config_last_write` mutex; the config watcher skips reloads within 2s of a write (same `SUPPRESS_TTL` shape as the vault watcher). Watcher task is canceled on vault swap via a `CancellationToken` on `VaultSession`. The app re-applies the merged config to its UI surfaces on the config-reloaded event. |
| `bug-events-bus-settings-changed-no-consumer` | Deleted the unused `"settings-changed"` UI event type and its emitter. The real-time settings sync path is now the config-reloaded event (from the config-file watcher above), on which the app re-applies the merged config to its UI surfaces directly — no event-bus indirection needed. |

Fixed 2026-05-13 (staging accept new-file tree marker):

| Slug | Fix |
| ---- | --- |
| `bug-accept-new-note-from-activity-page-leaves-stale-dirty-marker` | Race in the staging-changed handling for the file tree (`app/src/sidebar/files.rs`): the staging-proposals refresh and the tree refresh ran concurrently, so the tree re-render frequently read the stale proposal cache (still containing the just-accepted proposal) and re-painted the new file's row as staging-new / staging-dirty. Fix sequences the staging-proposals refresh before the tree refresh so the render always sees the post-accept proposal set. Matches the sequencing already used by the tree-context-menu Accept verb. |

Fixed 2026-05-12 (staging accept navigation):

| Slug | Fix |
| ---- | --- |
| `bug-staging-accept-no-navigation-chat` | Chat tool-call card accept handler (`app/src/chat/render.rs`) now captures the accept outcome's target path and opens it via the existing note-link seam as a preview tab. Reuses that seam (`bug-chat-couples-to-main-buffer-globals`) instead of adding a new dependency. |
| `bug-staging-accept-no-navigation-activity-detail` | The activity-detail accept path (`app/src/panels/home.rs`) now captures the accept outcome's target path, opens it as a preview tab, and if the file was already open as a sticky tab it activates the tab and reloads the buffer from disk (clean → silent reload; dirty → toast warning). The home row builder delegates unchanged. |
| `bug-staging-accept-no-navigation-staging-preview` | Staging preview mode accept button (`app/src/panels/buffer/mod.rs`) now captures the accept outcome's target path and opens it as a preview tab instead of just closing the preview, so the user lands on the accepted file rather than returning to the activity detail page. Reject path unchanged (still closes the preview). |

Fixed 2026-05-11 (UI bugs):

| Slug | Fix |
| ---- | --- |
| `bug-queue-detail-toggles-stale-on-mount` | The queue detail panel (`app/src/panels/queue.rs`) now re-seeds the direct-worker + expose-to-chat-agent checkboxes and worker-preference dropdown from the loaded settings on vault open, and also re-seeds when the toggles section becomes visible (tasks filter pill flips on). Fixes the stale-display gap where the panel initialized before any vault was open.
| `bug-dropdown-button-click-toggles-not-closes` | Added open-trigger tracking to the dropdown-menu helper: a click on the same trigger while its menu is open now closes instead of re-opening (toggle behavior). Call sites in the toolbar (`app/src/toolbar.rs`) and sidebar (`app/src/sidebar/files.rs`, `app/src/sidebar/trails.rs`) pass their trigger. Outside-click dismissal unchanged.
| `bug-remove-waypoint-trash-not-refreshed` | Added a waypoint-removed notification on the Trails sidebar (`app/src/sidebar/trails.rs`, mirroring the existing waypoint-appended pattern), fired after a successful remove + refresh. The host wires it to refresh the trash panel and the home activity surface (`app/src/panels/home.rs`) so they pick up the cascaded deletes (the watcher suppresses events for deleted paths).
| `bug-chat-input-double-height-on-first-open` | The chat input auto-sizing (`app/src/chat/render.rs`) now resets to its minimum height when empty and defers the initial sizing pass until after the first layout, so it measures the laid-out input rather than a pre-layout state. An empty input falls back to its minimum row height.
| `bug-chat-input-not-resizable-within-panel` | Added a drag handle above the chat input (`app/src/chat/render.rs`, mirrors `chat-panel-vertical-resize`). Min height 28px, max bounded by chat region minus transcript floor. Auto-grow defers to the user-set height when one is set. Persists per-vault under `vault.chat_input_height` via settings write-back (new `NonNegativeInt` value type in `core/src/config.rs`).

Fixed 2026-05-11 (tab-kinds migration):

| Slug | Fix |
| ---- | --- |
| `bug-editor-pane-not-extracted-from-main-ts` | S3 landed: the editor pane (`app/src/editor_pane.rs`, with the buffer surface in `app/src/panels/buffer/mod.rs`) owns editor view, buffer rendering, save/drift, buffer-scoped toolbar, status bar, mode controls, View menu, dirty-buffer diff, mutations menu — kept separate from the app's tabs/file-open/autosave wiring. |
| `bug-page-views-not-tab-kinds` | S2 landed: page-shaped center-pane views (home, home-detail, queue, settings) now render as tabs in the top tab strip (`app/src/workbench_host.rs`). Editor toolbar + status bar hide on non-buffer tabs. Home/Queue/Settings buttons route through the app-page tab-open path. Migrated from a sub-mode flag to app-page tabs. |

Fixed 2026-05-10:

| Slug | Fix |
| ---- | --- |
| `bug-filetree-trail-expand-shows-no-waypoints` | clicking the chevron on a trail-doc row in the file tree (`app/src/sidebar/files.rs`) no longer surfaced the trail's waypoint-notes nested under the row. Regressed when slice S1b reshaped the tree row rendering: the trail-waypoint children were inserted before the row itself was attached, so the insertion was lost. Fix: insert the trail-waypoint expansion after the row is attached, mirroring the (now correct) sibling-insertion pattern. The recursive waypoint-children walk and the trail-expansion toggle are unchanged. Confirmed the data path: the trail-get call returns the recursive resolved waypoints, cached and walked recursively to build the nested rows |
| `bug-tab-switch-promotes-outgoing-preview` | Switching tabs from a preview tab to any other tab demoted the preview tab to sticky, contradicting `editor-preview-tab-promotion`'s explicit promotion paths (edit, double-click tab, drag, "Keep open"). Root cause: in tab activation (`app/src/workbench_host.rs`), the incoming buffer's content was swapped in before the active-buffer pointer was updated, so a status refresh fired against the new content but the still-outgoing buffer, computed a false "dirty" against the outgoing buffer's loaded text, and triggered preview promotion. Fix: clear the active-buffer pointer before swapping content (mirroring the file-open / preview-replace paths) so the dirty check short-circuits to false and no false-positive promotion fires; the new buffer pointer lands after the swap. Per spec line 645: "Activating a preview tab from a different sticky tab is a normal tab switch, not a re-open. The italic stays — the preview is still a preview until promoted" — same rule applies in the reverse direction (switching *away* from a preview to any other tab also doesn't promote) |
| `bug-add-to-trail-verbs-dont-refresh-panel` | Both "Add to trail" verbs (editor toolbar pill and tree right-click) ended by appending a waypoint and relied on the file-changed event to refresh the Trails panel + membership cache. But `core::trails::append_waypoint` calls `Watcher::suppress` on the trail-doc + waypoint-note paths (correctly, to prevent indexer feedback loops), so notify drops events for those paths and the panel never repaints until the user toggles active trails. Fix: both verbs now refresh the Trails sidebar (`app/src/sidebar/trails.rs`) and the active-trail membership cache directly from their success path, wired from the file tree (`app/src/sidebar/files.rs`) and the toolbar pill. Same callback pattern the `+`-button create-trail flow already uses; no watcher-predicate broadening, no change to suppression semantics |
| `bug-id-stamping-mints-fresh-ulid-instead-of-adopting-path-ids` | `core::ops::ensure_note_id_stamped` now takes `store: &mut Store` and adopts the indexer's `path_ids[rel]` ULID when present, only minting a fresh `new_id()` when the path has never been ingested. `core::trails::append_waypoint` threads the store through; the host seam (`trail_append_waypoint_inner`) opens a fresh `Store::open(vault.root())` reader for the call (the shared `read_store: Arc<Mutex<Store>>` can't cross `.await` because `MutexGuard` isn't `Send`). Belt-and-braces in the indexer: `core::indexer::process_upsert` (both empty-chunks and chunked branches) now consults a new `frontmatter_hiker_id(contents)` helper before falling through to `new_id()` when `path_ids` has no row — so the case where a waypoint is created against a not-yet-ingested source still keeps the two systems in sync on the next ingest. Pre-existing inconsistency (frontmatter id ≠ path_ids id) is logged and left alone — clobbering risks user data; warrants a separate slug if it ever appears in real data. New regression test `trails::tests::append_waypoint_adopts_indexer_path_id_for_source`: ingests a source, captures `path_ids` id Y, calls `append_waypoint`, asserts the waypoint's `references.id == Y` and that `resolve_reference` returns `Resolved` (not `PathConflict`). Already-broken waypoints from before the fix keep rendering as orphans — out of scope per the bug report; user manually deletes and re-adds |
| `bug-trails-dropdown-stale-on-delete` | The Trails panel's file-changed filter (`app/src/sidebar/trails.rs`) only refreshed on `.hiker/trails/` paths or the active trail-doc path, so deleting a non-active trail-doc from the file tree (which moves the trail-doc + cascaded waypoint dir to trash via `core::trails::delete_trail`) left the `trails_list` cache stale; the Trails dropdown then listed a deleted trail and activating it errored. Fix: broadened the predicate to also refresh on any `.md` change outside `.hiker/` (created/deleted/renamed) — same conservative posture as the sibling trail-row-icon refresh, which already refreshes the tree's trail-doc set on the same predicate. `trails_list` is a small per-vault query so the extra calls are acceptable. Also wired an active-trail-deleted fallback in the Trails refresh: when the previously-active rel is no longer in the freshly-fetched `trails_list`, clear the active trail and re-paint so the dropdown falls back to "None" cleanly instead of showing the deleted trail's basename |
| `bug-trails-flat-not-tree` | Slice S1a backend foundation rewrite — landed alongside `bug-trails-filename-encodes-seq` since they overlap. (1) `TrailDocFrontmatter.waypoints` is now `Vec<WaypointEntry>` with recursive `waypoints: Vec<WaypointEntry>` children; `parse_trail_doc` / `write_trail_doc_frontmatter` recurse via `parse_waypoint_entry` / `waypoint_entry_to_json`; pre-existing flat-format yaml still parses cleanly as a tree of all-root entries. New helpers `walk_waypoints_depth_first` / `find_waypoint(_mut)` / `collect_descendant_ids` / `remove_waypoint_from_tree`. (2) Schema bumped to v6 — `trail_waypoints` now carries `parent_waypoint_id TEXT NULL` + `tree_path TEXT NOT NULL DEFAULT ''`, plus a `trail_waypoints_parent_waypoint` index; `seq` is dropped; `waypoints_of` orders by `tree_path`; `WaypointRow` + `TrailContainingHit` updated. Fail-loud per `store-version-fail-loud` — pre-real-use policy means delete + reindex, no migration code. (3) `append_waypoint` gained `parent_waypoint_id: Option<&str>` (None = root append; named parent uses `find_waypoint_mut`). (4) `remove_waypoint` cascades: collects every descendant path from the subtree before mutating, deletes each via `core::ops::delete` (one trash entry + `core::changes` row per descendant), drops the subtree from the trail-doc, returns `RemoveWaypointOutcome { removed_count, removed_paths }`. New helper `descendant_count` lets the UI pre-compute the cascade size for the confirm dialog without executing. (5) Indexer `update_trail_waypoints_if_relevant` rewritten: trail-doc ingest captures existing rows (to preserve per-row `source_path` / `source_id` written by the per-waypoint ingest path), clears all rows for the trail, and re-derives via `walk_waypoints_depth_first` so `parent_waypoint_id` + `tree_path` are filled canonically; per-waypoint ingest writes those columns as `(NULL, "")` and relies on the trail-doc walk to fill them. (6) `ResolvedWaypoint` + `TrailDetail.waypoints` are now recursive (`children: Vec<ResolvedWaypoint>`, `tree_path: String`); `get_trail` walks the resolved tree via `resolve_waypoint_tree`. (7) host seam (`app/src/actions.rs`): `trail_append_waypoint` accepts `parent_waypoint_id`; `trail_remove_waypoint` returns `WaypointRemovedDto { removed_count }`; new `trail_descendant_count` action. (8) the recursive `ResolvedWaypoint` shape, the `parent_waypoint_id` append arg, the `removed_count` remove result, and the descendant-count query are exposed natively. (9) UI rendering kept working via two temporary flatten-the-tree stopgaps (marked `// TODO trails-mode-side-trail-render`) in the Trails sidebar (`app/src/sidebar/trails.rs`) and the file tree (`app/src/sidebar/files.rs`); slice S1b replaces them with proper indented rendering. Test coverage: `parse_trail_doc_round_trips_nested_tree`, `parse_trail_doc_round_trips_empty_tree`, `parse_trail_doc_old_flat_format_parses_as_root_tree`, `walk_waypoints_yields_depth_first_with_tree_paths`, plus the existing `remove_waypoint`/`move_*` tests adapted to the new shape. Status updates: `trail-doc-shape`, `trail-storage-layout`, `trail-waypoints-derived-table`, `trail-side-trail-shape` all flipped to `done`; `trails-mode-remove-waypoint-verb`, `trails-mode-waypoint-card`, `trail-row-dropdown-chevron` stay `partial` pending the slice S1b UI work |
| `bug-trails-no-build-as-you-read-editor-verb` | Landed slug `trail-add-to-active-from-editor-verb` (slice S2). New "Add to trail" pill placed in the right-side toolbar cluster, just left of Save (`app/src/toolbar.rs`). Visibility: active trail set + active buffer is a plain file + path is `.md`/`.txt` + path is not a trail-doc + path doesn't start with `.hiker/trails/`. Label `"Add to trail: <basename>"` doubles as the always-visible active-trail indicator outside Trails mode. Click appends a waypoint directly (NOT via the capture helper, which swallows errors per its capture-flow contract; the editor verb is user-explicit and needs error visibility, mirroring the tree-verb pattern). Idempotency: a shared membership cache in the Trails sidebar (`app/src/sidebar/trails.rs`), populated via the trail-get call walking the recursive resolved waypoints (one call per refresh covers every path in the active trail at once — cheaper than per-note `trails_containing_note` lookups), refreshed on active-trail changes and file-changed events under `.hiker/trails/` or against the active trail-doc. The pill disables itself + tooltip "Already in this trail" when the membership cache reports the open path. The same cache drives idempotency for the existing tree-verb (`trail-add-to-active-from-tree-verb`): the file tree (`app/src/sidebar/files.rs`) reads membership synchronously when building the menu, disabling the verb with the same tooltip. Backend half: new `trails_containing_note(source_rel) -> Vec<TrailsContainingNoteHit { trail_id, trail_doc_rel }>` over a new `core::trails::trails_containing_note_with_paths` (pairs derived-table `trail_id`s from `Store::trails_containing_note` with their trail-doc rel-paths via the same `list_trails` walk the dropdown uses). The command itself isn't on the hot path of the pill (the trail-get cache is cheaper for the bulk-membership case) but lands now for the planned `mcp-tool-trails-containing-note` consumer and for cases where a UI surface needs cross-trail membership for one note rather than within-active-trail membership for many notes |
| `bug-trails-filename-encodes-seq` | Landed in the same slice S1a pass as `bug-trails-flat-not-tree`. `waypoint_filename` rewritten to `(source_basename, waypoint_id) -> "<basename>--<short-id>.md"` where `short_id_of` returns the upper-cased last 6 chars of the ULID. `append_waypoint` mints the waypoint id first and feeds it into the filename helper (no `seq` parameter remains). Collision handling: if the primary filename already exists (vanishingly rare, since the short-id is per-waypoint-id), the op iterates `<basename>_2--<short>.md`, `<basename>_3--<short>.md`, ... up to 1000 candidates. Filenames are now stable identifiers — never renamed on reorder/re-parent — and frontmatter owns order + tree shape. Test `waypoint_filename_uses_short_id_suffix` covers the happy path, the upper-casing, and the short-id-fallback for ulids shorter than 6 chars. No on-disk migration: the indexer doesn't care about the filename shape, frontmatter is truth, and existing dev-vault waypoints keep their old basenames |

Fixed 2026-05-09:

| Slug | Fix |
| ---- | --- |
| `bug-trails-and-tree-org-icons-in-editor-toolbar` | deleted both the trails-menu and tree-org-menu buttons from the editor toolbar (`app/src/toolbar.rs`); nothing else referenced either. The trails-glyph + tree-org-glyph icons were salvaged into the new sidebar mode switcher as the Cluster trees / Trails buttons per the row's "reused inside sidebar-mode-switcher when that lands" guidance |
| `bug-sidebar-top-row-not-unified` | collapsed the prior two-row sidebar header (mode switcher + separate tree toolbar) into a single uniform icon-only row that's persistent across every mode (`app/src/sidebar/files.rs`): `[Files][Clusters][Trails] | [+] [⋯]`. `+ New note` rendered as a compact icon button; the `⋯` actions menu sits with the `+` button after a spacer + divider between them and the mode buttons. The `+` button stays behaviorally Files-mode-only for now (Clusters/Trails currently have no body to attach to); when those modes' bodies land, each owns its own routing per spec — this row's scope is the layout/persistence/rename, not mode-aware behavior. `⋯` menu contents stay filetree-only until `cluster-editor-mode-menu` and the trails menu land — tracked under `sidebar-toolbar-actions-menu`'s remaining partial |
| `bug-render-mixed-with-business-logic` | separated row rendering from click/business logic across all five render sites: search + related results (`app/src/panels/search.rs`), recent-activity list (`app/src/panels/home.rs`), the file tree (`app/src/sidebar/files.rs`, dispatching folder toggle vs. open-note per row kind), and the queue detail rows (`app/src/panels/queue.rs`, gating toggle-expand on the row head so clicks inside an expanded details panel don't collapse the row). Keyboard nav keeps working since it routes through the same per-row dispatch |
| `bug-panels-duplicate-mount-pattern` | unified the mount + visibility pattern across the five panels — the file tree and discovery (`app/src/sidebar/files.rs`, `app/src/panels/discovery_pane.rs`) are sidebar-managed (always visible), while home and queue (`app/src/panels/home.rs`, `app/src/panels/queue.rs`) route their own visibility side effects through one shared hook, with the cross-panel uniforms (toast / error-formatting / settings / open-note / focus-editor) coming from one shared bundle |
| `bug-inline-svg-strings-scattered` | consolidated the scattered inline icon definitions (trash / robot / brain / diff / restore / close / user / dot) into one shared icon module (`app/src/icons.rs`) behind a common envelope, with per-icon size/stroke defaults capturing each call site's prior look. Migrated every call site — chat session row, queue detail robot/brain pills, the recent-activity author pills, and the mode-controls diff / restore / close buttons — to the shared icons. Icon color flows from the theme tokens that landed in the paired `bug-hardcoded-colors-bypass-css-variables` slug, so swapping accent / surface tokens repaints the icons for free |
| `bug-hardcoded-colors-bypass-css-variables` | tokenized every color literal into a single consolidated theme palette (`app/src/theme.rs`) — accent, text, surface/border, background, shadow, danger, warn, success, diff, and live-preview families — so every surface references named theme tokens instead of bare color literals. The scattered inline color literals across the settings pane, the live-preview decorations (frontmatter hiding / chunk boundaries / styling), and the diff view now resolve through the same palette, so swapping accent / surface tokens repaints them all |
| `bug-drift-resolution-policy-in-ui` | added `core::ops::resolve_drift(vault, changes, rel, choice: DriftChoice, new_text, extra_metadata) -> DriftResolution` with `DriftChoice = KeepMine | TakeTheirs | Cancel` and `DriftResolution = Written { new_hash, token } | TookTheirs { contents, token } | Cancelled`. The UI's keep/take/cancel branching now shrinks to "show modal, dispatch chosen variant" over `resolve_drift`; modal copy + default-focus stay in the file-open path (`app/src/actions.rs`, presentation). Both the save-time drift modal and the watcher-conflict-dirty modal route through the same op so MCP/CLI/agents get a typed surface for the conflict path |
| `bug-buffer-hash-tracking-in-ui` | added `core::ops::open_for_edit(vault, rel) -> { contents, token: BufferToken }` and `commit_buffer(vault, changes, token, new_text, extra_metadata) -> CommitOutcome` (`Written { new_hash, token } | DriftDetected { current_disk_text, current_hash }`). `BufferToken` is an opaque struct (private path/content_hash/opened_at_ms fields, serde-round-trippable). The UI buffer's loaded-hash field (`app/src/buffer.rs`) was replaced by an optional `BufferToken`; every save/open/mutation/silent-reload path migrated to `open_for_edit` + `commit_buffer`; the checked read/write entry points were dropped (read-only previews now use a thin plain read). On `DriftDetected`, the UI dispatches to `resolve_drift` per the paired slug above. `open_for_edit` / `commit_buffer` / `resolve_drift` are exposed as thin native wrappers in `app/src/actions.rs` |
| `bug-css-class-strings-scattered` | centralized the shared row-state markers used across multiple surfaces — the four index-state markers (unsupported / skipped / queued / indexed) used by the file tree (`app/src/sidebar/files.rs`) and the post-progress-event apply path, the drop-target marker (file-tree dnd), the queue-pill marker (`app/src/panels/queue.rs` filter pills), and the related-item marker (`app/src/panels/related.rs` rows + keyboard-nav lookups). Single-surface marker names stay inline per scope |
| `bug-sidebar-toggle-side-effects-fan-out` | declared a typed app-event seam for `sidebar-toggled` / `vault-opened` / `vault-closed` / `settings-changed`, so the sidebar / related toggle handlers (`app/src/layout.rs`) emit the toggle event rather than fanning out side effects inline — both the manual toggles and the programmatic expand-on-discovery path go through it. Vault open emits `vault-opened`; the settings layer is the canonical emitter for `settings-changed`, so any future panel can subscribe without holding a direct manager reference. No production subscriber today — every cross-module wake-up still rides direct calls (the file-tree watcher refresh, the discovery section-expand from settings-apply, etc.) — but the seam is declared so the next slug that wants cross-panel reactivity drops in on it |
| `bug-persist-setting-duplicated-per-module` | added a shared settings-write helper (over `app/src/state.rs::set_setting`) with set-user-setting / set-vault-setting / on-settings-changed; the shared write + "Saved" / "Error" feedback + error logging lives in one place, and each successful write fans out to subscribers before the feedback so future cross-panel reactivity has a seam. The file tree, discovery, and trash panels take it as a shared dependency — replacing the bespoke persist-setting closure each previously carried. The queue panel keeps its own toggles-tray feedback wired to its status line without re-implementing the write shape. A thin persist-setting shim still serves the host-side call sites not yet on the typed surface (View menu flips, sidebar/related toggles, chat-panel resize). The settings pane (`app/src/panels/settings/mod.rs`) is intentionally left alone: it re-applies the merged config on persist rather than just logging, a different shape than this row addresses. The on-settings-changed seam has no production subscriber today; declared so the next slug that wants cross-panel reactivity (the app-event seam from `bug-sidebar-toggle-side-effects-fan-out`, which this row pairs with) has a typed seam |
| `bug-chat-couples-to-main-buffer-globals` | replaced the chat panel's separate active-note / active-selection accessors with a single active-buffer dependency. The chat panel (`app/src/chat/render.rs`) reads the active-buffer snapshot once per send and takes both the buffer text and selection off it; the at-mention autocomplete reads the selection off the same snapshot. The dependency is resolved at construction rather than via ad-hoc closures over the editor globals. No subscription wiring — chat polls on send today, so the on-changed seam stays unused but available for future reactive consumers |
| `bug-main-ts-god-module` | split out four remaining slices on top of the prerequisite `bug-ui-state-in-mutable-closures` state store: the file-open path (file open + preview-slot replacement + preview promotion + the disk-drift save modal + the watcher-conflict-dirty modal, `app/src/actions.rs`), vault lifecycle (folder picker, default-vault bootstrap, open-vault error handling, `app/src/bootstrap.rs`), keybindings (the window-level handlers — settings global, search global, tab + navigation, `app/src/keybinds.rs`), the index-status snapshot + outstanding-count tally, and settings-apply as a mapping over per-surface mutators. Tab-strip plumbing (tab activation / close / cycle / jump-to / mutation-apply) stays separate from the file-open/preview-promotion/drift surface this row called out |
| `bug-ui-state-in-mutable-closures` | added an observable state store (`app/src/state.rs`) holding the active buffer / active path / preview-tab path, the open-buffer set + activation counter, the View-menu toggle flags, and the in-flight-mutations set, plus the `Buffer` / `BufferMode` / open-buffer types (`app/src/buffer.rs`). The former mutable globals are now derived caches synced through the store; every write rides through the store's setters so cross-module subscribers fire on every transition. Defined an active-buffer api over the store, ready to back the next slug (`bug-chat-couples-to-main-buffer-globals`). Discovery / chat / tree per-module state intentionally left in their own modules per row guidance — the central globals were where the cross-module observability problem was sharpest |
| `bug-ui-errors-not-routed-to-tracing` | lifts `obs-frontend-bridge` out of deferred. The UI now emits structured `tracing` events directly (the app is native Rust, so no IPC bridge is needed): a small UI logger fronts `error`/`warn`/`info`/`debug`/`trace` with a per-surface UI target (`ui::tree`, `ui::discovery`, etc.) and structured fields. Migrated the silent-catch sites across the file tree, discovery, home, queue, and trash surfaces to log through it; the action-dispatch path logs its own failures with the command + error. User-visible feedback paths (modals, status flashes, chat error rows, mutation toasts) intentionally left alone — separate concern |
| `bug-at-mention-resolution-duplicated` | extracted a shared at-note resolver over the chat-resolve-at-note call. The chat panel's context-block assembly (`app/src/chat/render.rs`) now calls the helper instead of resolving inline; future at-mention surfaces (queue-detail "show source note", etc.) get one seam. Only the resolve call + rel-path/content shaping moved into the helper — chat-side concerns (selection branch, dedupe, error wrapping) stayed at the callsite |
| `bug-ipc-responses-untyped` | the high-blast-radius command responses (search, recent-changes, get-settings + scoped variants, reload-config, set-setting) are validated against their expected shape, with failures surfacing on the existing error path. In the native action path (`app/src/actions.rs`) calls are direct typed Rust with no JSON boundary, so response-shape drift is caught at compile time rather than via runtime destructure-of-undefined. Resolves the silent-destructure failure mode when a response shape drifts |
| `bug-invoke-scattered-no-ipc-client` | replaced the scattered command-invocation calls with a single dispatch seam (`app/src/actions.rs`: `ActionRegistry` + `dispatch()`) that logs structured fields (command, error) on failure (paired with `bug-ui-errors-not-routed-to-tracing` for the tracing route). The registry exposes one entry per action the UI uses (settings / vault lifecycle / file I/O / trash / index / search / diff / changes / home / chat / mutations / task queue), and every UI surface routes through it rather than calling commands ad hoc. In the native action path the responses are direct typed Rust — no hand-mirrored DTOs; the runtime-validation work belongs to the sibling slug `bug-ipc-responses-untyped` |

Fixed 2026-05-08 (cleanup pass):

| Slug | Fix |
| ---- | --- |
| `bug-frontmatter-detection-duplicated-in-ui` | extracted a shared frontmatter-detection helper (`editor/editor-md/src/styling.rs`) and routed both the live-preview styling and the frontmatter-hiding decoration through it (preserving the historical `...`-closer acceptance). Detection rule mirrors `core::frontmatter::split`; the helper notes why an in-editor dedup beats a core call here (decoration-compute runs synchronously on every doc change — a per-keystroke round-trip would be a regression) |
| `bug-is-indexable-extension-duplicated-in-ui` | dropped the TS-side `indexableExts` allowlist + `isIndexableExt` predicate + `setIndexableExts` API + the `indexable_extensions` command. Tree row renderer + status-bar `renderIndexStatus` now defer unconditionally to `index_state_for`, which already returns `Unsupported` for paths outside `core::indexer::indexable_extensions`. Single source of truth in core |
| `bug-count-notes-recursive-walk-from-js` | new `count_notes_in(rel) -> u32` command backed by `core::vault::walk_indexable_files` (which already shares the `is_indexable_path` filter). The UI's note count collapses to one core call instead of a recursive client-side walk. The `.md`-only hardcoding is gone — `.markdown` and `.txt` now also count toward the delete-confirm "N notes" copy |
| `bug-author-class-string-parsing-in-ui` | added `core::changes::AuthorClass` enum (`user` / `agent` / `sync` / `import` / `other`) and an `author_class` field on `ChangeRow`, populated in `map_row` and on the `appended_tx` broadcast send. UI's `authorClass(author)` helper deleted; pill-rendering, filter-pill state, and `handleAgentChange`'s "is this an agent write?" gate all consume the typed field. UI `ChangeRow` interface mirrors the new field |

Fixed 2026-05-08 (later batch):

| Slug | Fix |
| ---- | --- |
| `bug-vault-home-inlined-in-main-ts` | extracted the vault-home panel (`app/src/panels/home.rs`) covering visibility, refresh, detail view, change/recent-modified notifications, stats-refresh debounce, snapshot restore, and the home/new-note buttons. It owns the activity rows, restored-from tracking, active detail view, author filters, and the stats-refresh debounce. The host wires the open-note / open-snapshot / before-show callbacks so the panel never reaches into editor or snapshot-preview state directly |
| `bug-search-and-related-inlined-in-main-ts` | extracted the discovery panel (`app/src/panels/discovery_pane.rs`, with search in `app/src/panels/search.rs` and related in `app/src/panels/related.rs`) covering related-refresh, mode switching, section-expand, toggle sync, clear, and input focus. It owns search modes, search/related epoch counters, debounce timers, section-collapse state, keyboard nav, and the discovery input/section listeners. The host wires the open-note / scroll-to-chunk / expand-panel callbacks. Chat (in the same panel host) stays in `app/src/chat/render.rs` |
| `bug-discovery-sections-overlap-after-toggle-cycles` | fixed inside the discovery panel's section-collapse handling (`app/src/panels/discovery_pane.rs`): collapsing a section now clears any residual sizing left behind by a prior expand/collapse, so rapid toggle cycles can't stack residual sizing across sections |
| `bug-mode-controls-and-view-menu-inlined-in-main-ts` | extracted the mode controls (`app/src/toolbar.rs`, with the per-mode rendering in `app/src/panels/buffer/mod.rs`): each owning surface registers a renderer keyed by buffer-mode kind (`snapshot` / `trash`) and the host swaps based on the active buffer mode. The View ▾ menu also lives here, building its items and populating the dropdown. The shared widget primitives (context menu / toast / confirm) had already landed in the earlier batch |
| `bug-tree-inlined-in-main-ts` | extracted the file tree (`app/src/sidebar/files.rs`: `FilesView`, `render()`) covering refresh, reveal-path, sort order, selected-folder, watcher notification, and the index-state cache helpers. It owns the expanded-folders set, sort order, watcher debounce, index-state cache, and the in-flight fetch coalescing set; the host wires the editor-coupled callbacks (open-file, get-buffer, set-buffer-path, clear-open-buffer-if-within, refresh-trash, render-index-status). Row rendering, index markers, drag-and-drop + drop handling, reveal, inline rename, context menu, delete, note count, sort-by menu, new-note button, tree-root + empty-space drops all moved here |
| `bug-snapshot-preview-inlined-in-main-ts` | introduced a buffer-mode enum (`BufferMode` with file / trash / snapshot variants, `app/src/buffer.rs`) replacing the scattered preview / snapshot-preview / display-path / snapshot-change-id / snapshot-row / diff-active flags on `Buffer`. Extracted the snapshot-preview surface (`app/src/panels/buffer/mod.rs`) covering open / close / toggle-diff / restore. It owns the diff-toggle in-flight guard; the host wires the editor view, live-preview / frontmatter-hiding decorations, save / dirty / mode-controls / chunk-boundaries refresh callbacks, and the on-close (return to recent-activity detail) + on-restore (the home snapshot restore) hooks |
| `bug-trash-inlined-in-main-ts` | extracted the trash bin (`app/src/sidebar/files.rs`) covering refresh, open-preview, close-preview. It owns the trash items, header + row context menus, the trash-changed reaction, the relative-time helper, and the wiring for the read-only trash-preview buffer (now the `trash` variant of the buffer-mode enum, surfaced in `app/src/panels/buffer/mod.rs`). The host wires the editor view and the editor-coupled callbacks (get/set buffer, set-read-only, update-status, refresh-chunk-boundaries, is-dirty, save, refresh-tree) |

Fixed 2026-05-08:

| Slug | Fix |
| ---- | --- |
| `bug-chat-context-lost-across-user-turns` | resolved by the `chat-session-*` slugs landing in this batch. `ChatRegistry` is now session-keyed (`HashMap<SessionId, SessionState>` plus an `active` slot) in the host chat module; `chat_send` appends to the existing `SessionState.history` rather than minting a fresh per-turn entry, and nothing GCs the registry on terminal `TurnFinished`. The frontend tracks `activeSessionId` and threads it into `chat_send` / `chat_continue` / `chat_stop` / `chat_cancel`. The backend persists each terminal turn via `core::sessions::append_turn` and on vault open `resume_latest_at_open` rebuilds the in-memory cache from the most-recent on-disk markdown |
| `bug-tree-sort-in-ui` | `core::vault::list_dir(rel, sort_by)` now accepts the configured `TreeSortBy` and returns pre-sorted entries (folders-first grouping kept; chosen order applies within each group). The `list_dir` command falls back to `vault.tree.sort_by` from the loaded config when the UI doesn't pass `sort` explicitly. UI's `sortTreeEntries`/`sortComparator` deleted; `renderDir` calls `list_dir` with the active `treeSortOrder` and renders the result as-is |
| `bug-search-result-bucket-pick-in-ui` | added `hits: Vec<NoteHit>` to `SearchResponse`; `core::search::query` populates it via the new `pick_bucket(lexical_hits, semantic_hits, fused, modes)` helper (both modes → `fused`, one mode → that engine's full PER_BACKEND_TOP_K bucket, both off → empty). UI's `pickResultBucket` deleted; `runSearch` renders `resp.hits`. MCP truncates `resp.hits` alongside `resp.fused` |
| `bug-activity-refresh-polled-not-pushed` | `scheduleActivityRefresh` and `scheduleVaultHomeModifiedRefresh` debouncers gone. New `refreshOnChangesAppended` runs on every op-log append event and refreshes recent-activity widget + detail + recent-modified directly. `handleAgentChange` no longer re-schedules home refreshes (upstream listener handles it). The watcher path keeps a direct refresh for external edits since those don't yet ride `core::changes` (deferred per `changes-write-path` notes) |
| `bug-index-status-polled-not-pushed` | added `IndexerHandle::subscribe_status` over the indexer's existing `watch::Sender<IndexStatus>`; the host spawns a forwarder task that emits index-status updates whenever the watch value changes (seeding the initial value at vault open). UI listens for the event and drops `pollIndexStatus` + the 2s `indexStatusInterval`; the buffer-path 250ms watcher stays (separate concern) |
| `bug-byte-to-char-conversion-in-ui` | `ChunkBounds` carries `char_start` / `char_end` (UTF-16 code units) populated by the new `core::store::enrich_char_offsets` helper; the `chunks_for` command reads the source file once via the vault and enriches the rows. UI's `byteOffsetToCharOffset` and `byteOffsetToLine` deleted — `openSearchHit` uses `target.char_start` directly and `chunkBoundsToState` calls `doc.lineAt(char_start)` |
| `bug-latest-per-path-computed-over-visible-window` | `ChangeRow` carries `is_current: bool` computed by SQL (`id = (SELECT MAX(id) FROM changes c2 WHERE c2.path = changes.path)`) over the whole table, not the visible 200-row window. UI's `buildLatestPerPath` and `latestPerPath` map deleted; row builders read `r.is_current`; `doRestoreSnapshot` finds the prior current row by scanning `activityRows` for `is_current` |
| `bug-diff-pane-collapse-into-toggle` | collapsed the separate diff pane into an in-place toggle: the Snapshot Diff toggle now flips the editor's doc + decorations in place via a diff-active flag on `Buffer` (`app/src/panels/buffer/decorations.rs`). Removed the standalone diff editor instance and its separate word-wrap / line-number / whitespace mirrors — the editor's own settings now serve the diff for free. Diff toggle hidden for `op = "deleted"` snapshot rows |

Fixed 2026-05-07:

| Slug | Fix |
| ---- | --- |
| `bug-show-line-numbers-noop` | the hide-line-numbers toggle now actually gates the line-number gutter rendering in the editor widget (`editor/editor-egui/src/widget.rs`), so toggling it off hides the gutter as intended |
| `bug-default-vault-not-persisted` | `open_vault_at` now also writes `vault.default` (user scope) when it differs from the opened root, alongside the existing `vault.recent` update |
| `bug-x-button-does-not-close` | `onCloseRequested` now always `preventDefault`s and drives the close itself via `win.destroy()`; the clean-path "let the host default-close" branch was unreliable, and `destroy()` avoids re-entering the handler that `close()` would |
| `bug-save-button-wraps-when-dirty` | added `white-space: nowrap` and a `min-width: 64px` to `#save-btn` so the dirty-state bullet glyph can't push the label onto a second line |

Fixed 2026-05-06:

| Slug | Fix |
| ---- | --- |
| `bug-save-button-double-listener` | dropped the early lambda; single click handler now lives at the related-notes refresh site |
| `bug-css-escape-incomplete` | replaced local helper with `CSS.escape()` |
| `bug-not-utf8-pins-index-error` | `process_upsert` returns `Skipped("not utf-8")` instead of `Error` |
| `bug-count-notes-swallows-errors` | seed and refresh paths surface `count_notes` failure as `last_error`; recovers when a later count succeeds |
| `bug-vault-resolve-accepts-absolute` | reject `RootDir`/`Prefix` components in `resolve` with a clear "expected vault-relative" message |
| `bug-queue-count-undercounts` | split `update_queue_count` into in-flight (`rx.len() + 1`) and idle (`rx.len()`) variants; main loop calls in-flight after recv, idle after job completes |
| `bug-progress-counter-unpaired-decrement` | unified `pendingCount + inFlightCount` into a single `outstandingCount`: `scan_complete` adds, every terminal event subtracts, `Started` is a no-op |
| `bug-vault-close-leaks-intervals` | `vaultIsOpen` flag + `startBackgroundIntervals` (called on each `openVault`) clears prior intervals and short-circuits when no vault is open |
| `bug-indexer-shutdown-broken` | `tx` wrapped in `Option<Sender>`; `shutdown` takes + drops it so the task's `recv()` returns `None` |
| `bug-watcher-suppress-ttl-too-short` | bumped `SUPPRESS_TTL` from 500ms → 2s, plus re-suppress after fs ops in `move_note` and `create_note` so the window starts close to when notify surfaces events |
| `bug-watcher-rename-pair-order-assumed` | branch on `RenameMode::From|To|Both` rather than trusting `paths[0]/paths[1]` ordering |
| `bug-watcher-unpaired-rename-as-modified` | `RenameMode::From` → `Deleted`, `RenameMode::To` → `Created`; only the genuine ambiguity case (Any/Other with one path) falls back to `Modified` |
| `bug-embedder-failure-counter-stuck` | embedder-failure drain now emits one `Error` per Upsert/Delete/Rename and replies `Err` to any pending `Move`, so the UI's outstanding counter actually reaches zero |
| `bug-pick-vault-blocks-tokio-worker` | replaced `std::sync::mpsc::recv()` with `tokio::sync::oneshot` so the file dialog suspends without parking a worker |
| `bug-vault-refuse-symlinks` | `Vault::resolve` walks each existing ancestor under the canonical root and rejects any symlink component (file *or* directory). 3 unit tests added |
| `bug-status-bar-path-overflow` | implemented `status-bar-path-basename-tooltip` (basename in `#status-path`, full path in `title=`) plus `ui-no-sibling-pushout` rule (`min-width: 0` + `flex-shrink: 1` on the status-bar regions) |
| `bug-move-note-routes-via-fresh-writer` | added `IndexJob::Move { from, to, reply }` + `IndexerHandle::move_note`; the host's move command now suppresses, sends the job via the indexer's owned writer, awaits the oneshot reply, and re-suppresses post-rename. No more parallel writer connection |
| `bug-sqlite-vec-init-transmute-fragile` | rewrote `register_vec_extension` to use rusqlite's typed `RawAutoExtension` alias and `register_auto_extension`; the transmute is still required (sqlite-vec ships an `extern "C" fn()` stub) but the destination type is now documented at the call site, and registration failures log instead of silently dropping |
| `bug-explicit-reindex-noops-on-unchanged-content` | added a `force: bool` field to `IndexJob::Upsert` / `IndexJob::FullScan`, plumbed through `process_upsert` and `run_full_scan`. The `index` command sets `force: true` so the menu's Reindex actions actually re-embed even when content_hash matches; watcher/startup paths keep `force: false` |
| `bug-related-panel-stale-on-vault-switch` | `openVault` now clears the related-notes panel via `refreshRelated(null)` on vault swap, alongside the existing `indexStateCache` / `inflightStateFetches` resets |
| `bug-too-large-errors-instead-of-skipping` | already fixed before this entry was filed: `process_upsert` at `core/src/indexer.rs:674` returns `UpsertOutcome::Skipped("file too large")` and persists the row via `Store::upsert_skipped`. `IndexerError::TooLarge` no longer exists in the enum. Bug-tracking entry was stale |
| `bug-index-status-shows-model-loading-with-no-vault` | the index-status rendering (`app/src/panels/buffer/mod.rs`) now shows a blank label + tooltip when no vault is open, so the early-init render path no longer surfaces a stale "model loading" status before a vault is opened |
