# Bug tracking

Known issues in the codebase. Each row is a kebab-case slug, a one-line description, optional file:line, and (where useful) a brief note on the intended fix. Same conventions as `status.md` — resolve by fixing in code, then strike the row (or remove it). If a bug ends up reshaping a feature, update the feature row in `status.md` instead of carrying both.

**No `// status: bug-…` markers in code.** Unlike feature slugs (which are stable references future readers may want to grep for), bug slugs are short-lived: once the row here is struck, the slug stops being meaningful. Tagging fixes inline just clutters the source. The git history and this file are enough.


## Open

| Slug | File | Notes |
| ---- | ---- | ----- |
| `bug-compute-diff-ipc-payload-unbounded` | the host (`compute_diff`), `core/src/diff.rs` (`compute`) | full `DiffResult` round-trips over IPC as JSON. For a multi-MB note with heavy edits, the payload is multiple MB of inline strings — the diff itself plus every context line — even though the UI already has both `before` and `after` in memory. Naturally bounded once the deferred `diff-viewer-grouped-hunks` variant lands (smaller hunks → smaller payload), and the eventual `mcp-tool-diff` will need a budget anyway. Fix at that point: a `compute_diff_grouped(..., context: u32) -> DiffResult` entry point + per-call size cap; or, if it ever bites in v1, switch the IPC to ts-rs binary serde. **Deferred-paired**: this row stays open as a pointer; the actual fix lands with `diff-viewer-grouped-hunks` |
| `bug-preview-tab-promotion-policy-in-ui` | `ui/src/main.ts` (`previewTabPath`, promotion sites in edit/dblclick/drag/keep-open verb), `ui/src/tabStrip/index.ts` | the "first edit promotes preview → sticky" state machine + the at-most-one-preview invariant are policy encoded across several UI callsites. Movable into a `core::tabs` state machine (`opened_preview` / `edited` / `pinned` / `closed` events; UI only renders). Lower-priority than the drift / hash slugs — there's no second consumer (MCP/CLI don't have tabs), so the win is purely "policy in one place." Defer until a second consumer appears, but file the slug so the invariant is named |
| `bug-settings-apply-orchestration-in-ui` | `ui/src/app/settingsApply.ts` (`applySettingsToUi`) | the function is a pure mapping from `Settings` → list of UI mutations; the *registry of which setting affects which surface* could live in core as a `SettingsImpact` table, with UI dispatching per-surface effects from a typed list. Marginal value at current settings.md size — flagged for completeness, not for near-term action. Revisit if the settings surface grows materially or if a second consumer (e.g. CLI `hiker config set` with live-apply semantics) appears. **Still deferred as of 2026-05-09**: the `bug-main-ts-god-module` extraction moved this into its own module as a pure mapping over per-surface mutator deps, which captures most of the practical win; the remaining core-side `SettingsImpact` table only pays off with a second consumer, and none has appeared (CLI still doesn't live-apply, MCP doesn't observe UI settings) |
| `bug-chat-session-resume-drops-tool-calls` | `core/src/sessions.rs` (`parse_session`), the host chat module (resume path) | Spec update lands first under `chat-session-markdown-store` (`llm.md`): resuming a session must rehydrate the tool-call structure from the markdown's `hiker-tool-call` fenced blocks instead of dropping them. Current behavior matches the prior "lossy resume" rule; the agent now mis-infers it can write notes without tool calls because the resumed history shows no prior tool use. Fix: extend `parse_session` to round-trip tool calls + tool results back into `SessionState.history`; demote `chat-session-markdown-store` in `status.md` from `done` to `partial` until this lands |
| `bug-trails-no-draft-pipeline` | `core/src/trails.rs` (`create_trail`, draft path, `hiker.draft` flag); `core/src/clustering.rs` (proposal emission); the host (MCP `trail_create` wrapper); `ui/src/main.ts` (activity detail page Pending filter); `ui/src/trails/index.ts` ("Show drafts" toggle, banner row, proposed waypoint rows) | spec evolved 2026-05-10 to allow agent/clustering-proposed draft trails (`trail-draft-from-agent`, `trail-draft-from-clustering`, `trail-draft-review-surface`). Replaces the prior "user-authored only" rule. Fix touches several layers: (1) `core::trails::create_trail` accepts a `draft: bool` arg; when true, places trail-doc at `<vault>/.hiker/trails/drafts/<trail-id>.md` and stamps `hiker.draft: true` in frontmatter; waypoints still live at `.hiker/trails/<trail-id>/waypoints/`; (2) `trails_list` and the Trails sidebar dropdown filter out drafts unless caller passes `include_drafts=true` / the user toggles "Show drafts"; (3) MCP `trail_create(draft=true)` exposes the new arg; default is `agent-write-review-mode`-aware (review on → drafts default true); (4) clustering pipeline gains an opt-in (`[clustering] propose_trails = false` default) that emits a draft trail when the cluster contains an implicit reading-order chain; (5) trails panel renders a banner row with [Accept trail] [Reject] for draft trails, plus minimal collapsed rows with [Accept] [Reject] for proposed waypoint additions; the activity detail page's Pending filter also lists these with the same Accept/Reject; (6) Trails sidebar dropdown grows a "Show drafts" per-vault toggle (off by default). New planned slugs: `trail-draft-from-agent`, `trail-draft-from-clustering`, `trail-draft-review-surface`. Pre-req: `staging-review-activity-detail-filter` + `staging-accept-reject-from-trails` (both `planned`) — fix can land independently if the draft preview surface uses a minimal trail-panel banner in the interim |
## Resolved

Fixed 2026-05-13 (write-note review):

| Slug | Fix |
| ---- | --- |
| `bug-write-note-pending-banner-oversized` | `#editor-pane` was a 3-row grid (`auto 1fr auto`). The toolbar header consumed row 1 (auto); the new `#write-note-pending-banner` (inserted as a sibling between toolbar and `#editor`) fell into row 2 (`1fr`) and ballooned to roughly two-thirds of the pane height. Converted `#editor-pane` to a flex column in `ui/src/style.css`: the toolbar / banner / status-bar all stay at content height (`flex: 0 0 auto` is the default), while the active content surface (`#editor`, `#vault-home`, `#settings-pane`, `#properties-pane`) gets an explicit `flex: 1 1 0; min-height: 0;` rule so it absorbs the remaining vertical space the way the prior `1fr` track did. Banner kept its existing amber strip styling; added an explicit `flex: 0 0 auto` to make the intent unambiguous. |
| `bug-write-note-review-redundant-exit-x` | `ui/src/main.ts::modeControls.register("write-note-review", …)` rendered an Exit icon-button next to Accept / Reject, duplicating the agent-diff toolbar toggle's exit affordance (forbidden by `patch-review.md:52`). Dropped the third `iconButton({ title: "Exit review", … })` registration. Also removed the `showToast("Accepted")` call on accept — the buffer reload after `openFile(targetPath)` is the user-visible confirmation; the toast was un-specced chrome. Reject's toast stays (the user-visible signal there is just the mode exit, which is easy to miss). Updated `docs/patch-review.md:107` to drop "Exit" from the write-note-review verb list and `docs/status.md:739` to remove the "Drift as of 2026-05-13" note. |
| `bug-write-note-review-accept-stale-proposal-id` | Already addressed in `ui/src/chat.ts::renderActionButtons` (landed alongside `bug-chat-tool-card-stale-after-cross-surface-accept`). The Accept handler now calls `resolveLiveProposalIds()` at click time, which for single-id whole-file proposals (`write_note` / `set_frontmatter` / `apply_tag`) queries `Ipc.stagingList({ path: targetPath })`, filters by matching `action` + `target_path`, and picks the newest live proposal — falling back to the captured id when the lookup turns up nothing. Batch (`edit_note`) shapes use the captured ids directly since they're authoritative for the lifetime of the batch. The result is that a chat-card Accept on a `write_note` rewrite resolves against whatever proposal is currently pending, even when the originally-captured id was replayed/reissued through staging churn. |
| `bug-patch-review-gutter-not-restored-on-exit` | The CM6 patch-review gutter is permanently installed (via `appendConfig` in `ui/src/patchReview/index.ts::mountPatchReview`) but is only meaningful during a review session. Its CSS width was unconditionally 56px to host the per-hunk Accept/Reject buttons, so the gutter stayed widened (with empty gutter elements) after the mode exited. Fix: `ui/src/main.ts::enterPatchReviewMode` toggles a `hiker-patch-review-active` class on the CM6 editor root; `exitPatchReviewMode` removes it. `ui/src/style.css` defaults the gutter to `width: 0; min-width: 0; overflow: hidden;` and only widens it (`56px`) when the class is present on `.cm-editor`. CM6's per-cell gutter rendering keeps working because the cell elements still receive their content; only the containing track's reserved width is collapsed. |

Fixed 2026-05-13 (chat tool-card):

| Slug | Fix |
| ---- | --- |
| `bug-chat-tool-card-no-link-for-staged-writes` | Added an `onOpenStagingProposal` seam to `ChatPanelOptions` in `ui/src/chat.ts`; `ui/src/main.ts` wires it to `openProposalReview` (the same dispatcher the vault-home activity widget / tree use, which routes through `openFile` so `note-open-routes-to-pending-review` lands the user in patch-review for `edit_note` and write-note-review for whole-file proposals). The tool-card head click now runs through a `handleHeadClick` helper that, for note-touching tool calls (`get_note`, `write_note`, `edit_note`, `set_frontmatter`, `apply_tag`, `remove_tag` — `edit_note` was added per the bug row), resolves the touched note's rel-path (`rel_path` / `path` on the result, args fallback) and any staged ids (`staging_id` singular for whole-file proposals, `staging_ids` plural for `edit_note` batches), then routes through `onOpenStagingProposal({id, target_path})` when any captured id is still pending, falling back to `onOpenNoteLink` when the proposal has resolved. Non-touching tools keep the prior toggle-card behavior. The chevron click handler now `stopPropagation`s so the expand/collapse JSON behavior is unaffected. The broader `chat-tool-call-opens-touched-note` feature stays `planned` in `status.md` — this lands the staged-write prong plus the routing seam, but not the multi-touch picker. |
| `bug-chat-tool-card-stale-after-cross-surface-accept` | The chat panel now subscribes to `hiker:staging-changed` and maintains a local `pendingStagingIds` set refreshed via `Ipc.stagingList()`. Accept/Reject button rendering moved into a `renderActionButtons(c)` helper that's called both on `tool_result` and on every staging-changed event; buttons are shown only when at least one of the card's captured `staging_id` / `staging_ids` is still in the live pending set. A cross-surface accept/reject (activity-detail row, tree context-menu, staging-preview surface) therefore clears the chat card's buttons live, and an in-card accept/reject re-renders authoritatively after the staging-changed event echoes back. The existing `bug-write-note-review-accept-stale-proposal-id` fix was preserved as `resolveLiveProposalIds()` inside the rendered handler. |

Fixed 2026-05-13 (staging surface routing):

| Slug | Fix |
| ---- | --- |
| `bug-staging-accept-navigates-from-activity-detail` | The vault-home mount's `onAcceptStaging` host callback in `ui/src/main.ts` (the activity-detail / home-widget surface — both `buildProposalRow` on the detail page and `buildPendingPreviewRow` on the home overview route here) no longer calls `openFile(outcome.target_path, …)`. Accept now just runs `Ipc.stagingAccept` and, when the target happens to already be the active buffer in plain `"file"` mode, reloads its contents from disk in place (dirty → toast warning, clean → `openForEdit` + dispatch the fresh body) so the user sees the result of the accept without leaving the list. Tree-context (`mountTree`) accept and staging-preview accept paths are unchanged — they still navigate via `openFile`, matching the chat-card behavior. `status.md`'s `staging-accept-navigates-to-preview` row flipped from `partial` to `done` and reworded to call out the activity-detail carve-out. |
| `bug-activity-pending-row-skips-patch-review-mode` | Two-part fix in `ui/src/main.ts`. (1) `openProposalReview` now delegates to the local `openFile` wrapper instead of calling `openFileApi.openFile` directly + manually invoking `enterPatchReviewMode`, so every pending-row click path (vault-home `buildProposalRow` / `buildPendingPreviewRow`, tree context-menu Select-version, autosave staging-preview restore) shares one entry point. (2) The `openFile` wrapper itself now honors the `note-open-routes-to-pending-review` contract for `edit_note` proposals: if `pendingEditProposalsForPath(rel)` is non-empty, open the file as normal and then call `enterPatchReviewMode(rel)` once the buffer is active. Whole-file proposal routing into `openWriteNoteReview` is unchanged. The misleading comment claiming `edit_note` proposals "do NOT auto-route" was deleted — that drift was the proximate cause of the bug, and it now matches what `status.md:720` says. |

Fixed 2026-05-13 (nav back):

| Slug | Fix |
| ---- | --- |
| `bug-nav-back-from-recent-activity-skips-home` | Recent-activity drill-in never opened a `home-detail` app-page tab — both entry points in `ui/src/vaultHome/index.ts` (the widget header onclick at line ~381 and the per-row click handler in `buildActivityPreviewRow`) called the local `showDetail("recent-activity")` directly, which just flips `#vault-home-overview` / `#vault-home-detail` `hidden` attributes inside the `home` tab. The active buffer stayed `kind: "home"`, `inferNavState` kept returning `{kind: "home"}`, and `activateTab`'s `checkpointNav` never fired. So pressing Back from the visible "Recent activity" surface ran Back from the `home` nav-stack entry, which correctly popped to the previously-open note — skipping past Home because the Home → Recent-activity transition was never recorded. Fix: route both clicks through a new `requestRecentActivityDetail()` helper that calls `deps.onOpenPage("home-detail", { view: "recent-activity" })`, which lands in main.ts's existing `onOpenPage` wiring (line 1687) and ends up in `openAppPageTab("home-detail", { view: "recent-activity" })`. That path evicts the home preview, creates a `__hiker:home-detail:recent-activity` tab, calls `activateTabInner` → `renderActiveTab` (which fires `vaultHome.api.showDetail(view)` so the DOM flip still happens) → `checkpointNav`. Nav stack now records `[…, home]` with `current = home-detail`, and Back pops to Home as expected. `showDetail` itself is unchanged — it's still the DOM-flip primitive `renderActiveTab` and the back-restore path both call into; the bug was purely the click handlers bypassing the tab-open seam. Helper falls back to direct `showDetail` when `onOpenPage` is unwired (defensive; main.ts always wires it). Pre-existing minor drift not touched: the autosave tab-state restore path at `main.ts:1435` passes `{}` to `openAppPageTab("home-detail", …)` instead of `{ view: "recent-activity" }`, so a restored home-detail tab keys as `__hiker:home-detail` rather than `__hiker:home-detail:recent-activity`. `inferNavState`'s `home-detail` branch already falls back to `recent-activity` for malformed keys, so this is cosmetic — flagged for a future tab-state restore touch-up. |

Fixed 2026-05-13 (menu trigger toggle):

| Slug | Fix |
| ---- | --- |
| `bug-menu-trigger-reopens-instead-of-toggling` | The toggle guard added by `bug-dropdown-button-click-toggles-not-closes` in `ui/src/widgets/contextMenu.ts` lived in `openContextMenu`'s entry: "same trigger + open menu → close and return". It never fired for the Mutations / View Options buttons because the outside-click handler (`mousedown` capture) ran *before* the button's `click` handler — `closeContextMenu()` nulled `openMenuEl` and `openMenuTrigger`, then the click's `openContextMenu` call saw a closed state and re-opened. Tree-actions / sort-by callsites don't expose the issue as visibly because their re-open just re-paints the same popover under the cursor. Fix: in the `onDocDown` outside-click handler, after the `menu.contains(target)` short-circuit, also short-circuit if the mousedown target is (or is contained by) `openMenuTrigger`. That defers dismissal to the trigger's own click handler, which then hits the same-trigger toggle branch and closes cleanly. Callsites without a `triggerEl` (legacy tree-actions, tabStrip context-menu, etc.) are unaffected — `openMenuTrigger` stays `null` and the original close path runs. |

Fixed 2026-05-13 (snapshot diff toggle):

| Slug | Fix |
| ---- | --- |
| `bug-snapshot-preview-diff-toggle-broken` | `toggleDiff` in `ui/src/snapshotPreview/index.ts` dispatched `renderDiff` (and `clearDiff` on the off→on→off return trip) against a view that `open()` had pinned read-only via `EditorState.readOnly.of(true)`. CM6 silently drops `changes:` transactions while RO, so both the doc-replacement (unified diff text on entry; snapshot blob on exit) and the decoration application no-op'd — click did nothing, no error. Fix: mirror `dirtyBufferDiff`'s shape — lift RO (`setReadOnly(false)`) immediately before the diff-direction's doc-replacing dispatch, wrap the dispatch + `renderDiff` / `clearDiff` in a `try { … } finally { setReadOnly(true); }` so RO is always restored (covers the candidate-3 in-flight-guard-stuck case too, since any throw inside the lifted region returns RO to the same state `open` set, and the outer `finally` still clears `diffToggleInFlight`). Round-trip verified by reading the off→on and on→off branches: both now run `setReadOnly(false)` before any `changes:` dispatch and restore `setReadOnly(true)` after. Candidate 2 (`Ipc.readFile` erroring silently) was not the culprit — the existing `try`/`alert` covered that path; left unchanged. |

Fixed 2026-05-13 (tree dirty dot only on active tab):

| Slug | Fix |
| ---- | --- |
| `bug-tree-dirty-dot-only-on-active-tab` | `paintDirtyTreeDot` in `ui/src/app/statusBar.ts` only marked the active buffer's tree row with `.dirty` and stripped the class from every other row — so a background tab with unsaved edits lost its dot the instant the user switched to a sibling tab. Fix: build a set of paths to mark by walking `tabStore.openBuffers` and applying the same per-entry dirty check the tab strip uses (`savedState.doc.toString() !== buffer.loadedText` for background entries, live `isDirty()` for the active one). Every row with a dirty open buffer keeps its dot regardless of which tab is active. Pending agent-review files were already covered by the `staging-dirty` class applied during `tree/index.ts::renderDir` (CSS at `ui/src/style.css:2774`), so no change was needed for that prong of the user's ask. |

Fixed 2026-05-13 (staging list order non-deterministic within same-ms batch):

| Slug | Fix |
| ---- | --- |
| `bug-staging-list-order-flaky-within-batch` | `build_list_query` in `core/src/staging.rs` ordered by `created_at_ms ASC, id ASC`. ULIDs share their 48-bit timestamp prefix within the same millisecond but have random 80-bit tails, so two rows inserted by one `propose_batch` call (same ms) sorted in an order that didn't match insertion order. The test `propose_batch_assigns_shared_batch_id_and_per_edit_payloads` flaked accordingly. Switched the tiebreaker to `rowid ASC` — SQLite's hidden rowid (the table has `id TEXT PRIMARY KEY`, not `WITHOUT ROWID`) increments with insertion order and is stable. |

Fixed 2026-05-13 (tab-strip click swallowed mid-edit):

| Slug | Fix |
| ---- | --- |
| `bug-tab-switch-requires-double-click-after-edit` | With ≥2 dirty buffers open, an edit followed immediately by a click on another tab required two clicks to switch. Root cause: `tabStrip.render()` in `ui/src/tabStrip/index.ts` unconditionally wiped & rebuilt every tab DOM node via `replaceChildren()`. It's invoked from `updateStatus()`, which runs on every CM6 `ViewUpdate` via the `statusUpdater` ViewPlugin in `ui/src/app/editor.ts` — including the `focusChanged` update produced when the editor blurs as a result of the tab-strip mousedown. The synchronous DOM swap landed between `mousedown` and `mouseup` on the same tab; the browser only emits `click` when both events hit the same element, so the first click was silently swallowed. The "only after a new edit" pattern is just the case where the recent doc-change makes the surrounding render path active (every keystroke also triggers a render, but it's the blur-driven render that lands between mouse events). Fix: memoize the rendered signature (active path, plus per-tab `[path, basename, folder, dirty, preview, kind]`) and short-circuit when nothing changed since the last render. DOM stays stable across no-op re-renders, mousedown/mouseup land on the same node, click fires on first attempt. Also a free perf win — every keystroke previously rebuilt N tab buttons. |

Fixed 2026-05-13 (write-note review drops dirty buffer on exit):

| Slug | Fix |
| ---- | --- |
| `bug-write-note-review-reject-discards-dirty-buffer` | `openWriteNoteReview` in `ui/src/main.ts` clobbered the live CM6 editor with the proposed content without first persisting the outgoing file-mode buffer's state to `openBuffers[path].savedState` — the user's unsaved edits only existed in the live editor, so the dispatch silently dropped them. `exitWriteNoteReview` then cleared the editor on reject (or Exit) instead of restoring the prior tab, so the dirty buffer was unreachable even though `openBuffers` still held a (stale) entry. Fix: (1) on entry, snapshot `editor.getState()` into the outgoing file-mode buffer's `savedState` (same persist step `activateTab` already does on tab switch); (2) on exit, if a file-mode buffer for `mode.targetPath` exists in `openBuffers`, call `tabs.activateTab(targetPath)` to restore it (CM6 state, dirty content, undo history) — otherwise fall back to the previous "clear editor" behavior. Accept path was unaffected because `write-note-review-blocks-on-dirty` refuses accept while any open buffer for the target is dirty. |

Fixed 2026-05-13 (staging storage moves to SQLite):

| Slug | Fix |
| ---- | --- |
| `bug-staging-storage-not-sqlite` | Rewrote `core/src/staging.rs` against a SQLite `<vault>/.hiker/staging.db` (single `proposals` table, schema per `settings.md` "Storage"). Content lives in a zstd-compressed BLOB column (level 3, same encoding as `changes-content-zstd`); per-id `.md` sidecars removed. `list(StagingFilter)` now runs an indexed SQL query (filter clauses on `target_path`/`surface`/`state`/`trail_id`/`json_extract(metadata,'$.session_id')`) instead of scanning an in-memory array. WAL + `synchronous=NORMAL`, `pragma user_version` with fail-loud schema-version mismatch. Pre-1.0 — no legacy migration code; users with the prior JSON+sidecar layout delete `.hiker/staging/` to start fresh. `staging_content` command unchanged (delegates to `Staging::content`, which now reads the BLOB). Public API of `core::staging` is unchanged; callers don't move. |

Fixed 2026-05-13 (nav history across app-page tabs + "app pages" naming cleanup):

| Slug | Fix |
| ---- | --- |
| `bug-nav-history-broken-for-app-page-tabs` | Three independent gaps fanned out into one failure mode (nav stack stuck / skipping). (1) `inferNavState` in `ui/src/main.ts` now derives state from the active tab's `(kind, payload)` — parsing `home-detail` view and `properties` path out of the synthetic `__hiker:<kind>[:<payload>]` tab key — instead of reading legacy DOM `hidden` / class flips. Added a `properties` `NavState` variant with a matching `applyNavState` branch (`openPropertiesTab(s.path)`). (2) Dropped the legacy `MutationObserver` in `ui/src/main.ts` that watched `editor-pane` class flips and the three vault-home `hidden` attributes: every kind switch already goes through `app/tabs.ts::activateTab`, which ends with `checkpointNav()`. The observer fired *again* after the canonical checkpoint (and on intermediate DOM mutations from `renderActiveTab`), double-pushing and occasionally reading mid-transition state. (3) `mountNavigation.navigate()` in `ui/src/navigation/index.ts` now captures `prev = current` *before* awaiting `apply`. `apply` for app-page kinds calls `openAppPageTab`, which evicts the active preview tab and calls `nav.pruneTab(previewTabPath)`; pruneTab nullified `current` when it matched, so the subsequent `dst.push(current)` silently dropped the entry the user just navigated away from (the "skipped entries" symptom). The push now uses the captured `prev`. `pruneTab` also now recognizes `properties` NavState entries when the closed tab's synthetic key has the `__hiker:properties:` prefix. **Naming cleanup folded in:** umbrella term "app pages" (or "app-page tabs" where the tab-strip aspect matters) replaces the inconsistent "page-kind tabs" / "meta pages" wording across `editor.md`, `status.md`, and code comments. TS helpers renamed: `pageTabKey` → `appPageTabKey`, `openPageTab` → `openAppPageTab`. `TabKind` union members are unchanged (already per-kind specific). `editor.md` §`tab-kinds` now introduces "app pages" in one sentence and uses it consistently below. Slug `tab-kinds` itself keeps its name. |

Fixed 2026-05-13 (home recent-activity widget consumes unified feed):

| Slug | Fix |
| ---- | --- |
| `bug-home-recent-activity-missing-pending-agent-review` | `refreshActivityWidget` in `ui/src/vaultHome/index.ts` now calls `Ipc.activityCount({source:"merged"})` and `Ipc.activityList({source:"merged", limit:5})` instead of the changes-only `changes_count` / `recent_changes` pair, so pending staging proposals appear as preview rows alongside committed change rows — same `core::activity` feed the editor status-bar version dropdown (`status-bar-version-dropdown-uses-unified-feed`) consumes. New `buildPendingPreviewRow` builds a compact staging row with inline `[Accept]` / `[Reject]` buttons (per `staging-accept-reject-from-activity-detail`); clicking the row routes to `onOpenStagingProposal`. Added `notifyStagingChanged()` to `VaultHomeApi`; `ui/src/main.ts`'s existing `hiker:staging-changed` listener now calls it so the widget repaints live when proposals appear/disappear/are accepted/rejected. |
| `bug-agent-created-note-preview-missing-accept-reject` | `openStagingPreview` / `closeStagingPreview` in `ui/src/main.ts` now call `updateStatus()` (which re-runs `renderActiveTab()`) instead of only repainting the status bar + mode-controls slot. When the user opened the staging preview from the activity-detail page (a `home-detail` kind tab), `renderActiveTab` had set `modeControlsEl.hidden = true`; the buffer-kind transition into the staging-preview wasn't re-evaluating toolbar visibility, so the staging mode-controls renderer ran into a hidden host and Accept/Reject never appeared. The buffer state already stamps the rel-path + proposal id (`mode.targetPath` / `mode.proposal_id`) and the staging mode-controls renderer was already in place — only the toolbar-visibility refresh was missing. Same fix applied symmetrically on close so the toolbar hides again when returning to the activity-detail page. Accept-navigate path (`bug-staging-accept-no-navigation-staging-preview`) unchanged. |

Fixed 2026-05-11 (config watcher + events bus cleanup):

| Slug | Fix |
| ---- | --- |
| `bug-config-toml-on-disk-changes-not-observed` | Added `notify` watcher on both config TOML files (user-scope `~/.config/hiker/config.toml` and vault-scope `vault/.hiker/config.toml`) in the host. Debounced at 500ms; on change calls `Config::load()`, swaps the in-memory `RwLock<Config>` (plus live `tasks.set_cfg` / `mcp_tools` mirrors), and emits `hiker:config-reloaded` with the merged config payload. Write-loop suppressed: `set_setting_inner` records `Instant::now()` in a shared `AppState::config_last_write` mutex; the config watcher skips reloads within 2s of a write (same `SUPPRESS_TTL` shape as the vault watcher). Watcher task is canceled on vault swap via a `CancellationToken` on `VaultSession`. Frontend listener in `ui/src/main.ts` calls `applySettingsToUi` with the new config. |
| `bug-events-bus-settings-changed-no-consumer` | Deleted the `"settings-changed"` event type from `ui/src/events/bus.ts::EventMap` (and its `Settings` import) and removed the `settings.onSettingsChanged` → `emitBusEvent("settings-changed", ...)` emitter from `ui/src/main.ts`. The real-time settings sync path is now `hiker:config-reloaded` (an event from the config-file watcher above), whose frontend listener calls `applySettingsToUi` directly — no bus indirection needed. |

Fixed 2026-05-13 (staging accept new-file tree marker):

| Slug | Fix |
| ---- | --- |
| `bug-accept-new-note-from-activity-page-leaves-stale-dirty-marker` | Race in the `hiker:staging-changed` listener in `ui/src/main.ts`: the handler fired `tree.api.refreshStagingProposals()` and `tree.api.refresh()` as parallel `void`-promises, so the tree re-render frequently read the stale `pendingProposals` cache (still containing the just-accepted proposal) and re-painted the new file's row with `staging-new` / `staging-dirty`. Fix awaits `refreshStagingProposals()` before `refresh()` inside an async IIFE so the tree's render always sees the post-accept proposal set. Matches the sequencing already used by the tree-context-menu Accept verb. |

Fixed 2026-05-12 (staging accept navigation):

| Slug | Fix |
| ---- | --- |
| `bug-staging-accept-no-navigation-chat` | Chat tool-call card accept handler now captures the `AcceptOutcome` from `Ipc.stagingAccept` and calls the existing `onOpenNoteLink(outcome.target_path)` callback, opening the target note as a preview tab. Reuses the existing note-link seam (`bug-chat-couples-to-main-buffer-globals`) instead of adding a new dep. |
| `bug-staging-accept-no-navigation-activity-detail` | Host `onAcceptStaging` callback in `ui/src/main.ts` now captures `target_path` from the `AcceptOutcome`, calls `openFile(target_path, { preview: true })`, and if the file was already open as a sticky tab it activates the tab and reloads the buffer from disk (clean → silent reload via `openForEdit`; dirty → toast warning). `vaultHome` row builder delegates unchanged. |
| `bug-staging-accept-no-navigation-staging-preview` | Staging preview mode accept button now captures `target_path` from `Ipc.stagingAccept` and calls `openFile(target_path, { preview: true })` instead of `closeStagingPreview()`, so the user lands on the accepted file rather than returning to the activity detail page. Reject path unchanged (still calls `closeStagingPreview()`). |

Fixed 2026-05-11 (UI bugs):

| Slug | Fix |
| ---- | --- |
| `bug-queue-detail-toggles-stale-on-mount` | Added `refreshFromSettings()` to `QueueDetailApi` in `ui/src/queueDetail/index.ts`, which re-seeds the direct-worker + expose-to-chat-agent checkboxes and worker-preference dropdown from `Ipc.getSettings()`. Called from `ui/src/main.ts::applyOpenedVault` and the `vault-opened` bus event subscriber; also re-seeds when the toggles section becomes visible (tasks filter pill flips on). Fixes the stale-display gap where `mountQueueDetail` ran at module-load before any vault was open.
| `bug-dropdown-button-click-toggles-not-closes` | Added `openMenuTrigger` tracking in `ui/src/widgets/contextMenu.ts`. `openContextMenu` now accepts an optional `triggerEl` parameter; when called with the same trigger while its menu is open, it closes instead of re-opening (toggle behavior). Call sites in `modeControls/index.ts`, `mutations/index.ts`, `tree/index.ts`, and `trails/index.ts` pass their trigger element. Outside-click dismissal unchanged.
| `bug-remove-waypoint-trash-not-refreshed` | Added `onWaypointRemoved` callback to `TrailsDeps` in `ui/src/trails/index.ts` (mirrors the existing `onWaypointAppended` pattern). Fired after successful `removeWaypoint` → `refresh()`. Wired in `ui/src/main.ts` to `trash.api.refresh()` + `vaultHome.api.notifyChangesAppended()` so the trash panel and home activity surface pick up the cascaded deletes (the watcher suppresses events for deleted paths).
| `bug-chat-input-double-height-on-first-open` | `autoSizeInput` in `ui/src/chat.ts` now clears inline height when the input is empty and defers the initial sizing pass via `requestAnimationFrame` after mount, so `scrollHeight` reads the laid-out element rather than a zero-width / display-pending state. Empty textareas let CSS `min-height` carry the row.
| `bug-chat-input-not-resizable-within-panel` | Added `.chat-input-resize-handle` div above the chat input in `ui/src/chat.ts` with pointer-down/move/up drag (mirrors `chat-panel-vertical-resize`). Min height 28px, max bounded by chat region minus transcript floor. Auto-grow defers to user-set height when `userInputHeight > 0`. Persists per-vault under `vault.chat_input_height` via settings write-back (new `NonNegativeInt` value type in `core/src/config.rs`). CSS in `ui/src/style.css`.

Fixed 2026-05-11 (tab-kinds migration):

| Slug | Fix |
| ---- | --- |
| `bug-editor-pane-not-extracted-from-main-ts` | S3 landed: new `ui/src/editorPane/index.ts` exporting `mountEditorPane(deps): EditorPaneApi` — owns CM6 view, buffer rendering, save/drift, buffer-scoped toolbar, status bar, mode controls, View menu, dirty-buffer diff, mutations menu. Host in `main.ts` wires deps and embeds the host into tabs/openFile/autosave deps. |
| `bug-page-views-not-tab-kinds` | S2 landed: page-shaped center-pane views (home, home-detail, queue, settings) now render as tabs in the top tab strip via `openAppPageTab()`. Editor toolbar + status bar hide on non-buffer tabs via `renderActiveTab()`. Home/Queue/Settings buttons route through `openAppPageTab`. Migrated from CSS-class-based sub-mode to app-page tabs. |

Fixed 2026-05-10:

| Slug | Fix |
| ---- | --- |
| `bug-filetree-trail-expand-shows-no-waypoints` | clicking the chevron on a trail-doc row in the file tree no longer surfaced the trail's waypoint-notes nested under the row. Regressed when slice S1b reshaped `renderDir` in `ui/src/tree/index.ts`: the trail-waypoint child-container was appended via `li.after(childContainer)` *before* `ul.appendChild(li)`, so the `li` had no parent and `Element.after()` silently no-ops on a parentless node. The folder branch dodges this by deferring through `queueMicrotask`; the trail branch ran synchronously. Fix: move the trail-waypoint expansion block to *after* `ul.appendChild(li)`, mirroring the (now correct) sibling-insertion pattern. No change to `appendWaypointChildren` (it correctly walks `.children` recursively); no change to `toggleTrailExpansion`. Confirmed the data path: `Ipc.trailGet` returns `TrailDetail.waypoints: ResolvedWaypoint[]`, cached into `trailDetailCache`, walked recursively by `appendWaypointChildren` |
| `bug-tab-switch-promotes-outgoing-preview` | Switching tabs from a preview tab to any other tab demoted the preview tab to sticky, contradicting `editor-preview-tab-promotion`'s explicit promotion paths (edit, double-click tab, drag, "Keep open"). Root cause: in `ui/src/app/tabs.ts::activateTab`, the CM6 `applySavedState` / first-activation `dispatch` runs before `setBufferState({ buffer: target.buffer, ... })`. CM6's update listeners fire synchronously on dispatch — including the `statusUpdater` ViewPlugin (`ui/src/app/editor.ts`) which calls `updateStatus()` → `isDirty()` → reads `getBuffer()` which still returns the *outgoing* buffer. The doc has already swapped to the incoming target's content but the buffer pointer hasn't, so `view.state.doc.toString() !== buffer.loadedText` returns true → `updateStatus()` calls `promotePreviewIfActive()` → outgoing preview tab is demoted. Fix: set `setBufferState({ buffer: null })` immediately before the doc dispatch, mirroring the same pattern `replacePreviewWith` and `openFile` already use. With buffer null during the dispatch window, `isDirty()` short-circuits to false (line 266: `if (!buffer || ...) return false`), no false-positive promotion fires. The new buffer pointer lands at the existing post-dispatch `setBufferState({ buffer: target.buffer, activePath: rel })`. Per spec line 645: "Activating a preview tab from a different sticky tab is a normal tab switch, not a re-open. The italic stays — the preview is still a preview until promoted" — same rule applies in the reverse direction (switching *away* from a preview to any other tab also doesn't promote) |
| `bug-add-to-trail-verbs-dont-refresh-panel` | Both "Add to trail" verbs (editor toolbar pill in `ui/src/trails/addToTrailPill.ts` + tree right-click in `ui/src/tree/index.ts`) ended with `Ipc.trailAppendWaypoint` and relied on the `hiker:file-changed` listener in `ui/src/main.ts` to refresh the trails panel + membership cache. But `core::trails::append_waypoint` calls `Watcher::suppress` on the trail-doc + waypoint-note paths (correctly, to prevent indexer feedback loops), so notify drops events for those paths and the panel never repaints until the user toggles active trails. Fix: added an `onAppended?` opt to `mountAddToTrailPill` and an `onWaypointAppended?` field to `TreeDeps`; both call `trailsPanel.api.refresh()` + `refreshActiveTrailWaypointPaths()` from the click handler's success path. Wired both from `ui/src/main.ts` at the pill mount + `mountTree` deps. Same deps-callback pattern the `+`-button create-trail flow already uses; no watcher-predicate broadening, no change to suppression semantics |
| `bug-id-stamping-mints-fresh-ulid-instead-of-adopting-path-ids` | `core::ops::ensure_note_id_stamped` now takes `store: &mut Store` and adopts the indexer's `path_ids[rel]` ULID when present, only minting a fresh `new_id()` when the path has never been ingested. `core::trails::append_waypoint` threads the store through; the host seam (`trail_append_waypoint_inner`) opens a fresh `Store::open(vault.root())` reader for the call (the shared `read_store: Arc<Mutex<Store>>` can't cross `.await` because `MutexGuard` isn't `Send`). Belt-and-braces in the indexer: `core::indexer::process_upsert` (both empty-chunks and chunked branches) now consults a new `frontmatter_hiker_id(contents)` helper before falling through to `new_id()` when `path_ids` has no row — so the case where a waypoint is created against a not-yet-ingested source still keeps the two systems in sync on the next ingest. Pre-existing inconsistency (frontmatter id ≠ path_ids id) is logged and left alone — clobbering risks user data; warrants a separate slug if it ever appears in real data. New regression test `trails::tests::append_waypoint_adopts_indexer_path_id_for_source`: ingests a source, captures `path_ids` id Y, calls `append_waypoint`, asserts the waypoint's `references.id == Y` and that `resolve_reference` returns `Resolved` (not `PathConflict`). Already-broken waypoints from before the fix keep rendering as orphans — out of scope per the bug report; user manually deletes and re-adds |
| `bug-trails-dropdown-stale-on-delete` | trails-panel `hiker:file-changed` filter in `ui/src/main.ts` (the trails-mode-body block at the top of the listener) only refreshed on `.hiker/trails/` paths or the active trail-doc path, so deleting a non-active trail-doc from the file tree (which moves the trail-doc + cascaded waypoint dir to trash via `core::trails::delete_trail`) left `trails_list` cache stale; the Trails-mode dropdown then listed a deleted trail and activating it errored. Fix: broadened the predicate to also refresh on any `.md` change outside `.hiker/` (created/deleted/renamed) — same conservative posture as the sibling `trail-row-icon` block immediately below, which already refreshes the tree's trail-doc set on the same predicate. Trails-list is a small per-vault query so the extra calls are acceptable. Also wired an active-trail-deleted fallback in `ui/src/trails/index.ts::refresh`: when the previously-active rel is no longer in the freshly-fetched `trails_list`, call `Ipc.trailSetActive(null)` and re-paint so the dropdown falls back to "None" cleanly instead of showing the deleted trail's basename |
| `bug-trails-flat-not-tree` | Slice S1a backend foundation rewrite — landed alongside `bug-trails-filename-encodes-seq` since they overlap. (1) `TrailDocFrontmatter.waypoints` is now `Vec<WaypointEntry>` with recursive `waypoints: Vec<WaypointEntry>` children; `parse_trail_doc` / `write_trail_doc_frontmatter` recurse via `parse_waypoint_entry` / `waypoint_entry_to_json`; pre-existing flat-format yaml still parses cleanly as a tree of all-root entries. New helpers `walk_waypoints_depth_first` / `find_waypoint(_mut)` / `collect_descendant_ids` / `remove_waypoint_from_tree`. (2) Schema bumped to v6 — `trail_waypoints` now carries `parent_waypoint_id TEXT NULL` + `tree_path TEXT NOT NULL DEFAULT ''`, plus a `trail_waypoints_parent_waypoint` index; `seq` is dropped; `waypoints_of` orders by `tree_path`; `WaypointRow` + `TrailContainingHit` updated. Fail-loud per `store-version-fail-loud` — pre-real-use policy means delete + reindex, no migration code. (3) `append_waypoint` gained `parent_waypoint_id: Option<&str>` (None = root append; named parent uses `find_waypoint_mut`). (4) `remove_waypoint` cascades: collects every descendant path from the subtree before mutating, deletes each via `core::ops::delete` (one trash entry + `core::changes` row per descendant), drops the subtree from the trail-doc, returns `RemoveWaypointOutcome { removed_count, removed_paths }`. New helper `descendant_count` lets the UI pre-compute the cascade size for the confirm dialog without executing. (5) Indexer `update_trail_waypoints_if_relevant` rewritten: trail-doc ingest captures existing rows (to preserve per-row `source_path` / `source_id` written by the per-waypoint ingest path), clears all rows for the trail, and re-derives via `walk_waypoints_depth_first` so `parent_waypoint_id` + `tree_path` are filled canonically; per-waypoint ingest writes those columns as `(NULL, "")` and relies on the trail-doc walk to fill them. (6) `ResolvedWaypoint` + `TrailDetail.waypoints` are now recursive (`children: Vec<ResolvedWaypoint>`, `tree_path: String`); `get_trail` walks the resolved tree via `resolve_waypoint_tree`. (7) host seam: `trail_append_waypoint` accepts `parent_waypoint_id`; `trail_remove_waypoint` returns `WaypointRemovedDto { removed_count }`; new `trail_descendant_count` command; all wired through `invoke_handler!`. (8) UI ipc surface: `ResolvedWaypoint` interface mirrors the recursive shape, `Ipc.trailAppendWaypoint` accepts `parentWaypointId`, `Ipc.trailRemoveWaypoint` returns `WaypointRemoved`, new `Ipc.trailDescendantCount`. (9) UI rendering kept working via two temporary `flattenTree` / `flattenWaypointTree` stopgaps (marked `// TODO trails-mode-side-trail-render`) in `ui/src/trails/index.ts` and `ui/src/tree/index.ts`; slice S1b replaces them with proper indented rendering. Test coverage: `parse_trail_doc_round_trips_nested_tree`, `parse_trail_doc_round_trips_empty_tree`, `parse_trail_doc_old_flat_format_parses_as_root_tree`, `walk_waypoints_yields_depth_first_with_tree_paths`, plus the existing `remove_waypoint`/`move_*` tests adapted to the new shape. Status updates: `trail-doc-shape`, `trail-storage-layout`, `trail-waypoints-derived-table`, `trail-side-trail-shape` all flipped to `done`; `trails-mode-remove-waypoint-verb`, `trails-mode-waypoint-card`, `trail-row-dropdown-chevron` stay `partial` pending the slice S1b UI work |
| `bug-trails-no-build-as-you-read-editor-verb` | Landed slug `trail-add-to-active-from-editor-verb` (slice S2). New `#add-to-trail-pill` button placed in the right-side toolbar cluster, just left of Save, in `ui/index.html`; mounted by `ui/src/trails/addToTrailPill.ts::mountAddToTrailPill` from `ui/src/main.ts`. Visibility: active trail set + `bufferStore.buffer.mode.kind === "file"` + path is `.md`/`.txt` + path is not a trail-doc (via `tree.api.isTrailDoc`, exposed alongside) + path doesn't start with `.hiker/trails/`. Label `"Add to trail: <basename>"` doubles as the always-visible active-trail indicator outside Trails mode. Click calls `Ipc.trailAppendWaypoint` directly (NOT `captureToActiveTrail` — the helper swallows errors per its capture-flow contract; the editor verb is user-explicit and needs error visibility, mirroring the tree-verb pattern). Idempotency: shared synchronous membership cache in new `ui/src/trails/membership.ts` (`getActiveTrailWaypointPaths`, `subscribeMembership`, `refreshActiveTrailWaypointPaths`, `installMembershipWatchers`), populated via `Ipc.trailGet` walking `ResolvedWaypoint.children` recursively (one call per refresh covers every path in the active trail at once — cheaper than per-note `trails_containing_note` lookups), refreshed on `activeTrailStore` changes and `hiker:file-changed` events under `.hiker/trails/` or against the active trail-doc. Pill subscribes to `activeTrailStore` / `bufferStore` / `subscribeMembership` for re-renders; disables itself + tooltip "Already in this trail" when membership cache reports the open path. Same cache also drives idempotency for the existing tree-verb (`trail-add-to-active-from-tree-verb`): `attachContextMenu` reads `getActiveTrailWaypointPaths().has(rel)` synchronously inside the menu builder, disabling the verb with the same tooltip. Backend half: new command `trails_containing_note(source_rel) -> Vec<TrailsContainingNoteHit { trail_id, trail_doc_rel }>` over a new `core::trails::trails_containing_note_with_paths` (pairs derived-table `trail_id`s from `Store::trails_containing_note` with their trail-doc rel-paths via the same `list_trails` walk the dropdown uses); `Ipc.trailsContainingNote` exposes it. The command itself isn't on the hot path of the pill (the `trail_get`-based cache is cheaper for the bulk-membership case) but lands now for the planned `mcp-tool-trails-containing-note` consumer and for cases where a UI surface needs cross-trail membership for one note rather than within-active-trail membership for many notes |
| `bug-trails-filename-encodes-seq` | Landed in the same slice S1a pass as `bug-trails-flat-not-tree`. `waypoint_filename` rewritten to `(source_basename, waypoint_id) -> "<basename>--<short-id>.md"` where `short_id_of` returns the upper-cased last 6 chars of the ULID. `append_waypoint` mints the waypoint id first and feeds it into the filename helper (no `seq` parameter remains). Collision handling: if the primary filename already exists (vanishingly rare, since the short-id is per-waypoint-id), the op iterates `<basename>_2--<short>.md`, `<basename>_3--<short>.md`, ... up to 1000 candidates. Filenames are now stable identifiers — never renamed on reorder/re-parent — and frontmatter owns order + tree shape. Test `waypoint_filename_uses_short_id_suffix` covers the happy path, the upper-casing, and the short-id-fallback for ulids shorter than 6 chars. No on-disk migration: the indexer doesn't care about the filename shape, frontmatter is truth, and existing dev-vault waypoints keep their old basenames |

Fixed 2026-05-09:

| Slug | Fix |
| ---- | --- |
| `bug-trails-and-tree-org-icons-in-editor-toolbar` | deleted both `<button id="trails-menu-btn">` and `<button id="tree-org-menu-btn">` from `ui/index.html`'s editor toolbar. No CSS or TS referenced either id (verified via grep). The trails-glyph + tree-org-glyph SVGs were salvaged into the new `sidebar-mode-switcher` as the Cluster trees / Trails buttons per the row's "reused inside sidebar-mode-switcher when that lands" guidance |
| `bug-sidebar-top-row-not-unified` | collapsed the prior two-row sidebar header (`#sidebar-mode-switcher` + separate `#tree-toolbar`) into a single uniform icon-only row that's persistent across every mode: `[Files][Clusters][Trails] | [+] [⋯]`. `+ New note` rendered as a compact icon button (`#new-note-btn` now styled as `.sidebar-mode-btn` with a plus glyph); `⋯` actions menu now `#sidebar-actions-btn` (renamed from `#tree-actions-btn`); flex spacer + 1px divider sit between the mode buttons and the `+` / `⋯` cluster. Removed `<header id="tree-toolbar">` from `ui/index.html` and the `#tree-toolbar` / `#new-note-btn` rules from `ui/src/style.css`. Renamed the dep on `TreeDeps` from `treeActionsBtn` to `sidebarActionsBtn`; `domRefs.tree.sidebarActionsBtn` byId binds `sidebar-actions-btn`. Updated the two `// status: tree-toolbar-actions-menu` markers in `ui/src/tree/index.ts` to `// status: sidebar-toolbar-actions-menu`. The `+` button stays behaviorally Files-mode-only for now (Clusters/Trails currently have no body to attach to); when those modes' bodies land, each owns its own routing per spec — this row's scope is the layout/persistence/rename, not mode-aware behavior. `⋯` menu contents stay filetree-only until `cluster-editor-mode-menu` and the trails menu land — tracked under `sidebar-toolbar-actions-menu`'s remaining partial |
| `bug-render-mixed-with-business-logic` | migrated all five render sites onto pure `domFor*(model): HTMLElement` builders + container-level click delegation: `discovery/index.ts::renderSearchResults` (proof-of-shape; `domForSearchResult` + `onSearchListClick`), `discovery/index.ts::renderRelated` (`domForRelatedRow` + `onRelatedListClick` on `relatedListEl`, `data-rel` carries the hit path), `vaultHome/index.ts::renderRecentList` (`domForRecentRow` + `onRecentListClick`, `data-path` on the `<li>`; delegation attached once per `<ul>` via a `WeakSet` guard so `replaceChildren` cycles don't restack listeners), `tree/index.ts::renderDir` (`domForTreeRow` builder produces `<li>` with `data-path` / `data-kind`; `onTreeClick` on `treeEl` resolves clicks via `closest("li[data-path]")`, dispatches folder toggle vs. `openNote` based on `data-kind`; per-row dnd / contextmenu / dblclick stay attached because they need per-row data the global delegation can't reconstruct, surfacing here per the bug row's "applyIndexMarker is fine as a class-toggle helper, focus on the row builder" guidance — `applyIndexMarker` left untouched), `queueDetail/index.ts` (`domForTaskRow` + `domForIndexRow` + `domForTaskRowWithBadge`; container click on `root` reads `data-task-id` for toggle-expand, `data-action="cancel"` for the cancel button short-circuit; gates toggle on click target being within `.queue-row-head` so clicks inside an expanded details panel don't collapse the row). Keyboard nav (`Enter` dispatching a synthetic `click()` on the focused row) keeps working because the delegated listeners pick up dispatched clicks identically. `npx tsc --noEmit` passes |
| `bug-panels-duplicate-mount-pattern` | migrated all five sidebar panels (`tree`, `discovery`, `vaultHome`, `queueDetail`, `trash`) onto `PanelController<Api>`; `tree` / `discovery` are sidebar-managed so expose `isVisible: () => true; setVisible: noop` while migrating just for the api-under-`controller.api` shape, `vaultHome` / `queueDetail` route their own setVisible side effects through the shared `onSetVisible` hook, and each panel's deps interface now `extends PanelDeps` so the cross-panel uniforms (`toast` / `formatErr` / `settings` / `openNote` / `focusEditor`) come from one bundle. main.ts callsites updated from `panel.X()` to `panel.api.X()` (controller-level `isVisible` / `setVisible` stay panel-level). `npx tsc --noEmit` passes |
| `bug-inline-svg-strings-scattered` | new `ui/src/icons/index.ts` exporting `Icons.{trash,robot,brain,diff,restore,close,user,dot}()` and a shared `svgWrapper(content, { size?, strokeWidth? })` owning the `viewBox="0 0 16 16"` / `fill="none"` / `stroke="currentColor"` / `stroke-linejoin` / `stroke-linecap` / `aria-hidden` envelope. Per-icon function defaults capture each call site's prior size/stroke (e.g. `Icons.trash()` defaults to size=12 / stroke=1.5 for the chat session row; `Icons.robot()` defaults to size=14 / stroke=1.4 matching the queueDetail pill, and the recent-activity `agent` pill calls `Icons.robot({ size: 12, strokeWidth: 1.4 })` to match its prior 12px form; `Icons.brain()` keeps the 0.9 stroke needed for legibility at 14px). `Icons.dot()` is the only fill-only icon and bypasses the stroke wrapper. Migrated every call site: `chat.ts:444` → `Icons.trash()`; `queueDetail/index.ts` `ROBOT_ICON_SVG`/`BRAIN_ICON_SVG` constants now thin aliases over `Icons.robot()` / `Icons.brain()`; `vaultHome/index.ts::authorPillIcon` collapses three inline strings into `Icons.user()` / `Icons.robot({...})` / `Icons.dot()`; `modeControls`'s `ICON_DIFF` / `ICON_RESTORE` / `ICON_CLOSE` deleted (main.ts swaps to `Icons.diff()` / `Icons.restore()` / `Icons.close()` at the four `iconButton` call sites — 2 snapshot-mode, 1 trash-mode close). `currentColor` flows through to the css-variable-driven theme tokens that landed in the paired `bug-hardcoded-colors-bypass-css-variables` slug, so swapping accent / surface tokens repaints the icons for free. `npx tsc --noEmit` passes |
| `bug-hardcoded-colors-bypass-css-variables` | one-pass tokenized every color literal in `ui/src/style.css` into a single consolidated `:root` block at the top of the file (the previous two `:root` blocks at L1 and L2249 are now one). New tokens: `--accent-hover` / `--accent-soft-fill` / `--accent-link*` / `--accent-chunk` / `--accent-on-strong` (joining the existing `--accent` / `--accent-fill*`); `--text-default` / `--text-muted` / `--text-dim` / `--text-faint` / `--text-strong` (the three near-duplicate greys `#888` / `#999` / `#aaa` collapsed into `--text-muted`, and `#ccc` / `#ddd` into `--text-default`); `--surface-hover-soft` / `--surface-hover` / `--surface-active` / `--border-default` / `--border-strong` for the historical `#888X`-alpha family; `--bg-app` / `--bg-popover` / `--bg-toast` / `--bg-pre` / `--bg-zebra` / `--bg-panel-elevated` / `--bg-panel-elevated-strong` / `--border-panel-strong` / `--border-divider` (collapsing `#2a2a2a` + `#2c2c2c`) / `--border-input`; `--shadow-soft` / `--shadow-strong` / `--bg-overlay-modal`; `--danger` / `--danger-hover` / `--danger-close-hover` / `--danger-text` / `--danger-fill` / `--danger-border` / `--danger-text-strong`; `--warn` (folding `#e0a040` + `#f0a020`) / `--warn-strong` / `--warn-soft-text` / `--warn-fill` / `--highlight-mark`; `--success` / `--success-strong`; `--diff-add-fill` / `--diff-del-fill` / `--diff-sep-border`; `--lp-code-bg` / `--lp-fence-bg`; existing `--queue-pri-high-color` / `--queue-pri-high-border` / `--queue-active-pulse-color` moved up. Every CSS rule body now references tokens only — no bare `#hex` / `rgba(...)` literals appear outside the `:root` block. TS-side: 17 color literals across 5 files (`settings.ts`, `editor/hideFrontmatter/index.ts`, `editor/chunkBoundaries/index.ts`, `diff/index.ts`, `editor/livePreview/index.ts`) replaced — `settings.ts` swaps `style.color = "#888"` for `classList.add("txt-muted")` (new helper class added at top of `style.css`); the four CM6 baseTheme entries swap their inline hex for `"var(--token)"` strings, which CM6's stylesheet generator passes through as-is. `npx tsc --noEmit` passes |
| `bug-drift-resolution-policy-in-ui` | added `core::ops::resolve_drift(vault, changes, rel, choice: DriftChoice, new_text, extra_metadata) -> DriftResolution` with `DriftChoice = KeepMine | TakeTheirs | Cancel` and `DriftResolution = Written { new_hash, token } | TookTheirs { contents, token } | Cancelled`. UI's `confirm3`-driven keep/take/cancel branching now shrinks to "show modal, dispatch chosen variant" via the new `Ipc.resolveDrift` wrapper; modal copy + default-focus stay in `app/openFile.ts` (presentation). Both the save-time drift modal (`handleDriftDetected`) and the watcher-conflict-dirty modal route through the same op so MCP/CLI/agents get a typed surface for the conflict path |
| `bug-buffer-hash-tracking-in-ui` | added `core::ops::open_for_edit(vault, rel) -> { contents, token: BufferToken }` and `commit_buffer(vault, changes, token, new_text, extra_metadata) -> CommitOutcome` (`Written { new_hash, token } | DriftDetected { current_disk_text, current_hash }`). `BufferToken` is an opaque struct (private path/content_hash/opened_at_ms fields, serde-round-trippable). UI's `Buffer.loadedHash` field replaced by `Buffer.token: BufferToken | null`; every save/open/mutation/silent-reload path migrated to `Ipc.openForEdit` + `Ipc.commitBuffer`; `Ipc.readFileWithHash` / `Ipc.writeFileChecked` deleted from the UI's IPC surface (read-only previews now use a thin `Ipc.readFile`). On `DriftDetected`, the UI dispatches to `resolve_drift` per the paired slug above. commands `open_for_edit` / `commit_buffer` / `resolve_drift` registered as 5–15-line wrappers in the host |
| `bug-css-class-strings-scattered` | added `ui/src/style/classes.ts` exporting `Classes` (`IX_UNSUPPORTED`, `IX_SKIPPED`, `IX_QUEUED`, `IX_INDEXED`, `QUEUE_PILL`, `DROP_TARGET`, `RELATED_ITEM`), `Selectors` (`QUEUE_PILLS`, `RELATED_ITEM`), and an `IX_STATE_CLASSES` tuple for the always-swap-as-a-group ix-* set. Migrated the multi-file shared classes — the four `ix-*` tree-row index-state markers (used in both `tree/index.ts::applyIndexMarker` and `main.ts`'s post-progress-event apply path), `drop-target` (`tree/index.ts` dnd handlers), `queue-pill` (`queueDetail/index.ts` filter pills, both the template literal and the two `querySelectorAll(".queue-pill")` callsites), and `related-item` (`discovery/index.ts` row builders + the `querySelectorAll`/`classList.contains` keyboard-nav lookups). Single-file class names (`related-item-title`/`-meta`/`-snippet`, `search-item`, `dragging`, etc.) stay inline per scope. `style.css` itself untouched — pure TS-side indirection. The bug row's claim that the classes appeared in `mutations/index.ts` and `vaultHome/index.ts` was stale; neither file uses the named classes today. `recent-row` doesn't exist in the codebase (likely a forward-looking name in the original row); skipped |
| `bug-sidebar-toggle-side-effects-fan-out` | added `ui/src/events/bus.ts` exposing typed `emit<E>(name, payload)` / `on<E>(name, cb): Unsubscribe` over an `EventMap` declaring `sidebar-toggled` (`{ open: boolean }`), `vault-opened` (`{ path: string }`), `vault-closed` (no payload), and `settings-changed` (the merged `Settings` snapshot). Pure module: no DOM, no host, no IPC; listener throws are caught and logged so one bad subscriber doesn't break siblings. Wired the sidebar / related toggle handlers in `main.ts` to `emit("sidebar-toggled", { open })` after flipping `appEl.classList` — both the manual click handlers and the programmatic `expandPanelIfCollapsed` discovery callback go through the bus. `applyOpenedVault` emits `vault-opened`. `SettingsManager.onSettingsChanged` is the canonical emitter for `settings-changed` per the bug row's pairing — host registers a listener that re-emits the merged config on the bus, so any future panel can subscribe via `on("settings-changed", ...)` without holding a direct manager reference. No production subscriber today on the bus side — every cross-module wake-up still rides direct calls in `main.ts` (`tree.notifyWatcher` from the watcher path, `discovery.setSectionExpanded` from `applySettingsToUi`, etc.) — but the seam is declared so the next slug that wants cross-panel reactivity drops in via `on(...)`. The bug row's named direct call sites (`tree.notifyWatcher`, `discovery.setSectionExpanded`, `chatPanel.setHeight`) survive in the host wiring; they're not invoked from the toggle handlers in current `main.ts` (the line numbers in the row had drifted), so the refactor was scoped to the actual toggle-handler call sites + the SettingsManager bridge |
| `bug-persist-setting-duplicated-per-module` | added `ui/src/settings/manager.ts` exposing `createSettingsManager({ flash?, logTarget? }) -> SettingsManager` with `setUserSetting(key, value)` / `setVaultSetting(key, value)` / `onSettingsChanged(cb)`. The shared `try { Ipc.setSetting(...); flash("Saved") } catch { Logger.error(...); flash(err, true) }` pattern lives once in the factory; each successful write fans out to subscribers before the flash so future cross-panel reactivity has a seam. main.ts constructs one host instance (no flash, `logTarget: "ui::app"`) and threads it into `mountTree`, `mountDiscovery`, `mountTrash` via a new `settings: SettingsManager` deps field — replacing the bespoke `persistSetting` closure each module previously took. queueDetail constructs its own local manager with the toggles-tray flash callback wired to `statusEl` so its "Saved" / "Error: ..." UX is preserved without re-implementing the try/catch shape. main.ts keeps a thin `persistSetting(scope, key, value)` shim that delegates to the manager for the host-side call sites that aren't yet on the typed surface (View menu flips, sidebar/related toggles, chat-panel resize). vaultHome was named in the row but never actually had `persistSetting` in its deps — its writes route through host callbacks — so no change there. `settings.ts` (the settings pane) intentionally left alone: its `persist` calls `onSettingApplied(cfg)` rather than just logging, which is a different shape than the bug row addresses. `onSettingsChanged` has no production subscriber today; declared so the next slug that wants cross-panel reactivity (e.g. the eventual `events/bus.ts` from `bug-sidebar-toggle-side-effects-fan-out`, which the row pairs with) has a typed seam |
| `bug-chat-couples-to-main-buffer-globals` | replaced chat panel's `getActiveNote` / `getActiveSelection` deps closures with a single `bufferApi: BufferApi` dep. `chat.ts` calls `bufferApi.getActive()` once per `chat_send` and reads both `bufferText` and `selection` off the snapshot; `hasEditorSelection` for the at-mention autocomplete reads `bufferApi.getActive()?.selection`. `main.ts` constructs the `bufferApi` (already declared per the prereq) above the `mountChatPanel` call so the deps are resolved at mount time rather than via two ad-hoc closures over `buffer`/`view`. No subscription wiring — chat polls on send today, so `BufferApi.onChanged` stays unused but available for future reactive consumers |
| `bug-main-ts-god-module` | extracted four remaining slices on top of the prerequisite `bug-ui-state-in-mutable-closures` `app/state.ts` store: `app/openFile.ts` (file open + preview-slot replacement + `promotePreviewIfActive`/`promotePreviewByPath` + the `disk_drift` save modal + the watcher-conflict-dirty modal), `app/vaultLifecycle.ts` (folder picker, `bootstrapDefaultVault`, `handleOpenVaultError`; host keeps `applyOpenedVault` since it touches every panel API), `app/keybindings.ts` (the three window-level `keydown` handlers — `Mod-,` settings global half, Ctrl-Space search global half, tab + navigation keybinds), `app/indexStatusBus.ts` (`hiker:index-status` + `hiker:reindex-progress` listeners owning the `IndexStatus` snapshot + `outstandingCount` tally; main.ts mirrors via `onStatusChanged`/`onOutstandingChanged` callbacks so `renderIndexStatus` keeps its existing local reads), `app/settingsApply.ts` (`applySettingsToUi` as a pure mapping over per-surface mutator deps). Each module exports a small mount/init function. main.ts went from 3185 to 2756 lines (~429 moved out). `activateTabInner` / `closeTab` / `cycleTab` / `jumpToTab` / `applyMutationToBuffer` left in main.ts — they're tab-strip plumbing, separate from the openFile/preview-promotion/drift surface this row called out |
| `bug-ui-state-in-mutable-closures` | added `ui/src/store/createStore.ts` (~30-line observer primitive: `get` / `set` / `update` / `subscribe`) and `ui/src/app/state.ts` exporting `bufferStore` (`{buffer, activePath, previewTabPath}`), `tabStore` (`{openBuffers, activationCounter}`), `viewSettingsStore` (the seven View-menu toggle flags), `inFlightMutationsStore`, and the `Buffer` / `BufferMode` / `OpenBufferEntry` types. Migrated main.ts's globals: bare `let buffer`, `activePath`, `previewTabPath`, view-toggle flags, and the in-flight-mutations Set are now derived caches synced via `*Store.subscribe`; every write rides through `setBufferState({...})` / `viewSettingsStore.update(...)` / `bumpActivationCounter()` / `add|removeInFlightMutationPath` so cross-module subscribers fire on every transition. Defined `BufferApi { getActive(): ActiveBufferSnapshot | null; onChanged(cb): Unsubscribe }` and constructed a host `bufferApi` in main.ts wired to `bufferStore` + the live CM6 view, ready to back the next slug (`bug-chat-couples-to-main-buffer-globals`) without further main.ts changes. Discovery / chat / tree per-module `let`-binding state intentionally left in their own modules per row guidance — main.ts's globals were where the cross-module observability problem was sharpest |
| `bug-ui-errors-not-routed-to-tracing` | lifts `obs-frontend-bridge` out of deferred. New `log_from_frontend(level, target, message, fields)` command in the host (typed `LogLevel` serde enum so the seam itself rejects garbage levels) emits a server-side `tracing::event!` at the matching level on a static `hiker::ui_bridge` target, with the runtime UI target (`ui::tree`, `ui::discovery`, etc.) carried as a `ui_target` field and `fields` flattened as a single compact-JSON `fields = ...` value. Non-`ui::*` targets are rewritten to `ui::bad_target` with a `bad_target` field rather than rejected — the bridge never becomes the reason a UI error is lost. New `ui/src/logger.ts` exposes `Logger.{error,warn,info,debug,trace}` with a typed `UiTarget` union; calls go through `Ipc.logFromFrontend(...)` and dual-write to `console.<level>` for dev-workflow parity. `Error`-shaped values in `fields` are stringified before crossing the IPC seam so the log line carries the message + stack rather than `{}`. `invokeWithLogging` in `ui/src/ipc/index.ts` now routes its own rejection log through `Logger.error("ui::ipc", "ipc command failed", { command, error })`; the `log_from_frontend` command name is special-cased to fall back to `console.error` so a broken bridge can't infinite-loop the logger. Migrated dev-console / silent-catch sites in `main.ts`, `tree/index.ts`, `discovery/index.ts`, `vaultHome/index.ts`, `queueDetail/index.ts`, and `trash/index.ts` to `Logger.error(...)`. User-visible feedback paths (`alert(...)`, `flashStatus(err, true)`, chat `appendError`, mutations `showToast`) intentionally left alone — separate concern |
| `bug-at-mention-resolution-duplicated` | extracted `ui/src/notes/resolver.ts::resolveAtNote(rel)` over `Ipc.chatResolveAtNote`. `chat.ts`'s `assembleContextBlocks` now calls the helper instead of invoking the IPC inline; future at-mention surfaces (queue-detail "show source note", etc.) get a one-import seam. Only the host call + `{relPath, content}` shaping moved into the helper — chat-side concerns (selection branch, dedupe, error wrapping) stayed at the callsite |
| `bug-ipc-responses-untyped` | added `ui/src/ipc/validators.ts` with hand-rolled `parseSearchResponse` / `parseChangeRowArray` / `parseSettingsConfig` (zod-shaped `parse*` API; throws on shape mismatch). `invokeWithLogging` grew an optional `validate` parameter; the high-blast-radius commands (`search_vault`, `recent_changes`, `get_settings` / `get_settings_scoped` / `reload_config` / `set_setting`) pass their validator. Validation throws ride the existing rejection path, so callers see the same `IpcError`-shaped failure. The remaining commands keep their TypeScript-only typed wrappers — runtime cost stays contained to the high-blast surfaces. zod intentionally not added as a dep; revisit if a fourth command needs runtime checks. Resolves the silent-destructure-of-`undefined` failure mode when a Rust DTO drifts |
| `bug-invoke-scattered-no-ipc-client` | added `ui/src/ipc/index.ts` with a single `invokeWithLogging<T>(cmd, args)` seam that logs structured fields (`command`, `error`) on rejection (TODO points at `bug-ui-errors-not-routed-to-tracing` for the eventual `Logger.error("ui::ipc", ...)` route). Typed `Ipc.*` surface exposes one method per command actually used by the UI (settings / vault lifecycle / file I/O / trash / index / search / diff / changes / vault-home / chat / mutations / task queue). Migrated every callsite — `main.ts`, `chat.ts`, `atMentions.ts`, `diff/index.ts`, `dirtyBufferDiff/index.ts`, `tree/index.ts`, `discovery/index.ts`, `vaultHome/index.ts`, `queueDetail/index.ts`, `mutations/index.ts`, `snapshotPreview/index.ts`, `trash/index.ts`, `settings.ts`. Only `ipc/index.ts` imports the IPC invoke primitive; grepping the UI for direct IPC imports returns one line. Response shapes hand-mirrored from the Rust DTOs (re-importing `ChangeRow` / `ChunkBounds` / `IndexState` / `DirEntry` / `TrashListItem` / `SettingsConfig` / `SettingsScope` from their owning panels rather than duplicating them); the runtime-validation work belongs to the sibling slug `bug-ipc-responses-untyped`, where high-blast-radius commands (`get_settings`, `search_notes`, `recent_changes`) get zod schemas and the rest get plain typed wrappers |

Fixed 2026-05-08 (cleanup pass):

| Slug | Fix |
| ---- | --- |
| `bug-frontmatter-detection-duplicated-in-ui` | extracted shared `ui/src/editor/frontmatter.ts::findFrontmatter(doc, acceptDotsClose?)` and routed both `livePreview` (with `acceptDotsClose=true` to preserve the historical `...`-closer acceptance) and `hideFrontmatter` through it. Detection rule mirrors `core::frontmatter::split`; the helper notes why a TS-side dedup beats a command here (CM6 decoration-compute is synchronous and runs on every doc change — IPC-per-keystroke would be a regression) |
| `bug-is-indexable-extension-duplicated-in-ui` | dropped the TS-side `indexableExts` allowlist + `isIndexableExt` predicate + `setIndexableExts` API + the `indexable_extensions` command. Tree row renderer + status-bar `renderIndexStatus` now defer unconditionally to `index_state_for`, which already returns `Unsupported` for paths outside `core::indexer::indexable_extensions`. Single source of truth in core |
| `bug-count-notes-recursive-walk-from-js` | new `count_notes_in(rel) -> u32` command backed by `core::vault::walk_indexable_files` (which already shares the `is_indexable_path` filter). UI `countNotesIn` collapses to one IPC call. The `.md`-only hardcoding is gone — `.markdown` and `.txt` now also count toward the delete-confirm "N notes" copy |
| `bug-author-class-string-parsing-in-ui` | added `core::changes::AuthorClass` enum (`user` / `agent` / `sync` / `import` / `other`) and an `author_class` field on `ChangeRow`, populated in `map_row` and on the `appended_tx` broadcast send. UI's `authorClass(author)` helper deleted; pill-rendering, filter-pill state, and `handleAgentChange`'s "is this an agent write?" gate all consume the typed field. UI `ChangeRow` interface mirrors the new field |

Fixed 2026-05-08 (later batch):

| Slug | Fix |
| ---- | --- |
| `bug-vault-home-inlined-in-main-ts` | extracted `ui/src/vaultHome/index.ts` exporting `mountVaultHome(deps) -> VaultHomeApi` (`isVisible`, `setVisible`, `refresh`, `showDetail`, `notifyChangesAppended`, `notifyRecentModified`, `scheduleStatsRefresh`, `doRestoreSnapshot`, `activeDetailView`). Module owns `activityRows`, `recentlyRestoredFromId`, `activeDetailView`, `activeAuthorFilters`, `allFiltersOnce`, the stats-refresh debounce, and the home/new-note button listeners. Host wires DOM ids and the `onOpenNote(rel)` / `onOpenSnapshot(row)` / `onBeforeShow` callbacks so the module never reaches into editor or snapshot-preview state directly |
| `bug-search-and-related-inlined-in-main-ts` | extracted `ui/src/discovery/index.ts` exporting `mountDiscovery(deps) -> DiscoveryApi` (`refreshRelated`, `scheduleRelatedRefresh`, `setMode`, `setSectionExpanded`, `syncToggleButtons`, `clear`, `focusInput`). Module owns search modes, search/related epoch counters, debounce timers, section-collapse state, roving-tabindex keyboard nav, and all the `<aside id="discovery">` listeners (input/keydown/clear-button/section-headers). Host wires DOM ids and the `onOpenNote` / `onScrollToChunk` / `expandPanelIfCollapsed` callbacks. Chat (in the same panel host) stays in `./chat` |
| `bug-discovery-sections-overlap-after-toggle-cycles` | fixed inside the new `./discovery` module's `applySectionCollapsed`: the helper now strips any inline `height` / `min-height` / `max-height` left behind by prior CSS animations after toggling `[hidden]` on the body, so rapid expand/collapse cycles can't stack residual sizing across sections |
| `bug-mode-controls-and-view-menu-inlined-in-main-ts` | extracted `ui/src/modeControls/index.ts` exporting `mountModeControls(deps) -> ModeControlsApi` (`register(kind, renderer)`, `render()`) plus `iconButton` + the `ICON_DIFF` / `ICON_RESTORE` / `ICON_CLOSE` constants. Each owning module registers a renderer keyed by buffer-mode kind (`snapshot` / `trash`) and the host swaps based on `buffer.mode.kind`. The View ▾ menu's button click handler also lives here, calling the host's `buildViewMenuItems` to populate via `openContextMenu`. The widget primitives (`./widgets/{contextMenu,toast,confirm}`) had already landed in the earlier batch |
| `bug-tree-inlined-in-main-ts` | extracted `ui/src/tree/index.ts` exporting `mountTree(deps) -> TreeApi` (`refresh`, `revealPath`, `setSortOrder`, `getSortOrder`, `setSelectedFolder`, `notifyWatcher`, plus index-state cache helpers `getIndexState` / `setIndexState` / `deleteIndexState` / `clearCaches` / `setIndexableExts` / `isIndexableExt` / `fetchIndexState`). Module owns expanded-folders set, sort order, watcher debounce, index-state cache, and the in-flight fetch coalescing set; host wires DOM ids (`treeEl`, `newNoteBtn`, `treeActionsBtn`) and editor-coupled callbacks (`openFile`, `getBuffer`, `setBufferPath`, `clearOpenBufferIfWithin`, `refreshTrashBin`, `renderIndexStatus`) via deps. `renderDir` / `renderTreeRowLabel` / `applyIndexMarker` / `attachDnd` / `performDrop` / `revealInTree` / `refreshTree` / `scheduleTreeRefreshFromWatcher` / `beginInlineRename` / `attachContextMenu` / `deleteFromTree` / `countNotesIn` / `sortOrderLabel` / `openSortByMenu` / new-note button / tree-actions menu / tree-root drop / empty-space context menu all moved. `main.ts` keeps wrapper function declarations (`refreshTree`, `revealInTree`, `scheduleTreeRefreshFromWatcher`) for unchanged call sites |
| `bug-snapshot-preview-inlined-in-main-ts` | introduced buffer-mode discriminated union (`mode: {kind: "file"} | {kind: "trash", displayPath} | {kind: "snapshot", row, changeId, diffActive}`) replacing the `preview` / `snapshotPreview` / `displayPath` / `snapshotChangeId` / `snapshotRow` / `diffActive` flags on `Buffer`. Extracted `ui/src/snapshotPreview/index.ts` exporting `mountSnapshotPreview(deps) -> SnapshotPreviewApi` (`open(row)`, `close()`, `toggleDiff()`, `restore()`). Module owns the diff-toggle in-flight guard; host wires CM6 view, language / live-preview / hide-frontmatter compartments, save / dirty / mode-controls / chunk-boundaries refresh callbacks, and `onClose` (return to recent-activity detail) + `onRestore` (vault-home's `doRestoreSnapshot`) hooks |
| `bug-trash-inlined-in-main-ts` | extracted `ui/src/trash/index.ts` exporting `mountTrash(deps) -> TrashApi` (`refresh`, `openPreview`, `closePreview`). Module owns `trashItems`, header click + contextmenu listeners, row context menu, `hiker:trash-changed` listener, `relativeTime` helper, all wiring for read-only trash-preview buffer (now `{kind: "trash"}` in the buffer-mode union). Host wires DOM ids (`trashBinEl` / `headerEl` / `listEl` / `chevronEl` / `labelEl`), CM6 view + language compartments, and editor-coupled callbacks (`getBuffer`, `setBuffer`, `setReadOnly`, `updateStatus`, `refreshChunkBoundaries`, `isDirty`, `save`, `refreshTree`) |

Fixed 2026-05-08:

| Slug | Fix |
| ---- | --- |
| `bug-chat-context-lost-across-user-turns` | resolved by the `chat-session-*` slugs landing in this batch. `ChatRegistry` is now session-keyed (`HashMap<SessionId, SessionState>` plus an `active` slot) in the host chat module; `chat_send` appends to the existing `SessionState.history` rather than minting a fresh per-turn entry, and nothing GCs the registry on terminal `TurnFinished`. The frontend tracks `activeSessionId` and threads it into `chat_send` / `chat_continue` / `chat_stop` / `chat_cancel`. The backend persists each terminal turn via `core::sessions::append_turn` and on vault open `resume_latest_at_open` rebuilds the in-memory cache from the most-recent on-disk markdown |
| `bug-tree-sort-in-ui` | `core::vault::list_dir(rel, sort_by)` now accepts the configured `TreeSortBy` and returns pre-sorted entries (folders-first grouping kept; chosen order applies within each group). The `list_dir` command falls back to `vault.tree.sort_by` from the loaded config when the UI doesn't pass `sort` explicitly. UI's `sortTreeEntries`/`sortComparator` deleted; `renderDir` calls `list_dir` with the active `treeSortOrder` and renders the result as-is |
| `bug-search-result-bucket-pick-in-ui` | added `hits: Vec<NoteHit>` to `SearchResponse`; `core::search::query` populates it via the new `pick_bucket(lexical_hits, semantic_hits, fused, modes)` helper (both modes → `fused`, one mode → that engine's full PER_BACKEND_TOP_K bucket, both off → empty). UI's `pickResultBucket` deleted; `runSearch` renders `resp.hits`. MCP truncates `resp.hits` alongside `resp.fused` |
| `bug-activity-refresh-polled-not-pushed` | `scheduleActivityRefresh` and `scheduleVaultHomeModifiedRefresh` debouncers gone. New `refreshOnChangesAppended` runs on every `hiker:changes-appended` event and refreshes recent-activity widget + detail + recent-modified directly. `handleAgentChange` no longer re-schedules home refreshes (upstream listener handles it). The watcher path keeps a direct refresh for external edits since those don't yet ride `core::changes` (deferred per `changes-write-path` notes) |
| `bug-index-status-polled-not-pushed` | added `IndexerHandle::subscribe_status` over the indexer's existing `watch::Sender<IndexStatus>`; the host spawns a forwarder task that emits `hiker:index-status` whenever the watch value changes (seeding the initial value at vault open). UI listens for the event and drops `pollIndexStatus` + the 2s `indexStatusInterval`; the buffer-path 250ms watcher stays (separate concern) |
| `bug-byte-to-char-conversion-in-ui` | `ChunkBounds` carries `char_start` / `char_end` (UTF-16 code units) populated by the new `core::store::enrich_char_offsets` helper; the `chunks_for` command reads the source file once via the vault and enriches the rows. UI's `byteOffsetToCharOffset` and `byteOffsetToLine` deleted — `openSearchHit` uses `target.char_start` directly and `chunkBoundsToState` calls `doc.lineAt(char_start)` |
| `bug-latest-per-path-computed-over-visible-window` | `ChangeRow` carries `is_current: bool` computed by SQL (`id = (SELECT MAX(id) FROM changes c2 WHERE c2.path = changes.path)`) over the whole table, not the visible 200-row window. UI's `buildLatestPerPath` and `latestPerPath` map deleted; row builders read `r.is_current`; `doRestoreSnapshot` finds the prior current row by scanning `activityRows` for `is_current` |
| `bug-diff-pane-collapse-into-toggle` | demoted `openDiffView` / `closeDiffView` to host-agnostic `renderDiff(view, input)` / `clearDiff(view, plainText)` over a `diffExtensions()` bundle the consumer spreads into its own CM6 view's extensions. Snapshot Diff toggle now flips the editor's CM6 view doc + decorations in place via a `Buffer.diffActive` flag. Removed `#diff-pane` DOM, `.diff-view` `#editor-pane` class, the standalone CM6 instance, and the `setDiffWordWrap` / `setDiffLineNumbers` / `setDiffWhitespace` mirrors (the editor view's own compartments now serve the diff for free). Diff toggle hidden for `op = "deleted"` snapshot rows |

Fixed 2026-05-07:

| Slug | Fix |
| ---- | --- |
| `bug-show-line-numbers-noop` | broadened the CSS selector to `.hide-line-numbers .cm-lineNumbers` and added `!important` so it wins over CM6's injected base-theme `display` on gutter elements |
| `bug-default-vault-not-persisted` | `open_vault_at` now also writes `vault.default` (user scope) when it differs from the opened root, alongside the existing `vault.recent` update |
| `bug-x-button-does-not-close` | `onCloseRequested` now always `preventDefault`s and drives the close itself via `win.destroy()`; the clean-path "let the host default-close" branch was unreliable, and `destroy()` avoids re-entering the handler that `close()` would |
| `bug-save-button-wraps-when-dirty` | added `white-space: nowrap` and a `min-width: 64px` to `#save-btn` so the dirty-state bullet glyph can't push the label onto a second line |

Fixed 2026-05-06:

| Slug | Fix |
| ---- | --- |
| `bug-save-button-double-listener` | dropped the early lambda; single click handler now lives at the related-notes refresh site |
| `bug-css-escape-incomplete` | replaced local helper with `CSS.escape()` |
| `bug-not-utf8-pins-index-error` | `process_upsert` returns `Skipped("not utf-8")` instead of `Error` |
| `bug-count-notes-swallows-errors` | seed and refresh paths surface `count_notes` failure as `last_error`; recovers when a later count succeeds |
| `bug-vault-resolve-accepts-absolute` | reject `RootDir`/`Prefix` components in `resolve` with a clear "expected vault-relative" message |
| `bug-queue-count-undercounts` | split `update_queue_count` into in-flight (`rx.len() + 1`) and idle (`rx.len()`) variants; main loop calls in-flight after recv, idle after job completes |
| `bug-progress-counter-unpaired-decrement` | unified `pendingCount + inFlightCount` into a single `outstandingCount`: `scan_complete` adds, every terminal event subtracts, `Started` is a no-op |
| `bug-vault-close-leaks-intervals` | `vaultIsOpen` flag + `startBackgroundIntervals` (called on each `openVault`) clears prior intervals and short-circuits when no vault is open |
| `bug-indexer-shutdown-broken` | `tx` wrapped in `Option<Sender>`; `shutdown` takes + drops it so the task's `recv()` returns `None` |
| `bug-watcher-suppress-ttl-too-short` | bumped `SUPPRESS_TTL` from 500ms → 2s, plus re-suppress after fs ops in `move_note` and `create_note` so the window starts close to when notify surfaces events |
| `bug-watcher-rename-pair-order-assumed` | branch on `RenameMode::From|To|Both` rather than trusting `paths[0]/paths[1]` ordering |
| `bug-watcher-unpaired-rename-as-modified` | `RenameMode::From` → `Deleted`, `RenameMode::To` → `Created`; only the genuine ambiguity case (Any/Other with one path) falls back to `Modified` |
| `bug-embedder-failure-counter-stuck` | embedder-failure drain now emits one `Error` per Upsert/Delete/Rename and replies `Err` to any pending `Move`, so the UI's outstanding counter actually reaches zero |
| `bug-pick-vault-blocks-tokio-worker` | replaced `std::sync::mpsc::recv()` with `tokio::sync::oneshot` so the file dialog suspends without parking a worker |
| `bug-vault-refuse-symlinks` | `Vault::resolve` walks each existing ancestor under the canonical root and rejects any symlink component (file *or* directory). 3 unit tests added |
| `bug-status-bar-path-overflow` | implemented `status-bar-path-basename-tooltip` (basename in `#status-path`, full path in `title=`) plus `ui-no-sibling-pushout` rule (`min-width: 0` + `flex-shrink: 1` on the status-bar regions) |
| `bug-move-note-routes-via-fresh-writer` | added `IndexJob::Move { from, to, reply }` + `IndexerHandle::move_note`; the host's move command now suppresses, sends the job via the indexer's owned writer, awaits the oneshot reply, and re-suppresses post-rename. No more parallel writer connection |
| `bug-sqlite-vec-init-transmute-fragile` | rewrote `register_vec_extension` to use rusqlite's typed `RawAutoExtension` alias and `register_auto_extension`; the transmute is still required (sqlite-vec ships an `extern "C" fn()` stub) but the destination type is now documented at the call site, and registration failures log instead of silently dropping |
| `bug-explicit-reindex-noops-on-unchanged-content` | added a `force: bool` field to `IndexJob::Upsert` / `IndexJob::FullScan`, plumbed through `process_upsert` and `run_full_scan`. The `index` command sets `force: true` so the menu's Reindex actions actually re-embed even when content_hash matches; watcher/startup paths keep `force: false` |
| `bug-related-panel-stale-on-vault-switch` | `openVault` now clears the related-notes panel via `refreshRelated(null)` on vault swap, alongside the existing `indexStateCache` / `inflightStateFetches` resets |
| `bug-too-large-errors-instead-of-skipping` | already fixed before this entry was filed: `process_upsert` at `core/src/indexer.rs:674` returns `UpsertOutcome::Skipped("file too large")` and persists the row via `Store::upsert_skipped`. `IndexerError::TooLarge` no longer exists in the enum. Bug-tracking entry was stale |
| `bug-index-status-shows-model-loading-with-no-vault` | `renderIndexStatus` now early-returns with a blank label + title when `vaultIsOpen` is false. `vaultIsOpen` was hoisted next to `indexStatus` so the early-init render path (CM6 view setup) can read it without a TDZ throw |
