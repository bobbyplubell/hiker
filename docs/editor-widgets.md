# Editor widgets

Rendered block and inline widgets in the live-preview layer: LaTeX math, Mermaid diagrams, and tables drawn in place of their markdown source. This is the Tier 3 surface `live-preview.md` defers — the decoration providers that turn `$…$`, `$$…$$`, ` ```mermaid ` fences, and pipe tables into rasterized widgets rather than tinted source. Builds on the editor crate's widget primitives (`editor/SPEC.md` §3.4–3.5, §9) and the egui-agnostic renderers in `hiker-render`.


## Pipeline

Source span → renderer → SVG → RGBA pixels → egui texture → blit. The work splits across three crates so each stays in its lane:

1. **`editor-md` (detect).** Each kind's detector walks the markdown parse (same tree the mark-only `equations.rs` / `diagrams.rs` / `embeds.rs` use) and yields source spans + kind. The `Decoration::InlineWidget` / `BlockWidget` carrying a trait object is constructed in `app` (rendering is app's job), so `editor-md` stays renderer-unaware.
2. **`app` (render + rasterize).** Calls `hiker_math` / `hiker_mermaid` for the SVG + metrics (+ interaction regions), then rasterizes the SVG to straight **RGBA pixels** via `resvg` (the same path `hiker-render/htmlview` uses; tiny-skia's premultiplied output is un-premultiplied) at size × dpr. The rasterizer loads a shared `fontdb` (system fonts + the bundled `hiker_mermaid` sans-serif) — without it `resvg` renders no glyphs for Mermaid's `<text>` labels, so the diagram draws but every label is blank. Returns the rgba + dimensions + baseline + `content_hash` (+ normalized `DiagramRegion`s for interactive diagrams).
3. **`editor-egui` (upload + cache + blit).** The `InlineWidget` / `BlockWidget` traits expose `pixels()` + `baseline()`; the painter uploads to an egui texture cached by `(widget_id, width, height)` and blits into the reserved rect (mirroring the minimap widget). `widget_id()` is the cache key — app sets it to the render's `content_hash`.

**The egui-free boundary is load-bearing.** `editor-core` has no egui dependency and must keep none, so the `InlineWidget` / `BlockWidget` traits cannot return an egui texture handle. They expose **raw RGBA bytes + dimensions + a content hash** (egui-free); `editor-egui` owns the GPU-texture upload and cache. `app` owns the SVG render (so `hiker-render` deps stay out of the editor crates); `editor-md` only reports spans and keeps the mark-only fallbacks. This split is why no single crate gains a dependency it shouldn't. App-side, the widget types, render helper, providers, and live-edit popup live in `buffer/widgets/`; the decoration-assembly pipeline that invokes the providers lives in the sibling `buffer/decorations.rs`. [widget-render-pipeline, widget-render-module-split]


## Decoration shapes

| Kind | Source | Decoration | Placement |
|---|---|---|---|
| Inline math | `$…$` | `InlineWidget` (atomic) | Rendered in place when its line is inactive; collapses to source with a popup preview while edited (see Cursor reveal). Baseline-aligned via the render's baseline metric |
| Display math | `$$…$$` | `BlockWidget` | Full-width own-height row |
| Mermaid | ` ```mermaid ` fence | `BlockWidget` | Full-width own-height row |
| WaveDrom | ` ```wavedrom ` fence | `BlockWidget` | Full-width own-height row |
| Table | pipe table block | `BlockWidget` | Full-width own-height row, native paint |

Inline math is the only inline widget — `InlineWidget::measure(font_size) → (w, h)` reserves a region among the glyphs and the cursor steps over it atomically (`editor/SPEC.md` §3.4, `editor-core` `InlineWidget`). The render's baseline metric (distance from box top to baseline) is what lets `$x^2$` sit on the surrounding text's baseline rather than floating. Inline math taller than the text line (a fraction, a large operator, a `\sum` with limits) grows that visual line's height to its measured height — the editor's line layout takes `max(text height, tallest inline widget height)` per line and baseline-aligns the widget to the text baseline. Everything else is a `BlockWidget` whose `measure(font_size, width) → height` supplies its own row height (§3.5) and participates in scroll + vertical layout. [widget-inline-math-baseline, widget-block-view-zone]

A block widget renders in place of its source: the provider hides the source lines (`LineStyle.hide`, the same mechanism folds use) and attaches a `BlockWidget` for the rendered output. On cursor-in the hide lifts and the widget is suppressed (see reveal below), so the raw markdown returns for editing. [widget-block-source-hide]
status:: done
touches:: [[code:hiker/panels/buffer/widgets]]
note:: display math + mermaid hide every source line (`LineStyle.hide`) and anchor a `BlockWidget` (`BlockSide::Above`) to the span's first line, so the rendered output paints where the source was; cursor-in lifts the hide (reveal). The hidden source is reachable by clicking the rendered block — see [[spec:widget-block-click-to-edit]] · evidence: `app/src/panels/buffer/widgets/placement.rs` (`emit_block_widget`)

**Widget paint.** The traits expose `pixels()` plus `baseline()` (inline) and `click_regions()` (interactive sub-regions); the `editor-egui` painter uploads to an egui texture cached by `(widget_id, width, height)` and blits into the reserved rect, reusing the minimap's offscreen-texture pattern. The `InlineWidget::display()` text path ([[spec:editor-inline-widget-display]]) still serves textual widgets. [widget-painter-texture-blit]
status:: done
touches:: [[code:hiker/decoration]], [[code:hiker/widget/texture_cache]]
note:: egui-free `WidgetPixels` accessor on both widget traits (raw RGBA + dims; `widget_id` is the content-hash cache key, `baseline()` for inline math). `TextureCache` on the host `PaintCache` keyed by `(widget_id, width, height)`, uploaded on miss via `ctx.load_texture`, reused on hit, evicted per-frame for unblitted entries. Block path in `widget/blocks.rs`, inline path in `widget.rs` (baseline-aligned); both fall back to the placeholder when `pixels()` is `None`/malformed. [[spec:editor-inline-widget-display]] text path unchanged. BUGFIX: the block texture blit drew through the unclipped `ui.painter()` and letterboxed into the full row rect (gutter included), so a tall diagram bled over the toolbar and a wide one into the line-number gutter. Now `TextureCache::blit` draws through the caller's body-clipped `&Painter` and `widget/blocks.rs` letterboxes into the CONTENT box (`text_origin_x … right`); per-region click zones map through that same content-box transform. Tests: `block_widget_click_regions::emits_region_zones_plus_whole_widget_zone` asserts the painted box clears the gutter · evidence: `editor/editor-egui/src/widget/texture_cache.rs` (`TextureCache`); `editor/editor-core/src/decoration.rs` (`WidgetPixels` + `pixels()`/`baseline()`)

**Fit to the text column, clip to the editor body.** A block widget letterboxes (aspect-preserving) into the **content box** — the prose column (`text_origin_x … right`), gutter excluded — not the full row rect, and the blit draws through the editor's body-clipped painter (`TextureCache::blit` takes the caller's clipped `&Painter`, not the unclipped `ui.painter()`). So a diagram wider than the column scales down to fit instead of bleeding into the line-number gutter, and a tall one scrolled to the top is clipped to the editor body instead of painting over the toolbar. To match the paint, `BlockWidget::measure(font_size, width)` is handed the *content* width (full width minus `content_origin_x`) and the diagram widgets (display math, Mermaid, WaveDrom) return a width-scaled own-height (`fit_block_height`: shrink a too-wide diagram and its height together; never upscale a narrow one) so the reserved row matches the letterboxed render with no empty vertical band. Interactive `click_regions` map through the same content-box letterbox, so a diagram node stays clickable where it's drawn. [widget-painter-texture-blit, widget-block-view-zone]

A `BlockWidget` may instead supply an egui-free **retained paint list** (`paint_list`) — plain geometry+style data (filled rects, lines, positioned text runs) — which the painter replays natively with no texture. Used by tables (see Tables). The painter prefers the paint list when present, else blits the texture, else draws the placeholder; a widget supplies one or the other. The hook is generic — any structured native-painted block reuses it. [widget-block-native-paint]
status:: done
touches:: [[code:hiker/decoration]], [[code:hiker/widget/blocks]]
note:: egui-free retained paint-list hook: `BlockPaint::{Rect, Line, Text}` (plain f32 geometry in logical points, egui-free `Color`, `TextAlign` for runs) returned from `BlockWidget::paint_list(font_size,width)`. The `editor-egui` block painter prefers the list (replays it primitive-by-primitive into the reserved rect, translating point coords to screen, font-laid text via galley) over the `pixels()` texture, over the placeholder — no texture-cache round-trip. Updated the now-current `Decoration::BlockWidget` doc (painting hook no longer deferred). Generic; first consumer is tables. The native path also emits per-region click zones ([[spec:editor-widget-click-regions]]) — mapping each `click_regions` rect linearly into the painted content box — so table cells are individually clickable ([[spec:widget-table-cell-edit]]). Tests assert the list wins over `pixels()`, the whole-widget click zone fires, and per-region zones fire on the native path · evidence: `editor/editor-core/src/decoration.rs` (`BlockPaint` enum + `TextAlign` + `BlockWidget::paint_list`), `editor/editor-egui/src/widget/blocks.rs` (`paint_native` replay), `editor/editor-egui/tests/block_widget_native_paint.rs`


## Cursor reveal

All widgets collapse to source for editing, and a floating popup previews the rendered result while you edit — one model for every kind, built on live preview's per-block trigger (`live-preview.md`):

- **Inline math.** Cursor off the line (no selection overlap): the `$…$` source is replaced in place by the rendered formula (inline atomic widget). Cursor on the line or selection overlap: the source shows as editable styled text in place (the current `equations.rs` mark style). [widget-reveal-inline]
status:: done
touches:: [[code:hiker/panels/buffer/widgets]]
note:: inline math: cursor on the span's line OR a selection overlap → emit nothing (the `math_decorations` source mark shows); else → `InlineWidget` · evidence: `app/src/panels/buffer/widgets/placement.rs` (`line_active` / `selection_overlaps`)
- **Block widgets (display math, Mermaid, WaveDrom, tables).** Cursor anywhere inside the block's source span (delimiters inclusive) or a selection overlap: the source lines reappear (`LineStyle.hide` lifts) and the `BlockWidget` is suppressed; outside, the source hides and the widget renders. Per-block, same trigger as fenced code ([[spec:live-preview-code-fence-block-reveal]]). [widget-reveal-block]
status:: done
touches:: [[code:hiker/panels/buffer/widgets]]
note:: display math: cursor anywhere inside the span (delimiters inclusive) OR a selection overlap → show source (no hide); else → hide + `BlockWidget`. Multi-cursor handled (all ranges) · evidence: `app/src/panels/buffer/widgets/placement.rs` (`cursor_inside` / `selection_overlaps`)
- **Highlighting a block reveals its delimiters.** A selection overlapping a fenced block expands the *whole* source — fence delimiter lines (` ```mermaid ` / ` ```wavedrom ` and the closing ` ``` `) included — not just the body. This is the [[spec:live-preview-selection-reveal-all]] / [[spec:live-preview-code-fence-block-reveal]] rule applied to the styling layer: the fence-marker reveal in `editor-md`'s `style_fenced_code_block` keys on cursor-line **OR** selection-overlap, so selecting the block (caret head landing past the closing fence) no longer collapses the ` ``` ` markers. Without this the widget layer revealed the diagram body while the styling layer still hid the fence lines — a visibly broken half-reveal.
- **Escape dismisses the popup, editing continues.** Pressing Escape while a live edit-preview popup is up hides it without leaving edit mode — the caret and selection are untouched and the source stays revealed for editing. The popup stays hidden until the caret leaves that widget and re-enters it (or moves to a different widget); it is *not* a document-wide suppression (the `Live edit preview` toggle owns that). Mechanism: the popup cache records the dismissed span's anchor (`inner_range.start`); the overlay paints nothing while the active span's anchor matches, and re-arms when the active span clears or changes. The buffer panel consumes Escape **only** when a popup is actually showing — otherwise Escape reaches the editor unchanged (clears selection). One model for inline math, display math, Mermaid, and WaveDrom. [widget-edit-popup-dismiss]
status:: done
implements:: [[code:hiker/panels/buffer/widgets/edit_preview/impl#[Cache]dismiss]], [[code:hiker/panels/buffer/widgets/edit_preview/dismissible_anchor]]
verifies:: [[code:hiker/panels/buffer/widgets/edit_preview/tests/dismissible_anchor_reports_then_suppresses]], [[code:hiker/panels/buffer/widgets/edit_preview/tests/dismissed_popup_stays_hidden_then_rearms]]
note:: Escape hides the live edit-preview popup without leaving edit mode — caret + selection untouched, source stays revealed for editing. The cache records the dismissed span's anchor (`inner_range.start`); `show` paints nothing while the active span's anchor matches and re-arms (clears the anchor) when the active span clears or changes, so leaving the widget and re-entering re-shows it. The buffer panel consumes Escape ONLY when a popup is up (`dismissible_anchor` returns `Some`) — otherwise Escape reaches the editor unchanged (clears selection); a second Escape (already dismissed) also falls through. Not a document-wide suppression (the `Live edit preview` toggle owns that). One model for inline/display math, Mermaid, WaveDrom. Tests: `dismissible_anchor_reports_then_suppresses`, `dismissed_popup_stays_hidden_then_rearms` · evidence: `app/src/panels/buffer/widgets/edit_preview.rs` (`Cache::dismissed_anchor` / `dismiss` / `dismissible_anchor`), `app/src/panels/buffer/mod.rs::show` (Escape handler)
- **Live popup preview while editing.** While the main cursor is inside a widget's source span, a floating preview of the rendered widget shows in a non-focus-stealing, non-interactive egui `Area` (foreground/tooltip layer) anchored just below the span's last line — scroll-correct (the anchor is recomputed each frame from `view.line_top_y(line) + editor_rect`, the same technique the wikilink card uses), nudged to stay on-screen, and *not* covering the line being typed. It does not shift the text (no below-line block). One popup at a time — the span containing the main caret (for per-line inline reveal, the nearest span on the caret's line). It reuses the render's `content_hash` so a static span isn't re-rasterized each frame, and dismisses the moment no span is revealed. Reveal detection scans a window *around the caret*, not the tight viewport — otherwise revealing a span shifts layout/scroll a few px, the span's delimiter scrolls out of the visible-line range, and the popup blinks off/on; anchoring detection to the caret keeps it steady. Gated by its own `Live edit preview` toggle ([[spec:widget-edit-popup-preview]]), separate from `Render widgets`. You edit source, you watch the render — without a WYSIWYG canvas. [widget-edit-popup-preview]
status:: done
implements:: [[code:hiker/panels/buffer/widgets/edit_preview/show]], [[code:hiker/panels/buffer/widgets/ActivePreviewSpan]], [[code:hiker/panels/buffer/widgets/active_preview_span]], [[code:hiker/panels/buffer/impl#[`BufCtx<'a>`]show_editor]], [[code:hiker/state/INDEXER_EVENTS_MAX]]
verifies:: [[code:hiker/panels/buffer/widgets/edit_preview/tests/gated_off_clears_cache_and_paints_nothing]], [[code:hiker/panels/buffer/widgets/tests/inline_revealed_emits_no_in_place_widget]], [[code:hiker/panels/buffer/widgets/tests/display_widget_suppressed_when_cursor_inside]], [[code:hiker/panels/buffer/widgets/tests/mermaid_revealed_emits_no_in_place_block]], [[code:hiker/panels/buffer/widgets/tests/mermaid_source_shown_when_cursor_inside]], [[code:hiker/panels/buffer/widgets/tests/active_preview_span_steady_off_screen_caret]], [[code:hiker/panels/buffer/widgets/tests/active_preview_span_inline_math_on_cursor_line]]
touches:: [[code:hiker/buffer]]
note:: floating non-interactive egui `Area` (Tooltip order) previews the revealed math/mermaid span live, anchored below the span via `view.line_top_y` + `editor_rect` (scroll-correct, no editor-egui change); one popup at a time; texture cached on the render `content_hash` so a static span doesn't re-raster; dismisses purely on cursor-out. Anti-flicker: `active_preview_span` scopes its scan to a window *around the caret* (`caret_scan_window`, `PREVIEW_SCAN_MARGIN_LINES`), NOT the scroll viewport — the reveal is a pure function of the caret, so a span parked under the caret stays found even when revealing it shifts layout/scroll a few px (the old viewport-scoped scan flipped Some/None → blink). Separate `Live edit preview` View-menu toggle (default on, in-memory per-buffer) folds into the popup's `gated = render_widgets && live_edit_preview && is_markdown` — turning it off suppresses only the popup; in-place widget rendering + region interactivity stay on. Replaces the rejected below-line `BlockWidget` preview · evidence: `app/src/panels/buffer/widgets/edit_preview.rs`; selector `app/src/panels/buffer/widgets/mod.rs::active_preview_span`; wired in `mod.rs::show_editor`; toggle in `toolbar_menus.rs` (`Live edit preview`) + `app/src/buffer.rs` (`Buffer::live_edit_preview`)
- **Multi-cursor / selection.** Union of all selection lines, same as [[spec:live-preview-selection-reveal-all]]. A selection overlapping any part of a widget's span reveals (and previews) it.


## Caching and invalidation

Two caches, owned by different layers, keyed independently so a change invalidates the minimum:

- **SVG render cache (app, the `CachedDeco` layer).** Keyed on `(source_text, kind, style, font_px, theme_token_hash)`. The decoration providers already fingerprint on `(path, selection, folds, viewport, theme)` per `design.md`; the widget layer adds per-span source + font_px to that key. A re-render fires when the user edits the span, the editor font size / zoom changes ([[spec:editor-zoom]]), or the theme flips (colors are baked into the SVG by the renderer). [widget-render-cache]
status:: done
touches:: [[code:hiker/buffer]], [[code:hiker/panels/buffer]]
note:: dedicated `math_widget` cache slot; fingerprint mixes doc id, full multi-cursor selection (`sel_fp`), viewport, font size, dpr, and theme fg (colors baked into the render). Per-keystroke re-render of on-screen spans only · evidence: `app/src/buffer.rs` (`math_widget` slot), `app/src/panels/buffer/mod.rs` (`render_fp`)
- **Texture cache (editor widget).** Keyed on `(svg_hash, target_size, device_pixel_ratio)` per `editor-widget-texture-cache`. A re-raster fires when the SVG changes, the widget is resized (soft-wrap width change), or the window moves to a different-DPR monitor. The editor asks the host for a redraw only when geometry changes (`editor-widget-redraw-on-geometry`).

The split means a theme flip re-renders SVG (new colors) and therefore re-rasters; a pure DPR change re-rasters only; scrolling does neither. Viewport scoping from the existing providers still applies — only spans within the rendered viewport range are rendered/rasterized, so a document with hundreds of equations pays only for what's on screen. [widget-render-viewport-scoped]
status:: done
touches:: [[code:hiker/panels/buffer/widgets]]
note:: only spans within the viewport are scanned + rendered; viewport rides the cache key · evidence: `app/src/panels/buffer/widgets/mod.rs` (passes `visible_range` to `math_spans`)

- **Persisted disk cache (app, below both in-memory layers).** Both caches above are in-memory and per-session, so the *first* open of a note in a session always misses and pays the full `resvg` blit. A third layer persists each rasterized diagram to disk so that cost survives across sessions: keyed by the **same `content_hash`** the render computes **plus a per-domain renderer revision** (the hash folds in source/style/theme but not the rendering *code*; the revision is bumped when a renderer's output changes for identical input, so a renderer fix never serves pre-fix pixels), it stores the RGBA8 output under `<vault>/.hiker/diagram-cache/` plus, for inline math only, a tiny `.base` sidecar carrying the baseline metric (width/height are self-described by the PNG). The disk cache is checked **before** the `resvg` blit and written back on a miss; for Mermaid the parse/layout still runs (it yields the interaction regions) but the blit is skipped. It sits *below* the in-memory caches — it never changes their behavior, only repopulates a render that would otherwise be recomputed. `.hiker/` holds only regenerable data (`design.md` [[spec:subsystem-notes-visible]]), so the cache is safe to delete at any time, and is kept bounded by a best-effort byte-budget LRU sweep (oldest-by-mtime) run once per session; the same sweep first purges entries written under an outdated renderer revision. Gated by `[render] cache_diagrams` (default on, vault scope, `settings.md`); when off the in-memory caches carry the session. Tables paint natively (no `resvg` raster — [[spec:widget-table-render]]), so there is nothing to persist for them. [widget-render-disk-cache, render-cache-diagrams-toggle]

**Theme-reactive color.** The renderers take foreground/axis colors from `MathOptions` / the Mermaid draw options; hiker passes the active theme's tokens (the same `theme.palette.accent` / `theme.markdown.code_bg` the current mark-only providers read), so light/dark both render correct contrast without a parallel stylesheet. [widget-render-theme-color]
status:: done
touches:: [[code:hiker/panels/buffer/widgets]]
note:: math fg comes from `theme.palette.fg` (straight RGBA), threaded into `render_math`'s `fg`; theme is part of the render fingerprint so a flip re-renders · evidence: `app/src/panels/buffer/widgets/mod.rs` (`theme_fg`)


## LaTeX math

First to land. Inline `$…$` and display `$$…$$`, via an `app` helper that wraps `hiker_math` with font size, color, style, and preamble options:

- **Style.** `$…$` → inline (compact, baseline-aligned `InlineWidget`); `$$…$$` → display (full size, centered, `BlockWidget`). [widget-latex-inline, widget-latex-display]
- **Font size.** Tracks the editor's body font size (and [[spec:editor-zoom]] when it lands) so math scales with the prose around it.
- **Color.** Set from the active theme's text token so math contrasts correctly in light/dark.
- **Preamble.** A per-vault macro file (`\newcommand` / `\def`) threads through the helper's preamble argument (empty by default); the file path + loader are deferred (see Deferred).
- **No external runtime.** `hiker-render/math` is pure Rust (pulldown-latex parser + a TeXbook-Appendix-G layout over an OpenType MATH font). No KaTeX, no MathJax, no JS — consistent with the project's native-Rust posture. [widget-latex-native]
status:: done
touches:: [[code:hiker/panels/buffer/widgets/render]]
note:: pure-Rust math, no KaTeX/MathJax/JS; in use · evidence: `hiker-render/math` (pulldown-latex + Appendix-G layout over OpenType MATH font), wired via `app/src/panels/buffer/widgets/render.rs`

Detection already exists in `equations.rs` (block + inline scan, viewport-scoped); this feature swaps its mark-only output for the render path while keeping the reveal-time mark style as the cursor-in source view.


## Mermaid

Second, now landed. ` ```mermaid ` fenced blocks via `hiker_mermaid::render(src, &MermaidOptions) → Result<MermaidRender, MermaidError>` (SVG out), rasterized to a `BlockWidget` in place of the fence. `render` is the dispatcher — it sniffs the diagram header (honoring `---` frontmatter / `%%{init}%%` config for theme / `Look` / font) and routes to the per-type renderer. The crate covers a broad and growing set (flowchart, sequence, class, state, ER, gantt, gitgraph, pie, …) — several via `hiker-graph` layout, so new diagram types light up through the same provider with no app change. A type `render` can't parse returns `Err`, which the provider falls back to tinted source per [[spec:widget-render-error-fallback]]. `MermaidOptions` carries font size, a classic/hand-drawn look, and theme colors, wired to the active editor theme. `mermaid_spans` (in `diagrams.rs`) feeds the provider; the `MermaidWidget` lives in `app/src/panels/buffer/widgets/`. [widget-mermaid-render]
status:: done
touches:: [[code:hiker/panels/buffer/widgets]]
note:: `mermaid_spans` reports `MermaidSpan { byte_range, inner_range }` from a shared fence scan; app provider renders each fence via `render_mermaid` (theme→`MermaidColors`), emits `hide` lines + a `BlockWidget` when the cursor is outside, shows source when the cursor is inside the fence (or a selection overlaps). Unparseable diagram → `render` returns `None` → no widget, `mermaid_decorations` tint is the fallback. Cached on the shared `render_fp` via the new `mermaid_widget` slot. Clicking a rendered diagram body places the caret in the hidden source to edit it ([[spec:widget-block-click-to-edit]]) · evidence: `editor/editor-md/src/diagrams.rs` (`mermaid_spans` detect), `app/src/panels/buffer/widgets/mod.rs` (`mermaid_widget_decorations` emit + `MermaidWidget`), `render.rs` (`render_mermaid`)


## WaveDrom

` ```wavedrom ` fenced blocks (WaveJSON timing waveforms + bitfield/register diagrams) via `hiker_wavedrom::render(src, &WaveDromOptions) → Result<WaveDromRender, WaveDromError>` (SVG out), rasterized to a `BlockWidget` in place of the fence — the same SVG → RGBA → texture path as Mermaid. `render` auto-detects the WaveJSON family (`{signal:[…]}` → timing, `{reg:[…]}` / bare array → bitfield). `WaveDromOptions` carries `font_size_px`, foreground/background colors, and the categorical series palette; hiker threads the active theme's text token as the foreground and a transparent background (the diagram sits on the editor surface), keeping WaveDrom's default skin for the series palette (purpose-chosen, theme-neutral). A body that fails to parse (`WaveDromError::{Parse, Empty, Unsupported}`) returns `Err`, which the provider falls back to tinted source per [[spec:widget-render-error-fallback]]. `wavedrom_spans` (in `diagrams.rs`, sharing Mermaid's language-parameterized fence scan) feeds the provider; the `WaveDromWidget` lives in `app/src/panels/buffer/widgets/`.

WaveDrom mirrors **display math** rather than Mermaid for interaction: it is a body-clickable block widget (click the rendered waveform → caret into the hidden source, [[spec:widget-block-click-to-edit]]) with the live edit-preview popup, but it carries **no** interactive regions — WaveJSON has no `click` / link model, so there are no per-node hit regions, registry, or hover tooltips. The same `render_widgets` / `Live edit preview` toggles and markdown gating apply. [widget-wavedrom-render]
status:: done
implements:: [[code:hiker/panels/buffer/widgets/render/wavedrom_content_hash]], [[code:hiker/panels/buffer/widgets/WaveDromWidget]]
verifies:: [[code:hiker/panels/buffer/widgets/tests/wavedrom_block_emitted_when_cursor_elsewhere]]
touches:: [[code:hiker/buffer]]
note:: ` ```wavedrom ` WaveJSON fences render in place via `hiker_wavedrom::render` (SVG→RGBA, same path as Mermaid), theme→`WaveDromColors` (fg = text token, transparent bg, default series palette). Mirrors **display math**, not Mermaid: a body-clickable `BlockWidget` with the live edit-preview popup and click-into-edit ([[spec:widget-block-click-to-edit]] via `widget_edit_targets`), but NO interactive regions / registry / hover (WaveJSON has no `click`/link model). `Err` (parse/empty/unsupported) → no widget, `wavedrom_decorations` tint is the fallback ([[spec:widget-render-error-fallback]]). Same `render_fp`, `render_widgets && is_markdown` gate, viewport scoping. Tests in `diagrams.rs` (detect), `render.rs` (render/hash), `widgets/mod.rs` (reveal/edit-target) · evidence: `editor/editor-md/src/diagrams.rs` (`wavedrom_spans` + `wavedrom_decorations`, sharing Mermaid's lang-parameterized fence scan), `app/src/panels/buffer/widgets/render.rs` (`render_wavedrom` + `WaveDromColors` + `wavedrom_content_hash`), `widgets/mod.rs` (`WaveDromWidget` + `wavedrom_widget_decorations` + `theme_wavedrom_colors` + `PreviewKind::WaveDrom`), `edit_preview.rs` (preview render), `decorations.rs` (`wavedrom` / `wavedrom_widget` cached slots), `app/src/buffer.rs` (cache slots), `app/Cargo.toml` (`hiker-wavedrom`)


## Interactivity (links + tooltips)

Rendered diagrams aren't static images — flowchart and class diagrams carry per-node hit regions, so a mermaid `click` directive becomes a real clickable node.

- **Regions from the renderer.** `hiker_mermaid` returns hit regions (id, viewBox-px rect, link, tooltip) alongside the SVG for flowchart/class diagrams; other types return none (still render). The app normalizes each to a `DiagramRegion` with 0..1 fractional coords keyed off the render dimensions; the diagram `callback` is dropped (no JS engine). [widget-mermaid-links]
status:: done
implements:: [[code:hiker/os_open/open_external_url]], [[code:hiker/panels/buffer/diagram_nav/open_zim]], [[code:hiker/panels/buffer/widgets/render/DiagramRegion]], [[code:hiker/panels/buffer/widgets/render/mermaid_regions]], [[code:hiker/panels/buffer/widgets/MERMAID_REGION_TAG]], [[code:hiker/panels/buffer/widgets/DiagramLink]], [[code:hiker/panels/buffer/widgets/DiagramRegionRegistry]], [[code:hiker/panels/zim/resolve_archive_path]]
verifies:: [[code:hiker/panels/buffer/widgets/render/tests/mermaid_with_regions_carries_link_and_tooltip]], [[code:hiker/panels/buffer/widgets/render/tests/raster_free_regions_match_rasterizing_path]], [[code:hiker/panels/buffer/widgets/tests/mermaid_click_region_and_registry_agree]]
touches:: [[code:hiker/os_open]], [[code:hiker/panels/buffer]]
note:: a mermaid `click X "…"` directive becomes an interactive region: `render_mermaid_with_regions` (via `hiker_mermaid::render_with_regions`) returns normalized `DiagramRegion`s; `MermaidWidget::click_regions()` emits a `WidgetClickRegion` per linked region with a content-hash+index id under `MERMAID_REGION_TAG` (distinct from the wikilink tag); the panel rebuilds a per-frame `DiagramRegionRegistry` (genuinely raster-free via `render.rs::mermaid_regions` — parse/layout + region derivation, no resvg blit, split out of `render_mermaid_with_regions` and sharing its `content_hash`; the registry no longer rasterizes every frame) and drains tagged clicks into `dispatch_link(app, &LinkTarget)` — the single `LinkTarget`→action mapping: `External`→OS opener (`open_external_url`), `Zim`→`resolve_archive_path`+`zim::open_at_article`, `VaultPath`/`Wikilink`→`core::url::resolve_path`+`open_file` (unresolved wikilink → create) · evidence: `app/src/panels/buffer/widgets/render.rs` (`render_mermaid_with_regions` + `DiagramRegion`), `widgets/mod.rs` (`MermaidWidget::click_regions`, `MERMAID_REGION_TAG`, `DiagramRegionRegistry`, `mermaid_link_registry`), `app/src/panels/buffer/diagram_nav.rs` (`dispatch_link` + `handle_clicks`), `mod.rs` (`build_diagram_interaction` + click drain), `extract.rs::open_external_url`, `zim.rs::resolve_archive_path`
- **Regions become editor click-zones.** `BlockWidget::click_regions(font_size, width)` returns egui-free 0..1 regions with ids; the painter emits a `ClickZone { action: WidgetClick(id) }` per region plus the whole-widget fallback zone — the same multi-zone mechanism patch-review's per-hunk buttons use. Regions fire on **both** paint paths: on the texture path the painter maps each through the same letterbox transform as the blit; on the native path ([[spec:widget-block-native-paint]]) it maps them linearly into the painted content box (no letterbox). The `font_size`/`width` args are the layout inputs (same as `measure`/`paint_list`) so a widget whose regions depend on its laid-out geometry — a **table**, whose per-cell rects depend on column widths at the paint-time width — computes them; resolution-independent regions (a diagram's normalized hit-boxes) ignore the args. Region ids carry a dedicated tag bit (distinct from wikilink's and, for tables, from mermaid's), mixed from the widget content hash + region index; mermaid keeps a per-buffer `id → { link, tooltip }` registry rebuilt each frame (raster-free), while table cells map `id → caret offset` through the shared edit-target map ([[spec:widget-table-cell-edit]]). [editor-widget-click-regions]
status:: done
touches:: [[code:hiker/decoration]], [[code:hiker/widget/blocks]], [[code:hiker/widget/texture_cache]]
note:: A `BlockWidget` exposes clickable sub-regions in NORMALIZED widget coords (`{x,y,w,h}` fractions 0..1 + host `id`); egui-free f32s, `editor-core` stays egui-free. `click_regions(font_size, width)` takes the same layout inputs as `measure`/`paint_list` so layout-dependent widgets (table cells) can compute their rects; resolution-independent widgets (mermaid) ignore them. Regions fire on **both** paint paths: the texture path maps each through the SAME `texture_cache::letterbox` transform as the blit; the native path ([[spec:widget-block-native-paint]]) maps them linearly into the painted content box (origin `text_origin_x`, no letterbox). Either path pushes a `WidgetClick(region.id)` zone per region plus the whole-widget `WidgetClick(widget_id)` zone (regions first, whole-widget last). Default empty → unchanged single-zone behavior; requires `handles_click()`. Host maps the region id back to an action; mirrors the per-button `ActionRow` precedent · evidence: `editor/editor-core/src/decoration.rs` (`WidgetClickRegion` + `BlockWidget::click_regions(font_size,width)`), `editor/editor-egui/src/widget/blocks.rs` (per-region zones, both paths), `editor/editor-egui/src/widget/texture_cache.rs` (`letterbox` shared), `editor/editor-egui/tests/block_widget_click_regions.rs` + `block_widget_native_paint.rs`
- **Links resolve through one place.** A clicked region's link string goes through `core::url::classify → LinkTarget` and the app's single `diagram_nav::dispatch_link`: `External` (http(s)/mailto) → OS opener; `Zim { archive, article }` → resolve the archive path and open a ZimView tab; `VaultPath` / `Wikilink` → resolve via the index and open the note (unresolved wikilink → create-and-open). `core::url` is the unified link classifier — pure, store-free, shared by the diagram path and (incrementally) the existing wikilink / zim / external-open handlers so every link string in hiker classifies the same way. [url-classify]
status:: partial
touches:: [[code:hiker/panels/zim]], [[code:hiker/url]]
note:: pure, store-free, UI-free link-shape classifier — the single source of truth for "what kind of target is this link string?". Distinguishes `External` (`http(s)`/`mailto:`), `Zim { archive, article }` (mirrors `zim.rs`'s `zim://<authority>/<rest>` shape), `VaultPath` (slash- or known-extension-shaped), and `Wikilink` (`[[…]]` or bare name, alias/`#section` stripped). Precedence: brackets → external scheme → `zim://` → path shape → bare name. Optional `resolve_path` helper takes the existence/name lookups as closures so the module stays store-free. `app/src/panels/zim.rs::resolve_href` routes its external-scheme bail-out through `classify`; the interactive-diagram click path ([[spec:widget-mermaid-links]]) consumes both `classify` and `resolve_path` via `diagram_nav::dispatch_link`. Remaining: route the wikilink pill click path through it too (still on `wikilink::resolve_path` directly) · evidence: `core/src/url.rs` (`classify`, `LinkTarget`, `resolve_path`)
- **Hover tooltips.** Hovering a region whose registry entry has a `tooltip` shows an egui tooltip at the pointer — host-side hit-testing of the region click-zones against the pointer, mirroring the wikilink hover card. [widget-diagram-hover-tooltip]
status:: done
touches:: [[code:hiker/panels/buffer/diagram_nav]]
note:: hovering a diagram region whose registry entry carries a `tooltip` shows an egui tooltip at the pointer; mirrors `wikilink_nav`'s hit-test (translate widget-local zone rects to screen, test the pointer). Tooltip-only regions (no link) are surfaced for hover but emit no click zone · evidence: `app/src/panels/buffer/diagram_nav.rs` (`track_hover`), `mod.rs` (diagram-zone snapshot)


## Tables

Different in mechanism from math/mermaid: pipe tables render as a **natively-painted** `BlockWidget`, not an SVG round-trip. A table is structured layout (cells, alignment, borders) that egui draws directly far crisper than rasterizing an SVG of it, and re-lays-out for free on zoom / DPR / soft-wrap-width change with no re-raster. There is deliberately no `hiker-render` table engine.

This is the one widget kind that paints natively rather than through `pixels()`, so it builds the `BlockWidget` native-paint hook (`paint_list`, see Widget paint): the `TableWidget` returns an egui-free retained paint list — filled rects for header/cell backgrounds and borders, lines for grid rules, positioned text runs honoring each column's alignment — that the `editor-egui` painter replays with its own painter. `editor-core` stays egui-free (it describes *what* to draw, not *how*), and the hook is generic enough that any later structured native widget reuses it. [widget-block-native-paint]

The rest of the flow matches every other kind. `editor-md`'s `table_spans` detector walks the same parse tree the mark-only providers use (GFM tables already parse via `Options::ENABLE_TABLES`) and yields each pipe-table block's `byte_range`. The app provider (`table_widget_decorations`) parses rows / cells / column alignments, builds a `TableWidget: BlockWidget` that measures its own height (row count × wrapped cell content at the paint-time width, via the trait's `measure(font_size, width)`) and emits the paint list, hides the source lines (`LineStyle.hide`), and emits the `BlockWidget` — fingerprint-cached on the shared `render_fp` like the `math_widget` / `mermaid_widget` slots, viewport-scoped. Reveal-on-cursor lifts the hide and shows the raw pipe-and-dash source, same per-block trigger as the others ([[spec:widget-reveal-block]]). A malformed table (no rule row) falls back to tinted source, never a crash ([[spec:widget-render-error-fallback]]). [widget-table-render]
status:: done
implements:: [[code:hiker/panels/buffer/widgets/tables/impl#[TableWidget]row_height]]
verifies:: [[code:hiker/panels/buffer/widgets/tables/tests/provider_emits_block_when_cursor_elsewhere]], [[code:hiker/panels/buffer/widgets/tables/tests/provider_reveals_source_when_cursor_inside]], [[code:hiker/panels/buffer/widgets/tables/tests/cell_carries_inline_styled_runs]], [[code:hiker/panels/buffer/widgets/tables/tests/paint_emits_rich_text_for_styled_cell]], [[code:hiker/panels/buffer/widgets/tables/tests/long_cell_reserves_height_for_wrapped_lines]], [[code:hiker/panels/buffer/widgets/tables/tests/narrow_column_never_shrinks_below_its_longest_word]], [[code:hiker/panels/buffer/widgets/tables/tests/fitting_table_stretches_to_full_width]], [[code:hiker/panels/buffer/widgets/tables/tests/bold_cell_reserves_more_width_than_plain]], [[code:hiker/panels/buffer/widgets/tables/tests/bold_header_stays_on_one_line_at_full_width]]
touches:: [[code:hiker/buffer]], [[code:hiker/tables]]
note:: `table_spans` walks the pulldown-cmark parse (`Tag::Table`/`TagEnd::Table`, GFM `ENABLE_TABLES`) yielding each table block's `byte_range` + per-column alignments; `editor-md` stays renderer-unaware. The app `TableWidget` parses rows/cells, lays out a grid (content column widths, wrapped cells), `measure(font_size,width)→height`, and builds an egui-free `paint_list` (header bg, grid rules/borders, alignment-honoring text runs, theme colors) via [[spec:widget-block-native-paint]] — no SVG raster. Reuses `emit_block_widget` for source-hide + reveal-on-cursor; malformed table (no ` | --- | ` rule row) is never detected → tinted source remains ([[spec:widget-render-error-fallback]]). Fingerprinted on the shared `render_fp`, gated on `render_widgets && is_markdown`, viewport-scoped · evidence: `editor/editor-md/src/tables.rs` (`table_spans` + `TableSpan` + `ColumnAlign`), `app/src/panels/buffer/widgets/tables.rs` (`TableWidget` parse/layout/`paint_list`), `app/src/panels/buffer/widgets/mod.rs` (`table_widget_decorations`), `decorations.rs` (`table_widget` cached! slot), `app/src/buffer.rs` (`table_widget` cache slot)

**Per-cell click-to-edit.** A table refines the whole-block [[spec:widget-block-click-to-edit]] model to cell granularity: clicking a cell drops the caret at the **end of that cell's source content** (caret ready to append), which lands inside the table's span and so triggers reveal — the source shows with the caret already in the right cell. Mechanism: the table parse is position-aware (each cell records the byte offset just past its content, escapes-and-trailing-whitespace correct), and `TableWidget::click_regions(font_size, width)` emits one normalized region per cell (row-major, `id = table_cell_id(content_hash, i)` under a dedicated tag bit). The native painter maps each region into the painted box and emits a per-region click-zone ([[spec:editor-widget-click-regions]]); the app registers every cell `id → absolute caret offset` (plus a whole-widget `id → table start` fallback for clicks off any cell) into the same `WidgetEditTargets` map mermaid/display-math use, so a cell click routes through the existing `place_caret_for_block_click` with no new drain bucket. Caret-to-exact-character within a cell is deferred — end-of-cell is the unit. [widget-table-cell-edit]
status:: done
verifies:: [[code:hiker/panels/buffer/widgets/tables/tests/cell_click_targets_land_at_end_of_cell_content]], [[code:hiker/panels/buffer/widgets/tables/tests/click_regions_one_per_cell_and_tagged]], [[code:hiker/panels/buffer/widgets/tables/tests/cell_offsets_survive_escaped_pipe]]
touches:: [[code:hiker/widget/blocks]]
note:: clicking a rendered TEXT cell places the caret at the END of that cell's source content (caret ready to append), refining [[spec:widget-block-click-to-edit]] to cell granularity. Position-aware parse: `split_row_cells` records each cell's content-end byte offset (source bytes — escapes precede trailing whitespace, so unescaping `|` doesn't shift it); `cell_ends` flattens them row-major. `TableWidget::click_regions(font_size,width)` emits one normalized region per cell (id `table_cell_id(content_hash,i)` under `TABLE_CELL_TAG`=bit 60, clear of mermaid bit 61 / wikilink bit 62) via the shared `cell_boxes` layout. `table_edit_targets` maps each TEXT cell id → absolute caret offset + a whole-widget `content_hash → table start` fallback, merged into the same `WidgetEditTargets` mermaid/math use, so the existing `place_caret_for_block_click` drain handles it with no new bucket. BLOCK cells (math / diagram / image, `cell_edit::cell_is_block`) are EXCLUDED — caret-in-span would reveal the table and pre-empt the in-place edit's double-click entry ([[spec:widget-table-cell-edit-inplace]]'s "block cells stay selection"); they enter edit via double-click / the right-click menu instead (fixed 2026-06-10, was every cell). Exact-character-within-cell deferred; end-of-cell is the unit. Tests: end-of-cell offsets, block-cell exclusion (`block_cells_are_excluded_from_edit_targets`), region tagging, escaped-pipe offset, native per-region zone · evidence: `app/src/panels/buffer/widgets/tables.rs` (`split_row_cells` offset-aware parse, `cell_ends`, `cell_boxes`, `TableWidget::click_regions`, `table_cell_id`/`TABLE_CELL_TAG`, `table_edit_targets`), `app/src/panels/buffer/widgets/mod.rs` (`widget_edit_targets` merges table targets), `editor/editor-egui/src/widget/blocks.rs` (native-path per-region zones), `editor/editor-egui/tests/block_widget_native_paint.rs::native_paint_emits_per_region_zones`

**In-place cell edit.** A single cell — a diagram cell especially — edits *in place* without the table collapsing to its raw pipe source. Triggers: **double-click a block cell** (math / mermaid / wavedrom / image) enters edit directly; **right-click any cell** adds "Edit diagram" (block) or "Edit cell" (text) to the table context menu. A plain single click is unchanged — a text cell still reveals-to-source, a block cell stays selection. While editing, the owning table **suppresses its whole-table reveal** (`TableProviderInputs::editing_table`, keyed by the table's byte start) so it stays fully rendered even though the caret sits inside it; two accent cues draw on top — a soft frame around the whole table, a brighter outline on the active cell (no dimming of others). The editor is a small popover anchored to the active cell (it may extend past the narrow cell width — a one-line fence is wider than the cell), a transient `editor-egui` editor seeded with ONLY that cell's source; its markdown live-preview re-renders the diagram as you type (the embedded-buffer edit-preview path), never covering the table. Each change **splices the cell's byte range** in the underlying markdown as an ordinary buffer edit (the cell's editable range is the trimmed content between unescaped `|`, `\|` preserved) — routed through the op-log binding, so undo / op-log / sync just work; buffer stays truth, no structured-model-then-reserialize. **Tab / Shift+Tab** move to the next / prev cell row-major across row boundaries; **Esc / click-outside** commits and closes. The editor-core widget stays interaction-agnostic — right-click, double-click, and the focused-cell are resolved host-side against the per-cell click zones (the same seam the overflow menu uses). [widget-table-cell-edit-inplace]
status:: done
touches:: [[code:hiker/buffer]], [[code:hiker/panels/buffer]], [[code:hiker/panels/buffer/clipboard_menu]], [[code:hiker/panels/buffer/decorations]], [[code:hiker/panels/buffer/widgets/tables]]
note:: edit ONE cell (a diagram especially) in place without the table collapsing to pipe source. Triggers: double-click a block cell (fast path) / right-click → "Edit diagram" (block) or "Edit cell" (text); a plain click is unchanged (text cells reveal, block cells stay selection). While editing, the owning table suppresses its whole-table reveal (`editing_table` keyed by byte start) so it stays rendered; a soft accent frame around the table + a brighter active-cell outline (no dimming). The overlay is a foreground `egui::Area` popover anchored to the active cell hosting a transient `editor-egui` editor seeded with only the cell source (mirrors `canvas::edit`); its markdown live-preview re-renders the diagram as you type. Each change splices the cell's byte range (trimmed content between unescaped `|`-preserving pipes) as an ordinary buffer edit through the op-log binding → undo/op-log/sync free; buffer stays truth. Tab/Shift+Tab move cells row-major across rows; Esc / click-outside commits + closes. editor-core stays interaction-agnostic — right-click / double-click / focused-cell resolved host-side against the per-cell click zones (the overflow-menu seam). Tests: cell editable-RANGE incl. `|`, reveal-suppression (editing table does NOT emit reveal), splice replaces only the cell range, block-vs-text gating, Tab nav index, non-editing byte-identical guard. Snapshot `target/table-snapshot-editing.png` shows the rendered table + frame + active-cell outline + popover · evidence: `app/src/panels/buffer/table_cell_edit.rs` (overlay `show`/`enter`/`exit`/`splice`/`move_cell`/`draw_cues`, double-click + right-click trigger resolution, `CellEdit` state), `app/src/panels/buffer/widgets/tables/cell_edit.rs` (`TableCellTarget` + `table_cell_targets` editable-RANGE derivation + `cell_is_block`), `app/src/panels/buffer/widgets/tables.rs` (`split_row_cells`→`SplitCell` start+end, `cell_ranges`, `TableProviderInputs::editing_table` reveal-suppression), `app/src/buffer.rs` (`Buffer::editing_cell`), `app/src/panels/buffer/decorations.rs` (`DecoRebuildCtx::editing_table` + fp), `app/src/panels/buffer/clipboard_menu.rs` (menu item), `app/src/panels/buffer/mod.rs` (trigger wiring + overlay drive), `tools/table-snapshot` (`TABLE_SNAPSHOT_EDITING`)


## Error fallback

A renderer returning `None` (math parse failure) or `Err` (Mermaid / WaveDrom parse/unsupported-type) never breaks the buffer: the provider falls back to the current tinted-source mark (the `equations.rs` / `diagrams.rs` mark styles — `mermaid_decorations` / `wavedrom_decorations`), plus a small error glyph in the gutter or at the span end with the parse error in its tooltip. The user keeps seeing and editing their source. [widget-render-error-fallback]
status:: partial
verifies:: [[code:hiker/panels/buffer/widgets/tables/tests/provider_emits_nothing_for_malformed_table]]
note:: a `render_math` `None` (parse/layout failure) emits nothing for that span — the `math_decorations` tinted-source mark remains the fallback; source for an unrenderable display block is NOT hidden. Never panics. GAP: the spec's gutter error glyph + parse-error tooltip is not yet emitted · evidence: `app/src/panels/buffer/widgets/mod.rs` (the `render_math(..)? else continue` paths)


## Toggle and gating

- **`Render widgets`** — a new View-menu entry (`editor.md`'s [[spec:editor-view-options-menu]]), default on, in-memory in v1 (persistence rides `settings.md`'s editor section later). Off shows the tinted-source marks (today's behavior). Distinct from [[spec:view-live-preview-toggle]], which governs Tier 1 marker fade — a user can have live preview on and rendered widgets off, or either alone. [widget-render-toggle]
status:: done
implements:: [[code:hiker/panels/buffer/toolbar_menus/render_toggles]]
touches:: [[code:hiker/buffer]]
note:: View-menu entry, default on, in-memory per-buffer; flips `buffer.render_widgets` + clears the deco cache. Distinct from `Live preview` · evidence: `app/src/panels/buffer/toolbar_menus.rs` (`Render widgets` checkbox), `app/src/buffer.rs` (`Buffer::render_widgets`)
- **`Live edit preview`** — a second View-menu entry, default on, gating only the floating edit popup. The popup shows when `render_widgets && live_edit_preview && is_markdown`; in-place rendering and diagram interactivity ride `render_widgets` alone, so turning the popup off leaves rendered widgets and clickable links intact. [widget-edit-popup-preview]
- **Markdown-buffer-gated.** Like live preview, the providers emit nothing when the buffer isn't rendered as markdown ([[spec:live-preview-disabled-non-md]]). [widget-render-gating]
status:: done
touches:: [[code:hiker/panels/buffer]]
note:: the widget provider runs only for markdown buffers (`.md`, or `.txt` with render-txt-as-markdown), independent of the live-preview flag · evidence: `app/src/panels/buffer/mod.rs` (`is_markdown` gate on the `math_widget` layer)


## Deferred

- **Inline images (`![alt](url)`).** Same pipeline with an image decode instead of an SVG render — texture cache, host paint, reveal-on-cursor all reuse. Adds fetch (local-path first; remote fetch needs the security model live preview's Tier 3 flags) and alt-text fallback. Lands as its own application of [[spec:widget-render-pipeline]] once math/mermaid are solid. [widget-image-render]
status:: planned
note:: inline `![alt](url)` via the same pipeline (image decode → texture); local-path first, remote fetch needs a security model; deferred
- **Drag-to-resize.** Resizing a rendered image/diagram by dragging its corner, persisting the size in the source (an HTML width attr or a hiker-namespaced marker). UI + source-write contract deferred. [widget-render-resize]
status:: planned
note:: drag-to-resize a rendered image/diagram, persisting size in source; deferred
- **Per-vault math preamble file.** The `MathOptions.preamble` field is plumbed; the file path + loader land with `settings.md`'s vault-config work. [widget-latex-preamble]
status:: planned
note:: per-vault macro file (`\newcommand`/`\def`); field plumbed now, path+loader deferred to `settings.md` vault-config · evidence: `MathOptions.preamble`
- **Math/diagram export.** Copy-as-image / copy-as-SVG on a rendered widget. Trivial given the SVG is already in hand; deferred until asked. [widget-render-export]
status:: planned
note:: copy-as-image / copy-as-SVG on a rendered widget; deferred
- **Background rendering for heavy diagrams.** v1 renders + rasterizes synchronously on the decoration-cache path (cached, viewport-scoped, so cost is bounded to on-screen spans). If a large Mermaid layout (dagre on a big graph) janks a frame, move the render to a background task that paints a spinner placeholder until the texture is ready — same `spawn_blocking` shape the embedder uses ([[spec:embedder-spawn-blocking]]). Deferred until profiling shows it's needed; the synchronous path is correct, just potentially slow on pathological inputs. [widget-render-async]
status:: planned
note:: background render+raster for heavy diagrams with a spinner placeholder ([[spec:embedder-spawn-blocking]] shape); deferred until profiling shows the synchronous cache path janks


## Out of scope

- **Editing diagrams visually.** Mermaid/WaveDrom/tables are edited as source, not through a WYSIWYG canvas. The draw.io family (`ideas.md` `[drawio-source-ingest]`) is a separate source-type concern, not an editor widget.
- **HTML/CSS rendering inside markdown.** Inline `<div>`/styled HTML is not rendered; that's the `hiker-render/htmlview` surface (the no-JS HTML/CSS renderer, e.g. the ZIM viewer in `zim.md`), not the markdown editor.
- **Syntax highlighting inside fenced code** — already owned by [[spec:editor-code-syntax-highlight]] (`live-preview.md`), a styling layer, not a widget.
- **Anything mutating the source file.** Widgets are decoration-only (drag-to-resize, when it lands, is the single deliberate exception and writes through the normal edit path).

## Registry imports (from status.md)

Entries imported from the retired status registry that had no anchor in this doc —
re-home them into the relevant sections as the doc evolves.

- **widget-renderers-svg-out** — LaTeX via `hiker-math` (`render_latex`/`render_latex_with_preamble`), Mermaid via `hiker-mermaid` (`render`); host rasterizes SVG→RGBA, editor uploads+blits. Contract established end-to-end; provider→decoration wiring pending [widget-renderers-svg-out]
  status:: partial
  touches:: [[code:hiker/panels/buffer/widgets/render]], [[code:hiker/widget/texture_cache]]
  note:: evidence: `app/src/panels/buffer/widgets/render.rs` (raster), `editor/editor-egui/src/widget/texture_cache.rs` (blit)
- **widget-render-providers** — LaTeX + Mermaid + tables: detectors report spans; app providers emit `InlineWidget` (atomic) / `BlockWidget` decorations with reveal. Math/Mermaid render via SVG→texture; tables paint natively via the `paint_list` hook ([[spec:widget-table-render]]) [widget-render-providers]
  status:: done
  implements:: [[code:hiker/panels/canvas/content/EditorPane#decoration_cache]], [[code:hiker/panels/canvas/content/paint_editor]], [[code:hiker/panels/canvas/content/card_decoration_ctx]]
  touches:: [[code:hiker/panels/buffer]], [[code:hiker/equations]]
  note:: evidence: `editor/editor-md/src/equations.rs` (`math_spans`) + `diagrams.rs` (`mermaid_spans`) + `tables.rs` (`table_spans`) detect, `app/src/panels/buffer/widgets/mod.rs` (`math_widget_decorations` / `mermaid_widget_decorations` / `table_widget_decorations` emit)
- **widget-render-pipeline** — full LaTeX + Mermaid pipeline: detect (editor-md) → render+rasterize (`render_math` / `render_mermaid` → `RenderedWidget`) → `InlineWidget`/`BlockWidget` decoration → texture blit [widget-render-pipeline]
  status:: done
  touches:: [[code:hiker/panels/buffer/widgets/render]]
  note:: evidence: `app/src/panels/buffer/widgets/render.rs` (`render_math`), `widgets/mod.rs` (decoration emission), `editor-egui` blit
- **widget-render-module-split** — `editor-md` reports spans (no `hiker-render`/egui dep); `app` owns render, rasterize, and the widget structs (`InlineMath`/`DisplayMath`) implementing the egui-free `editor-core` traits [widget-render-module-split]
  status:: done
  touches:: [[code:hiker/equations]], [[code:hiker/panels/buffer/widgets]]
  note:: evidence: `editor/editor-md/src/equations.rs` (detect only), `app/src/panels/buffer/widgets/mod.rs` (render+emit)
- **widget-inline-math-baseline** — `InlineMath::measure`/`baseline` return logical points (physical px ÷ dpr); blit sizes the box from `measure` so HiDPI renders 1×. `apply_line_height_decorations` now scans `InlineWidget` decorations in the height-tracked layer and grows each line's text-row height to `max(text_height, tallest widget.measure().1)` (before the soft-wrap multiplier; hidden lines stay collapsed). The stopgap height-cap in `blit_inline_pixels` is removed — the texture paints at its full measured height, baseline-aligned to the surrounding text baseline (`span.baseline_y = centered galley top + font_ascent`) via `InlineWidget::baseline()`. Row growth feeds cursor rect / hit-testing / scroll / the soft-wrap multiplier unchanged. Tests: tall widget grows its row, plain line unchanged, sub-line-height widget doesn't shrink, caret/`y_at_row_top` reflects the grown row [widget-inline-math-baseline]
  status:: done
  touches:: [[code:hiker/panels/buffer/widgets]]
  note:: evidence: `app/src/panels/buffer/widgets/mod.rs` (`InlineMath`), `editor/editor-egui/src/widget.rs` (`apply_line_height_decorations`, `blit_inline_pixels`, `galley_ascent`), `editor/editor-egui/tests/inline_widget_height.rs`
- **widget-block-view-zone** — display math + mermaid + wavedrom + tables emit a `BlockWidget` with own-height `measure(font_size,width)` (logical points). The diagram widgets scale own-height to fit the content `width` (`fit_block_height` — shrink a too-wide render + its height together, never upscale) so a wide diagram fills the column with no empty vertical band; `measure` is handed the content width (full width − `content_origin_x`) to match the paint box. Tables wired ([[spec:widget-table-render]]) [widget-block-view-zone]
  status:: done
  touches:: [[code:hiker/panels/buffer/widgets]]
  note:: evidence: `app/src/panels/buffer/widgets/mod.rs` (`DisplayMath`, `MermaidWidget`, `WaveDromWidget`), `widgets/placement.rs` (`fit_block_height`), `widgets/tables.rs` (`TableWidget`), `editor/editor-egui/src/widget.rs` (measure gets content width)
- **widget-block-click-to-edit** — a body click on a rendered block widget (mermaid diagram / display math) places the caret inside its source span, triggering the existing reveal (source shows + edit-preview popup) — the way into otherwise-hidden source. `widget_edit_targets` maps each on-screen block widget's whole-widget id (its render `content_hash`, raster-free) → `inner_range.start`. Both block kinds set `handles_click()=true`. The drain routes a click via `widgets::classify_widget_click` — **membership-keyed consumers (diagram registry, then edit-target map) are checked BEFORE the wikilink BIT test**, since a bare `content_hash` body-click id can coincidentally set ANY reserved tag bit. BUGFIX: the wikilink check was a bit-test running first, so a `content_hash` with bit 62 (`WIKILINK_WIDGET_TAG`) set — ~half of diagrams — was stolen into the wikilink handler and silently dropped ("clicking the diagram does nothing"); tables were immune only because their cell ids (`table_cell_id`) mint bit 62 clear, and mermaid links too (`region_click_id`), so only the bare-`content_hash` body-click path was exposed. A genuine wikilink pill id is never a content hash, so membership-first is unambiguous. Regression test: `content_hash | WIKILINK_WIDGET_TAG` present in `edit_targets` classifies as `Edit`. Tables refine this to per-cell granularity (caret at end-of-cell), see [[spec:widget-table-cell-edit]] [widget-block-click-to-edit]
  status:: done
  implements:: [[code:hiker/panels/buffer/widgets/WidgetEditTargets]], [[code:hiker/panels/buffer/widgets/classify_widget_click]]
  verifies:: [[code:hiker/panels/buffer/widgets/tests/edit_target_maps_mermaid_widget_id_into_its_span]], [[code:hiker/panels/buffer/widgets/tests/edit_target_maps_display_math_widget_id]], [[code:hiker/panels/buffer/widgets/tests/edit_target_maps_wavedrom_widget_id_into_its_span]]
  note:: evidence: `app/src/panels/buffer/widgets/mod.rs` (`widget_edit_targets`, `WidgetEditTargets`, `DisplayMath::handles_click`), `app/src/panels/buffer/mod.rs` (`build_diagram_interaction` builds the map; the `WidgetClick` drain routes by map membership)
- **widget-cursor-reveal** — collapse-to-source model implemented for math (see [[spec:widget-reveal-inline]] / [[spec:widget-reveal-block]]); the additive live popup is the separate [[spec:widget-edit-popup-preview]] [widget-cursor-reveal]
  status:: done
  touches:: [[code:hiker/panels/buffer/widgets]]
- **widget-render-disk-cache** — persisted layer below the in-memory `CachedDeco`/texture caches: rasterized math/Mermaid/WaveDrom widgets stored as `<vault>/.hiker/diagram-cache/<domain>-r<rev>-<hash:016x>.png` (straight RGBA8; PNG self-describes w/h) + a `.base` sidecar (4 LE bytes) for inline-math baseline. Hit short-circuits the `resvg` blit (and the LaTeX/WaveDrom layout; Mermaid still parses for regions). Domain-prefixed filenames keep math/mermaid/wavedrom distinct even at equal hash. Best-effort 64 MB oldest-by-mtime LRU sweep runs once/session via a `OnceLock` on first `store`; it first purges stale-revision (and pre-revision-scheme) entries. `<rev>` is `Domain::revision` (`disk_cache.rs`), a per-domain const bumped whenever that renderer's pixels change for identical input (incl. dependency changes, e.g. `hiker-graph` layout for Mermaid) — stale entries become unreachable immediately and are deleted by the purge. Tables paint natively (no raster) → nothing to persist. Canvas node embeds pass `None` (no vault-scoped cache). All I/O best-effort: any decode/write error degrades to a live render. Tests: round-trip block + inline-baseline, miss→None, disabled→no-ctx, domain non-collision, sweep eviction [widget-render-disk-cache]
  status:: done
  implements:: [[code:hiker/panels/canvas/edit/render_text_widget]]
  touches:: [[code:hiker/buffer_view]], [[code:hiker/panels/buffer]], [[code:hiker/panels/buffer/decorations]], [[code:hiker/panels/buffer/widgets/disk_cache]], [[code:hiker/panels/buffer/widgets/render]]
  note:: evidence: `app/src/panels/buffer/widgets/disk_cache.rs` (`DiagramCacheCtx` + `Domain` + `load`/`store` + `sweep_to_budget`), `app/src/panels/buffer/widgets/render.rs` (`render_math` / `render_mermaid` / `render_wavedrom` / `render_mermaid_with_regions` check-then-blit-then-write-back, keyed by the existing `content_hash`), `app/src/panels/buffer/decorations.rs` (`DecoRebuildCtx.diagram_cache` threaded into the three widget providers below the `cached!` layer), `app/src/panels/buffer/mod.rs` (`diagram_cache_ctx` builds the ctx from the vault root + `[render] cache_diagrams`), `app/src/buffer_view.rs` (embed path), `edit_preview.rs` (popup path)
- **render-cache-diagrams-toggle** — `[render] cache_diagrams` (default on, vault scope) gates the persisted diagram-cache layer ([[spec:widget-render-disk-cache]]). Off → render path skips the disk cache, in-memory caches carry the session. Live-applied (read each frame when building the cache ctx); no restart. Surfaced in Settings via [[spec:settings-section-render]] [render-cache-diagrams-toggle]
  status:: done
  implements:: [[code:hiker/config/patch/ELIGIBLE_USER]], [[code:hiker/config/sections/RenderConfig]], [[code:hiker/config/Config#render]], [[code:hiker/panels/settings/impl#[`SettingsCtx<'a>`]render_section]]
  touches:: [[code:hiker/panels/buffer]]
  note:: evidence: `core/src/config/sections.rs` (`RenderConfig::cache_diagrams` default true), `core/src/config/mod.rs` (`Config.render`), `core/src/config/patch.rs` (`render.cache_diagrams` eligible vault bool + test `render_cache_diagrams_is_eligible_vault_bool`), `app/src/panels/buffer/mod.rs` (`diagram_cache_ctx` reads the flag)
- **widget-render-order** — LaTeX first, Mermaid second, tables last [widget-render-order]
  status:: planned
- **widget-latex-inline** — `$…$` → `render_math(.., MathKind::Inline, ..)` → baseline-aligned `InlineWidget` (atomic). Row growth for tall formulas handled by [[spec:widget-inline-math-baseline]] [widget-latex-inline]
  status:: done
  touches:: [[code:hiker/equations]], [[code:hiker/panels/buffer/widgets]]
  note:: evidence: `editor/editor-md/src/equations.rs` (`MathKind::Inline` span), `app/src/panels/buffer/widgets/mod.rs` (`InlineMath`)
- **widget-latex-display** — `$$…$$` → `render_math(.., MathKind::Display, ..)` → full-width `BlockWidget`; source hidden in place [widget-latex-display]
  status:: done
  touches:: [[code:hiker/equations]], [[code:hiker/panels/buffer/widgets]]
  note:: evidence: `editor/editor-md/src/equations.rs` (`MathKind::Display` span), `app/src/panels/buffer/widgets/mod.rs` (`DisplayMath`)
