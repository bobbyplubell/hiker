# About

A second Markdown page, loaded from a file.

> [!tip] How it works
> `include_dir!` embeds the `pages/` folder into the wasm binary at compile
> time, so it works in the browser with no filesystem at runtime.

## Headings space out

### …at three different sizes

Move the cursor onto any line to see its raw Markdown source reappear.
