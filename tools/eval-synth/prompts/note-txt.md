You are writing one short plain-text note for a personal vault. The
file extension will be `.txt`, NOT markdown.

Topic: {{topic}}
Aspect: {{aspect}}
{{#crosslinks}}

Constraints:
- No `#` markdown headings. Plain prose.
- {{length}}.
- Section labels (if any) should be SHORT ALL-CAPS LINES on their own (like
  "BACKGROUND" or "FAILURE MODES"), 2-6 words, with blank lines around
  them. Use them sparingly — at most 2 in a short note, 4 in a long one.
  Do not use them at all if the content flows fine without.
- The first line should be a concrete title (3-7 words), capitalized as a
  title (NOT all-caps), on its own line, followed by a blank line. Do
  not prefix with `# `.
- No frontmatter. Do not wrap the note in a code fence.
- Do not preface with "Here is a note...". Just write the note.
- Prose preferred. A short numbered list or bullet list is fine if it
  fits, but not as the entire note.

Write the note now.
